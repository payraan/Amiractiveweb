# درخواست ممیزی مستقل — پلتفرم نارمون

> این متن برای یک ایجنت/مهندس مستقل نوشته شده که ریپو را می‌گیرد و باید
> عمیقاً بررسی‌اش کند. مالک پروژه آن را کپی می‌کند و می‌فرستد.

---

## ۰. قواعد بازی — این را اول بخوان

**۱. ادعای بدون شاهد ارزش ندارد.** برای هر یافته باید بنویسی:
   - فایل و شماره‌ی خط
   - **سناریوی مشخص شکست**: چه ورودی/وضعیتی → چه خروجی غلطی
   - **چطور راستی‌آزمایی کردی**: کد را خواندی؟ اجرا کردی؟ کوئری زدی؟

   «این ممکن است مشکل‌ساز باشد» یافته نیست. «اگر کاربر X را بفرستد، تابع Y
   مقدار Z را برمی‌گرداند که غلط است چون…» یافته است.

**۲. اگر چیزی عجیب دیدی، اول فرض کن عمدی است.** این کدبیس کامنت‌های
   فارسی مفصل دارد و بیشتر تصمیم‌های عجیب، دلیلشان بالای همان تابع نوشته
   شده. اگر کامنت دلیلی می‌دهد و تو فکر می‌کنی دلیل غلط است، **آن استدلال
   را نقض کن**، نه اینکه کامنت را ندیده بگیری.

**۳. کاری نکن که وضعیت را عوض کند.** `git push` نزن، کامیت نکن، مهاجرت
   اجرا نکن، به دیتابیس پروداکشن وصل نشو. فقط خواندن و — اگر خواستی —
   اجرای محلی.

**۴. اولویت با پول است.** یک باگ در لایه‌ی پول از ده باگ رابط کاربری
   مهم‌تر است.

---

## ۱. پلتفرم چیست

**نارمون** — بازار پیش‌بینی فارسی‌زبان، سه سطح روی یک دیتابیس:

| سطح | مسیر | احراز هویت |
|---|---|---|
| سایت | `/` و بقیه | کوکی HMAC `amir_session` |
| مینی‌اپ تلگرام | `/app` | هدر `x-tg-auth` |
| ربات تلگرام | وبهوک `/api/tg/webhook` | `tg_user_id` |

`currentPlayerId()` هر سه را به یک شناسه می‌رساند. **هیچ روت اختصاصی
مینی‌اپی وجود ندارد و نباید ساخته شود.**

**استک:** Next.js 16 (App Router)، React 19، TypeScript، PostgreSQL با
`pg` خام (بدون ORM)، Tailwind v4. مسیر `@/*` → `src/*`. **بدون تست
خودکار** — به همین دلیل این ممیزی مهم است.

**چهار بازی، دو اقتصاد:**

1. **بازار ایران** (`/iran`, `src/lib/iran.ts`) — استخر parimutuel با تتر.
   **تنها بازی با پول واقعی.**
2. **ترید/آرنا** (`/arena`, `src/lib/poly.ts`) — دیتای Polymarket، امتیازی.
3. **نبض بازار** (`/predict`, `src/lib/settle.ts`) — حدس قیمت، امتیازی.
4. **کمبو** (`/combos`, `src/lib/combos.ts`) — چندپایه، امتیازی.

**نکته‌ی حیاتی برای فهم کد:** پلتفرم الان در **نسخه‌ی دمو** است
(`src/lib/platform-mode.ts`). همه‌ی موجودی‌ها مجازی‌اند، واریز و برداشت
بسته‌اند. ولی **کد هر دو حالت را دارد** و روزی با یک متغیر محیطی به حالت
واقعی می‌رود. **هر دو مسیر را بررسی کن.**

---

## ۲. ناوردهایی که هرگز نباید بشکنند

اینها را `scripts/health.sql` می‌سنجد (۴۲ چک). **اجرایش کن و بعد سعی کن
راهی پیدا کنی که یکی‌شان را از طریق API بشکنی.**

```
۱. usdt_balance + demo_balance  =  SUM(wallet_ledger.amount)
۲. demo_balance                 =  SUM(wallet_ledger.demo)
۳. ir_markets.yes_total/no_total = مجموع ir_bets.stake همان طرف
۴. ir_markets.creator_cut       =  SUM(ir_bets.creator_cut)
۵. ir_markets.early_cut         =  SUM(ir_bets.early_cut)
۶. جمع پرداختی یک بازار         ≤  کل استخر
۷. هیچ موجودی منفی
۸. سهم نفرات اول                ≤  کمیسیون پلتفرم
```

**سؤال کلیدی:** آیا توالی‌ای از فراخوانی API وجود دارد که یکی از این‌ها را
بشکند؟ مخصوصاً با درخواست‌های **هم‌زمان**.

---

## ۳. نقاط پرخطر — اینجا وقت بگذار

### الف) لایه‌ی پول — بالاترین اولویت

**`src/lib/iran.ts`**

- `moveFunds()` — تنها تابعی که موجودی را عوض می‌کند. قراردادش می‌گوید
  ردیف بازیکن باید از قبل با `FOR UPDATE` قفل شده باشد.
  **بررسی کن: آیا همه‌ی فراخوان‌ها این قرارداد را رعایت می‌کنند؟ اگر یکی
  نکند چه می‌شود؟**
- ستون `amount` مبلغ **کل** است و `demo` سهم دموی همان مبلغ (علامت‌دار).
  موجودی واقعی = جمع تجمعی `(amount − demo)`.
- `settleIrMarket()` — سه مسیر: تسویه‌ی عادی، ابطال ضریب‌پایین، ابطال
  بی‌برنده. **ریاضی هر سه را جدا بررسی کن.** آیا مجموع پرداختی‌ها می‌تواند
  از استخر بیشتر شود؟ با اعداد مرزی (استخر خیلی نامتوازن، یک شرط‌بند،
  ضریب زیر ۱) چه می‌شود؟
- `oddsFor()` — کامنتش می‌گوید «باید همان عددی را بدهد که تسویه می‌پردازد»
  و «سه بار در این پروژه شکست». **بررسی کن الان می‌خواند یا نه.**

**`src/app/api/ir/bet/route.ts`** — ثبت شرط. مسیر پول واقعی.
**`src/lib/withdrawal.ts`** — برداشت. `realOnly: true` یعنی هرگز از بونوس.
**`src/lib/deposit-sync.ts`** — شارژ واریز. یکتاسازی روی `txid`.

### ب) اقتصاد و ضد تقلب

**تز محصول: «امتیاز فقط از مهارت می‌آید، نه شانس و نه پول.»**

- **امتیازدهی صفر-انتظار** (`src/lib/game.ts`): حدس کور باید به‌طور میانگین
  امتیاز **منفی** بگیرد. **این را عددی بسنج** — شبیه‌سازی مونت‌کارلو بزن
  روی دارایی‌های مختلف و تایم‌فریم‌های مختلف. اگر جایی امید امتیاز مثبت
  است، آن یک باگ اقتصادی است. (سابقه: یک بار ۲۲ دارایی از ۴۲ مزرعه‌ی
  امتیاز بودند.)
- **`vol_scale`** — آستانه با نوسان دارایی مقیاس می‌خورد و **در لحظه‌ی
  ساخت راند قفل می‌شود**. آیا می‌شود با انتخاب زمانِ ثبت، امید امتیاز را
  مثبت کرد؟
- **سهم سازنده و سهم نفرات اول** — ادعای کد این است که فارم‌کردن همیشه
  ضرر می‌دهد چون هر دو سهم از معادله حذف می‌شوند:
  `خالص = S×(۱ − p − c − e) + S×c + S×e − S = −p×S`
  **این را نقض کن یا تأیید کن.** آیا ترکیبی از حساب‌ها، رفرال، بوست و
  زمان‌بندی وجود دارد که خالصش مثبت شود؟
- **لیدربورد** — سقف تعداد دارد تا با خرید MOON نشود رتبه خرید. **بررسی
  کن سقف واقعاً کار می‌کند.**

### ج) احراز هویت و امنیت

- `src/lib/session.ts` — کوکی `playerId.expiry.sig` با HMAC. **بدون
  `SESSION_SECRET` باید fail-closed باشد.**
- `src/lib/admin.ts` — کوکی جدا، ۱۲ ساعته.
- `src/lib/tg-auth.ts` — اعتبارسنجی `initData` تلگرام.
- `src/middleware.ts` — سقف نرخ در حافظه (با هر دیپلوی صفر می‌شود).
- `src/app/api/wallet/webhook/route.ts` — وبهوک درگاه، با توکن در
  query string.

**سؤال‌ها:** آیا جایی هست که هویت از **بدنه‌ی درخواست** خوانده شود به‌جای
کوکی/هدر؟ آیا timing attack روی مقایسه‌ی امضاها ممکن است؟ آیا روت ادمینی
هست که `verifyAdmin` را جا انداخته باشد؟ آیا IDOR هست — یعنی کاربری
بتواند با عوض‌کردن یک شناسه به داده‌ی دیگری برسد؟

### د) هم‌زمانی و یکتاسازی

- تسویه‌ی دوباره‌ی یک بازار → پرداخت دوباره؟
- دو درخواست شرط هم‌زمان با موجودی کافی برای فقط یکی؟
- دو وبهوک واریز با یک `txid`؟
- دو برداشت با یک `unique_param`؟
- سهمیه‌ی ماهانه‌ی دمو (`src/lib/demo-allowance.ts`) دو بار؟
- کرون تسویه‌ی هم‌زمان روی دو نمونه؟

### ه) نسخه‌ی دمو در برابر واقعی

`src/lib/platform-mode.ts` — پیش‌فرض **دمو** است (fail-safe).

**بررسی کن:** آیا مسیری هست که در حالت دمو پول واقعی جابه‌جا کند، یا در
حالت واقعی پول مجازی را قابل برداشت کند؟ آیا نگهبان‌ها همه‌ی نقاط ورود و
خروج پول را پوشش می‌دهند؟

---

## ۴. باگ‌های شناخته‌شده — دوباره گزارش نکن

اینها را می‌دانیم. اگر **جزئیات تازه‌ای** داری بگو، وگرنه رد شو:

1. سقف نرخ در حافظه است و با هر دیپلوی صفر می‌شود.
2. `robots.txt` و `sitemap.xml` وجود ندارند.
3. ثبت‌نام سایت هیچ اعتبارسنجی تلگرامی ندارد → چندحسابی ممکن است.
4. تأیید نهایی برداشت در درگاه دستی است.
5. همه‌ی دیتای نبض بازار به Yahoo Finance گره خورده (تک‌نقطه‌ی اتکا).
6. `vol_scale` در لحظه‌ی ساخت راند قفل می‌شود (زمان‌بندی نوسان).
7. تحلیل رفتار برای مقیاس چند میلیون رویداد به جدول تجمیع نیاز دارد.
8. کاور بازار در سایت و مینی‌اپ دیده نمی‌شود (فقط تلگرام).

---

## ۵. چیزی که ازت می‌خواهم

به این ترتیب:

1. **ناوردهای پول** — سعی کن بشکنی‌شان. `scripts/health.sql` را اجرا کن،
   بعد از هر سناریو دوباره اجرا کن.
2. **ریاضی تسویه** — سه مسیر بازار ایران، با اعداد مرزی.
3. **امتیازدهی صفر-انتظار** — شبیه‌سازی عددی، نه خواندن کد.
4. **ضد فارم** — نقض یا تأیید معادله‌ی بالا.
5. **امنیت** — احراز هویت، IDOR، هم‌زمانی، ورودی‌های اعتبارسنجی‌نشده.
6. **حالت دمو/واقعی** — پوشش نگهبان‌ها.
7. **هر چیز دیگری که پیدا کردی.**

---

## ۶. قالب خروجی

برای هر یافته دقیقاً این ساختار:

```
### [شدت: بحرانی | بالا | متوسط | پایین] عنوان کوتاه

**فایل:** src/lib/example.ts:123
**سناریوی شکست:** کاربر A با موجودی ۱۰ تتر دو درخواست هم‌زمان
  می‌فرستد → هر دو موفق می‌شوند → موجودی منفی می‌شود.
**چطور راستی‌آزمایی کردم:** کد را خواندم و با دو curl هم‌زمان روی
  دیتابیس محلی تست کردم؛ خروجی: …
**چرا مهم است:** …
**پیشنهاد رفع:** …
**اطمینان:** بالا | متوسط | حدس
```

⚠️ **فیلد «اطمینان» را صادقانه پر کن.** یک یافته‌ی «حدس» که صادقانه
برچسب خورده، ارزشمند است. یک حدس که «بحرانی» جا زده شود، وقت همه را
تلف می‌کند.

**در پایان بنویس:**
- چه بخش‌هایی را خواندی و چه بخش‌هایی را نخواندی (نقشه‌ی پوشش)
- چه چیزهایی را بررسی کردی و **سالم بودند** — این هم به‌اندازه‌ی
  یافته‌ها مهم است
- اگر جایی نتوانستی نتیجه بگیری، بگو کجا و چرا

---

## ۷. اجرای محلی (اگر خواستی)

```bash
npm install
npx tsc --noEmit     # باید پاک باشد
npm run lint         # baseline = ۳۳ مورد (۲۷ خطا + ۶ هشدار) — بالاتر نرود
npm run dev          # localhost:3000
```

دیتابیس محلی لازم است (`DATABASE_URL` در `.env.local`). اگر نداری، بیشتر
تحلیل ایستا انجام بده و صریح بگو چه چیزی را نتوانستی اجرا کنی.

⚠️ `npm run build` و بعد `npm run dev` بدون پاک‌کردن `.next` همه‌ی روت‌ها
را ۴۰۴ می‌کند. اگر دیدی همه‌چیز ۴۰۴ است: `rm -rf .next`.
