# مانیتورینگ نارمون — راهنمای عملی

> هر خط لاگ یک JSON تک‌خطی روی stdout است و Railway فیلدهایش را ایندکس می‌کند.
> در Railway → Logs، با `@نام‌فیلد:مقدار` فیلتر بزن.
>
> ⚠️ `message` و `level` نام‌های رزرو Railway هستند و عمدا همان‌ها استفاده
> شده‌اند. عوضشان نکن، وگرنه Railway خط را خالی نشان می‌دهد.

---

## ۱. سه فیلتری که هر روز باید ببینی

```
@level:error              هر چیزی که واقعا خراب است
@evt:cron.tick            کرون هر ۱۵ دقیقه — نبودنش یعنی کرون مرده
@evt:deposit.credited     هر واریز موفق
```

اگر `cron.tick` بیش از ۲۰ دقیقه دیده نشد، سرویس کرون Railway را چک کن. تسویه،
واریز، آشتی برداشت و صف پخش همه از همان یک تیک می‌آیند.

---

## ۲. ردیابی یک کاربر

```
@playerId:7               همه‌چیزِ یک حساب
@tgUserId:123456789       همان از سمت تلگرام
@who:p:7                  درخواست‌های API همان حساب
```

---

## ۳. سیگنال‌های فارم و سوءاستفاده

اینها مهم‌ترین بخش‌اند. هر کدام یک درِ ورودیِ ارزش به سیستم‌اند.

| فیلتر | چه می‌گوید | چه‌وقت نگران شو |
|---|---|---|
| `@evt:auth.registered` | ثبت‌نام سایت (**بدون اثبات هویت**) | چند حساب پشت‌سرهم از یک `ip`، یا همه با `viaRef:true` |
| `@evt:tg.account_created` | ساخت حساب از مینی‌اپ | جهش ناگهانی بدون کمپین |
| `@evt:referral.attached` | یک حساب به دعوت‌کننده وصل شد | چند `playerId` زیر یک `referrerId` در چند دقیقه |
| `@evt:referral.paid` | **MOON از هیچ ساخته شد** | مجموع `commission` رشد کند بدون رشد `credits.purchased` |
| `@evt:bonus.granted` | هدیه‌ی کانال داده شد | تعدادش باید با رشد واقعی کانال بخواند |
| `@evt:bonus.denied` | هدیه رد شد | `state:left` عادی است؛ انبوه `state:unknown` یعنی ربات از گروه بیرون افتاده |
| `@evt:challenge.entered` | ورود به چالش پراپ | ورودی MOON است و جایزه حساب واقعی — تنها پل MOON به پول |
| `@evt:tg.link_failed` | اتصال ناموفق | `reason:tg_taken` یعنی یک تلگرام دنبال حساب دوم است |
| `@evt:ratelimit.blocked` | سقف نرخ خورد | یا سوءاستفاده، یا سقف برای کاربر سالم تنگ است |
| `@evt:referral.self_attempt` | خود-ارجاعی | بسته است، ولی تلاشش خودش سیگنال است |

**نکته‌ی مهم:** `ip` مدرک نیست. مخاطب ما پشت VPN است و ده‌ها کاربر واقعی از یک
آی‌پی خروجی می‌آیند. آی‌پی فقط سرنخ است؛ لنگر واقعی `tg_user_id` است.

---

## ۴. مسیر پول

```
@evt:deposit.*      واریز — credited / skipped / list_failed
@evt:withdraw.*     برداشت — requested / rejected / submitted / completed / gateway_*
@evt:ir.bet         هر پیش‌بینی تتری (فیلد demo سهم بونوس را جدا می‌کند)
@evt:ir.settled     تسویه‌ی بازار — paid / void_low_odds / void_no_winners
@evt:credits.purchased   خرید MOON
@evt:boost.purchased     خرید بوست
@evt:gateway.*      خودِ درگاه Zovix
```

`ir.settled` مهم‌ترینشان است: `pool`، `paid` و `commission` را با هم می‌دهد، پس
درآمد هر بازار بدون هیچ کوئری دیتابیسی دیده می‌شود.

⚠️ `@evt:ir.settle_failed` یعنی **پول کاربران در وضعیت `settling` قفل مانده**.
این فوری‌ترین هشدار پلتفرم است.

---

## ۵. لایه‌ی تلگرام

```
@evt:tg.update              هر پیام و دکمه‌ی ربات
@action:/bonus              فقط یک دستور خاص
@action:w:                  فقط مسیر کیف پول در ربات
@evt:tg.update_failed       خطای پردازش آپدیت
@evt:tg.call_failed         تماس ناموفق با Bot API
@evt:tg.rate_limited        سقف تلگرام خورد (۴۲۹)
@evt:tg.blocked             کاربری ربات را بلاک کرد
@evt:broadcast.throttled    پخش به سقف خورد
```

`tg.update` فیلد `ms` دارد؛ `@ms:>2000` کندی را نشان می‌دهد.

⚠️ متن پیام کاربر **هرگز** لاگ نمی‌شود — آدرس کیف پول و مبلغ برداشت از همان
مسیر می‌گذرند. فقط نوع کنش (`action`) ثبت می‌شود.

---

## ۶. کنش‌های ادمین (رد حسابرسی)

```
@evt:admin.login            ورود و تلاش ناموفق
@evt:admin.topup            دست‌کاری دستی موجودی
@evt:admin.ir_action        هر تصمیم روی بازار (resolve و finalize پول جابه‌جا می‌کنند)
@evt:admin.withdraw_refund  برگشت دستی برداشت
@evt:admin.unlink_telegram  قطع اتصال تلگرام (هدیه‌ی کانال هم صفر می‌شود)
```

---

## ۷. لاگ دسترسی

هر درخواست API از میدل‌ور رد می‌شود و ثبت می‌گردد:

```
@evt:api.write     هر درخواست تغییردهنده (POST/PUT/DELETE)
@evt:api.read      خواندن‌ها — فقط با LOG_LEVEL=debug
```

خواندن‌ها عمدا `debug` هستند: مینی‌اپ فهرست بازارها را مرتب می‌خواند و اگر
`info` بودند، لاگ پروداکشن پر می‌شد و همان چیزی که دنبالش بودی گم می‌شد.

**برای عیب‌یابی عمیق:** در Railway متغیر `LOG_LEVEL=debug` را بگذار، مشکل را
بازتولید کن، و **دوباره بردار**. در حالت debug هر تماس تلگرام و هر خواندن API
هم ثبت می‌شود.

---

## ۸. پیکربندی‌های خاموش‌کننده

اینها یعنی یک قابلیت کامل مرده است و کاربر فقط «کار نمی‌کند» می‌بیند:

```
@evt:config.session_secret_missing   هیچ‌کس نمی‌تواند وارد شود
@evt:config.admin_secret_missing     پنل ادمین قفل است
@evt:tg.not_configured               ربات توکن ندارد
@evt:bonus.not_configured            TG_GROUP_ID ست نشده
@evt:gateway.not_configured          درگاه کلید ندارد
@evt:tg.webhook_unauthorized         رمز وبهوک با تلگرام نمی‌خواند
@evt:db.pool_error                   قطعی دیتابیس
```

---

## ۹. متغیرهای محیطی مرتبط

| متغیر | نقش |
|---|---|
| `LOG_LEVEL` | `info` پیش‌فرض · `debug` برای عیب‌یابی · `warn` برای سکوت |
| `TG_GROUP_ID` | گروه هدیه — بدون آن `/bonus` هیچ هدیه‌ای نمی‌دهد |

---

## ۱۰. آنچه عمدا لاگ نمی‌شود

- متن پیام کاربر و کپشن
- `initData` و توکن نشست
- رمز، کلید API و امضا (لاگر خودش `[redacted]` می‌کند)
- آدرس کیف پول و `txid` کامل (کوتاه می‌شوند: `TKZgMi…ffNQnx`)

⚠️ فیلترِ راز روی **بخشی از نام** کار می‌کند. یعنی هر فیلدی که `key` یا `hash`
در نامش باشد حذف می‌شود — حتی `hasKey` یا `hasHeader`. موقع افزودن لاگ تازه،
نام فیلد را طوری بگذار که تصادفا با این فهرست برخورد نکند.
