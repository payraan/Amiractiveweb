# درخواست ممیزی مستقل — پلتفرم نارمون

تو یک ممیز مستقلی. این ریپو یک پلتفرم بازار پیش‌بینی فارسی‌زبان است که
**پول واقعی کاربران** را نگه می‌دارد و تا چند روز دیگر عمومی می‌شود. از تو
می‌خواهم پیش از لانچ، ایرادهای واقعی را پیدا کنی.

**پاسخ به فارسی.**

---

## ۰. قاعده‌ی اصلی این ممیزی

**ادعای بدون شاهد قبول نیست.** برای هر یافته باید بنویسی:

1. **فایل و شماره‌ی خط**
2. **سناریوی مشخص شکست** — با ورودی واقعی: «کاربر A این را می‌فرستد، آن اتفاق می‌افتد»
3. **چرا مطمئنی** — کدام خط کد این را ممکن می‌کند

اگر مطمئن نیستی، بنویس «مشکوک، تأیید نشده» و دلیل تردیدت را بگو. **یک یافته‌ی
قطعی از ده حدس ارزشمندتر است.** فهرست بلندِ نگرانی‌های کلی («شاید ورودی
اعتبارسنجی نشود») بی‌فایده است — دقیقاً بگو کدام ورودی، در کدام خط.

اولویت‌بندی کن: 🔴 پول یا امنیت · 🟡 درستی منطق · 🟢 کیفیت.

---

## ۱. پلتفرم چیست

**نارمون** — بازار پیش‌بینی فارسی، RTL. سه سطح روی **یک دیتابیس**:

| سطح | مسیر | احراز هویت |
|---|---|---|
| سایت | `/` و بقیه | کوکی امضاشده `amir_session` |
| مینی‌اپ تلگرام | `/app` | هدر `x-tg-auth` (توکن یک‌ساعته) |
| ربات تلگرام | وبهوک `/api/tg/webhook` | `tg_user_id` |

`src/lib/current-player.ts` هر سه را به یک `playerId` می‌رساند. **هیچ روت
اختصاصی مینی‌اپی وجود ندارد** — عمدی است.

استک: Next.js 16 App Router، React 19، TypeScript، PostgreSQL با `pg` خام
(بدون ORM). مسیر `@/*` → `src/*`. بدون تست خودکار.

### سه اقتصاد جدا

1. **بازار ایران** (`src/lib/iran.ts`) — **تنها بخش با پول واقعی**. استخر
   parimutuel تتری، کمیسیون ۳٪. اینجا محافظه‌کارانه‌ترین نگاه را داشته باش.
2. **ترید** (`src/lib/poly.ts`) — امتیازی، دیتا از پالی‌مارکت. برد =
   +(۱۰۰−احتمال٪)، باخت = −احتمال٪.
3. **نبض بازار** (`src/lib/game.ts`) — امتیازی، پیش‌بینی قیمت.

**واحد `MOON` فقط قابلیت باز می‌کند و هرگز امتیاز یا رتبه نمی‌خرد.**

---

## ۲. قواعدی که پروژه برای خودش گذاشته — نقض‌شدنشان یافته است

- کسر پول **همیشه** داخل ترنزاکشن با `SELECT … FOR UPDATE`.
- `moveFunds()` تنها راه تغییر موجودی؛ `recordRevenue()` برای درآمد.
- **پول واقعی فقط از وبهوک درگاه وارد می‌شود.**
- همه‌ی احراز هویت **fail-closed** است: بدون `SESSION_SECRET` هیچ نشستی
  پذیرفته نمی‌شود. هیچ‌جا نباید مقدار پیش‌فرض برای کلید باشد.
- `db()` یک بلوک SQL **idempotent** را در هر cold start اجرا می‌کند؛ هیچ
  فریم‌ورک مهاجرتی نیست. هر تغییر اسکیما باید `IF NOT EXISTS` باشد.
- همه‌ی دیتای upstream (یاهو، پالی‌مارکت، Myfxbook، Gemini) **سمت سرور**
  پروکسی می‌شود. بازدیدکننده‌ی ایرانی نباید مستقیم به آن‌ها بخورد — انتقال
  هرکدام به کلاینت یک یافته‌ی 🔴 است.
- **میدل‌ور در `src/middleware.ts` است، نه ریشه.** فایل ریشه اجرا نمی‌شود.
- شناسه‌ی دارایی‌ها (`src/lib/assets.ts`) و شناسه‌ی تیرهای چالش هرگز تغییر
  نمی‌کنند — در دیتابیس ذخیره شده‌اند.
- امتیازدهی **صفر-انتظار** است: حدس تصادفی به‌طور میانگین امتیاز منفی
  می‌گیرد. هر جا این خاصیت بشکند، یافته است.
- `src/lib/poly-scoring.ts` و `src/lib/wallet-rules.ts` و
  `src/lib/ledger-labels.ts` عمداً **بدون ایمپورت**اند تا کلاینت هم بخواندشان.
- `total_points` از نوع `NUMERIC(14,4)` است. هر `SELECT` باید
  `ROUND(...)::int` بزند وگرنه `pg` **رشته** برمی‌گرداند.

---

## ۳. آنچه من قبلاً تست کرده‌ام — این‌ها را دوباره کشف نکن

تست‌های زیر با داده‌ی واقعی روی Postgres محلی انجام شده و **سبز** بوده‌اند.
اگر خلافش را دیدی حتماً بگو، ولی دنبال تکرارشان نگرد:

- هر سه سطح احراز به یک `playerId` می‌رسند؛ ده روت بازیکن‌محور از دو سطح
  پاسخ **بایت‌به‌بایت یکسان** می‌دهند.
- پیش‌بینی ثبت‌شده از مینی‌اپ، از سایت و ربات دیده می‌شود (بازار ایران،
  ترید، نبض بازار).
- **مسابقه‌ی هم‌زمان:** با موجودی ۲۰ تتر، ۱۰ درخواست هم‌زمان ۵ تتری از دو
  سطح احراز — دقیقاً ۴ موفق، ۶ رد، موجودی نهایی صفر، بدون خرج اضافه.
- شرط منفی/صفر/NaN/بی‌نهایت/بیش از موجودی، بازار قفل‌شده و ناموجود، طرف
  نامعتبر — همه رد می‌شوند و موجودی دست‌نخورده می‌ماند.
- توکن با امضای دستکاری‌شده، شناسه‌ی عوض‌شده با امضای قبلی، کوکی جعلی — ۴۰۱.
- ۹ روت پولی بدون احراز → ۴۰۱.
- برداشت: مبلغ زیر حداقل، آدرس نامعتبر، چک‌سام غلط، مبلغ منفی → همه رد.
- سقف نرخ روی `ir-bet` فعال است (۶۰ در پنجره، یازدهمین دسته رد شد).
- رفرال: انتساب درست، رفرال خودی مسدود، هدیه دوبار داده نمی‌شود.
- دو کوئری سلامت (پایین) صفر برمی‌گردانند.

---

## ۴. جاهایی که مشکوکم و می‌خواهم دقیق نگاه کنی

این‌ها فرضیه‌اند، نه یافته. تأیید یا ردشان کن — **با شاهد**:

1. 🔴 **`settlePolyDue()` در `src/lib/poly.ts`** — تسویه‌ی امتیازی. آیا
   idempotent است؟ اگر کرون دو بار هم‌زمان اجرا شود چه می‌شود؟ (تسویه‌ی بازار
   ایران در `iran.ts` جداست و ادعا می‌کند idempotent است — آن را هم بسنج.)
2. 🔴 **`src/app/api/wallet/webhook/route.ts`** — تنها راه ورود پول واقعی.
   آیا امضا درست تأیید می‌شود؟ آیا **replay** ممکن است (همان وبهوک دوبار →
   دوبار شارژ)؟ آیا مبلغ از بدنه خوانده می‌شود یا از درگاه دوباره پرسیده
   می‌شود؟
3. 🔴 **`src/lib/zovix.ts`** — امضای HMAC روی بدنه‌ی form-urlencoded. رشته‌ی
   امضاشده باید **بایت‌به‌بایت** با بدنه‌ی ارسالی یکی باشد. آیا هست؟
4. 🟡 **تسویه‌ی بازار ایران** — آیا مجموع پرداختی‌ها هرگز از استخر بیشتر
   می‌شود؟ گرد کردن اعشار به نفع چه کسی است؟ بازار باطل (`MIN_ODDS`) کمیسیون
   نمی‌گیرد — مطمئن شو.
5. 🟡 **`src/lib/challenge.ts`** — اقتصاد چلنج پراپ. آیا کسی می‌تواند با
   انتخاب بازارهای کم‌ریسک شرط `CONSISTENCY_PCT` را دور بزند؟
6. 🟡 **سقف نرخ در حافظه است** (`src/middleware.ts`) و با هر دیپلوی صفر
   می‌شود. زیر بار ۵۰ هزار کاربر، آیا خودِ جارو گلوگاه می‌شود؟
7. 🟡 **`is_demo`** — آیا از راه بُرد بازار منتقل می‌شود؟ (گمان می‌رود نه، و
   پول دمو به حساب غیردمو می‌رسد.)
8. 🟡 **`getWithdrawal` در `zovix.ts` هیچ‌جا صدا زده نمی‌شود** — برداشت
   گیرکرده روی وضعیت `submitted` یخ می‌زند. تأیید کن.
9. 🟢 **`referred_by`** هیچ CHECK ی در سطح دیتابیس ندارد؛ گارد فقط در
   `attachReferral` است. آیا مسیر دیگری هست که این ستون را بنویسد؟
10. 🟢 **خطای hydration** روی همه‌ی صفحه‌ها دیده می‌شود (از جمله `/legal`).
    علتش را پیدا کن.

---

## ۵. چیزهایی که عمدی‌اند — یافته نیستند

- استخر parimutuel تتری بازار ایران با تصمیم صریح مالک ساخته شده.
- نبودن ORM و نبودن فریم‌ورک مهاجرت، عمدی است.
- نبودن تست خودکار، وضع فعلی است (پیشنهاد تست بده، ولی نبودش را به‌عنوان
  باگ گزارش نکن).
- کارت‌های تلگرامی دو حالت دارند (`forward` با دکمه‌ی لینک، `live` با
  callback) — این محدودیت خود تلگرام است.
- اسم‌های خاص در ترجمه‌ی عنوان بازارها عمداً لاتین می‌مانند.
- baseline لینت ۳۲ مورد است. بالا رفتنش یافته است؛ خودِ ۳۲ نه.

---

## ۶. کوئری‌های سلامت — باید همیشه صفر برگردانند

```sql
-- موجودی در برابر دفترکل
SELECT p.id, p.usdt_balance, COALESCE(SUM(w.amount),0) AS ledger
FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
GROUP BY p.id, p.usdt_balance
HAVING p.usdt_balance <> COALESCE(SUM(w.amount),0);

-- استخر بازار در برابر مجموع پیش‌بینی‌ها  ← مهم‌ترین
SELECT m.id, m.yes_total, m.no_total,
       COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0) AS real_yes,
       COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0)  AS real_no
FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
GROUP BY m.id, m.yes_total, m.no_total
HAVING m.yes_total <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0)
    OR m.no_total  <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0);
```

اگر کوئری سلامت **سومی** به ذهنت می‌رسد که باید همیشه صفر باشد، بنویسش —
این ارزشمندترین چیزی است که می‌توانی بدهی.

---

## ۷. ترتیب کار

۱. اول **مسیر پول**: `iran.ts`، `wallet/webhook`، `withdrawal.ts`، `zovix.ts`.
۲. بعد **احراز هویت**: `session.ts`، `tg-auth.ts`، `current-player.ts`،
   `admin.ts`، `middleware.ts`.
۳. بعد **تسویه و idempotency**: `settle.ts`، `poly.ts`، `iran.ts`.
۴. بعد **بقیه**.

---

## ۸. خروجی مورد انتظار

```
## 🔴 بحرانی
### ۱. <عنوان کوتاه>
فایل: src/lib/xxx.ts:۱۲۳
سناریو: کاربر A ... سپس ... نتیجه: ...
چرا: خط ۱۲۳ ... بدون ...
اطمینان: قطعی | مشکوک
پیشنهاد: ...

## 🟡 متوسط
...

## 🟢 کیفیت
...

## بررسی‌شده و سالم
<فهرست کوتاه چیزهایی که نگاه کردی و مشکلی نداشتند — تا بدانم پوشش کجا بوده>
```

بخش آخر اجباری است. اگر جایی را نگاه نکردی، صریح بگو نگاه نکردی.

اگر برای رسیدن به یک نتیجه‌ی قطعی لازم است کدی اجرا کنی، بگو **دقیقاً چه
دستوری** را اجرا کنم و نتیجه‌اش را برایت می‌فرستم.
