# ممیزی دور چهارم — اقتصاد و ریاضی، نه کد

سه دور قبل **کد** را ممیزی کردند و هر سه یافته‌ی واقعی دادند. این دور جنس
دیگری دارد: هیچ خط کدی اشکال ندارد، ولی ممکن است **اعداد** غلط باشند.

از تو شبیه‌سازی و حساب می‌خواهم، نه بازبینی کد.

**پاسخ به فارسی. با عدد جواب بده، نه با توصیف.**

---

## ۰. پرسش محوری

> **آیا کسی می‌تواند با پرداخت ۱۱۷ دلار، حساب پیش‌بینی ۵۰,۰۰۰ دلاری ببرد،
> و این کار را به‌طور سیستماتیک تکرار کند؟**

اگر جواب بله باشد، پلتفرم پیش از لانچ ورشکست می‌شود. اگر جواب نه باشد، باید
بدانیم نرخ قبولی واقعی چقدر است تا قیمت‌گذاری معنی داشته باشد.

این تنها کار بازِ اقتصادی پیش از لانچ است.

---

## ۱. اعداد واقعی سیستم

### قیمت واحد داخلی (MOON)

| بسته | MOON | قیمت | هر MOON |
|---|---|---|---|
| mini | ۲۰ | ‎$۲.۵ | ‎$۰.۱۲۵ |
| prime | ۳۰۰۰ | ‎$۱۲۵ | **‎$۰.۰۴۱۷** |

کاربر عاقل همیشه بزرگ‌ترین بسته را می‌خرد، پس **‎$۰.۰۴۱۷ به‌ازای هر MOON**
مبنای محاسبه است.

### تیرهای چالش

| تیر | ورودی (MOON) | ورودی (‎$) | جایزه |
|---|---|---|---|
| fx250 | ۶۰۰ | ‎$۲۵ | حساب معاملاتی ‎$۲۵۰ نزد بروکر |
| fx500 | ۱۰۰۰ | ‎$۴۱.۷ | حساب معاملاتی ‎$۵۰۰ |
| fx1k | ۱۸۰۰ | ‎$۷۵ | حساب معاملاتی ‎$۱,۰۰۰ |
| pr5k | ۴۰۰ | ‎$۱۶.۷ | حساب پیش‌بینی ‎$۵,۰۰۰ |
| pr10k | ۷۰۰ | ‎$۲۹.۲ | حساب پیش‌بینی ‎$۱۰,۰۰۰ |
| pr25k | ۱۶۰۰ | ‎$۶۶.۷ | حساب پیش‌بینی ‎$۲۵,۰۰۰ |
| pr50k | ۲۸۰۰ | ‎$۱۱۶.۷ | حساب پیش‌بینی ‎$۵۰,۰۰۰ |

مسیر `forex` جایزه‌اش حساب مارکتینگیِ بروکر است و برای ما هزینه ندارد.
**مسیر `predict` از جیب ماست** — تمرکز اینجاست.

### شرایط عبور (یکسان برای همه‌ی تیرها)

```
هدف سود        : ۳۰۰ پوینت
حداکثر افت     : ۱۵۰ پوینت
سقف ضرر روزانه : ۶۰ پوینت
حداقل پیش‌بینی : ۲۵
حداقل روز فعال : ۷
مهلت           : ۳۰ روز
قانون ثبات     : هیچ روزی بیش از ۳۵٪ کل سود نباشد
بازه‌ی احتمال   : فقط ۲۵٪ تا ۷۵٪ شمرده می‌شود
سقف شمارش     : فقط ۵۰ پیش‌بینی اولِ تسویه‌شده
سقف ورود      : ۳ بار در ۳۰ روز (فقط مسیر predict)
```

### فرمول امتیاز (صفر-انتظار)

```
برد  = +(۱۰۰ − احتمال٪)
باخت = −(احتمال٪)
```

برای هر احتمالی، امیدریاضی دقیقاً صفر است. یعنی حدس تصادفی به‌طور میانگین
صفر می‌گیرد، نه منفی — و با هزینه‌ی MOON منفی می‌شود.

### کمبو (چند پیش‌بینی در یک تیکت)

۲ تا ۵ پا، همه باید درست باشند. یک پای غلط یعنی کل تیکت. اهرم پلکانی دارد.
**آیا کمبو خاصیت صفر-انتظار را نگه می‌دارد؟** این را جدا حساب کن.

---

## ۲. چیزی که می‌خواهم — با عدد

### الف) نرخ قبولی

با شبیه‌سازی مونت‌کارلو (حداقل ۱۰۰ هزار تکرار برای هر حالت)، احتمال قبولی
را حساب کن برای:

1. **حدس‌زن تصادفی** — انتخاب تصادفی بین بله/خیر
2. **پیش‌بینی‌کننده‌ی متوسط** — ۵۲٪ دقت واقعی
3. **پیش‌بینی‌کننده‌ی خوب** — ۵۵٪
4. **پیش‌بینی‌کننده‌ی عالی** — ۶۰٪

برای هرکدام بگو: نرخ قبولی، و اینکه کدام شرط بیشتر از همه ردشان می‌کند
(هدف؟ افت؟ قانون ثبات؟).

⚠️ حواست به توزیع احتمال باشد: کاربر آزاد است در بازه‌ی ۲۵–۷۵٪ هر بازاری
را انتخاب کند. بررسی کن آیا **انتخاب سیستماتیکِ یک نقطه از بازه** (مثلاً
همیشه ۲۵٪، یا همیشه ۷۵٪، یا همیشه ۵۰٪) نرخ قبولی را بالا می‌برد. اگر یک
نقطه بهتر از بقیه است، آن یک سوراخ است.

### ب) اقتصاد پلتفرم

با نرخ قبولی‌ای که درآوردی:

- **ارزش انتظاری هر ورود برای کاربر** چقدر است؟ (به دلار)
- اگر مثبت باشد، **کاربر عاقل بی‌نهایت بار وارد می‌شود** — سقف ۳ بار در ۳۰
  روز چقدر جلویش را می‌گیرد؟
- برای اینکه پلتفرم سر‌به‌سر شود، **جایزه‌ی ‎$۵۰,۰۰۰ باید چه سقف پرداختی
  داشته باشد**؟ (جایزه «حساب پیش‌بینی» است با پرداخت کریپتوییِ سقف‌دار —
  فرض کن سقف ماهانه‌ی X درصد از اندازه‌ی حساب.)
- کدام تیر بدترین نسبت را دارد؟

### ج) صفر-انتظار، آزموده‌شده

- تأیید کن که `برد=۱۰۰−p` و `باخت=−p` واقعاً برای هر p امیدریاضی صفر دارد.
- **کمبو را جدا حساب کن.** با ۲ تا ۵ پا و اهرم پلکانی، آیا امیدریاضی
  همچنان صفر است یا کمبو یک سوراخ مثبت باز می‌کند؟ اگر مثبت است، با چه
  ترکیبی و چقدر؟
- آیا ترکیب «نبض بازار + ترید + کمبو» راهی برای ساختن مزیت مثبت می‌دهد؟

### د) سوراخ‌های استراتژیک

- آیا **سقف ۵۰ پیش‌بینی** با انتخاب زمان‌بندی قابل دور زدن است؟ (مثلاً ثبت
  ۲۰۰ پیش‌بینی و اینکه فقط ۵۰ تای اولِ *تسویه‌شده* شمرده می‌شود — آیا
  می‌شود ترتیب تسویه را انتخاب کرد؟)
- آیا **قانون ثبات ۳۵٪** با پخش‌کردن مصنوعی روی روزها دور می‌خورد؟
- آیا **۳ ورود در ۳۰ روز** با چند حساب دور می‌خورد؟ (ضدتقلب به «تلگرام
  متصل» گره خورده — این کافی است؟)
- ورودی از **MOON** پرداخت می‌شود و MOON از رفرال و هدیه هم می‌آید. آیا
  می‌شود ورودی چالش را عملاً رایگان کرد؟

---

## ۳. آنچه تغییرناپذیر است — راه‌حل نباید بشکندشان

- **امتیاز خریدنی نیست.** هر پیشنهادی که MOON بیشتر را به شانس قبولی
  بیشتر تبدیل کند، رد است.
- **صفر-انتظار حفظ شود.** حدس تصادفی نباید سودآور باشد.
- شناسه‌ی تیرها (`fx250`, `pr5k`, …) عوض نمی‌شود — در دیتابیس ذخیره‌اند.
- واژگان شرط‌بندی به‌کار نرود.

---

## ۴. خروجی

عدد بده، نه توصیف. جدول بهتر از پاراگراف.

```
## نرخ قبولی (مونت‌کارلو، N=…)
| دقت کاربر | نرخ قبولی | شرطِ ردکننده‌ی غالب |

## ارزش انتظاری هر ورود
| تیر | ورودی $ | EV کاربر $ | EV پلتفرم $ |

## صفر-انتظار
- تک‌پیش‌بینی: …
- کمبو: … (با عدد)

## سوراخ‌ها
### ۱. <عنوان>
سناریو با عدد: …
اثر مالی: …
پیشنهاد: …
این پیشنهاد چه چیزی را می‌شکند: …

## توصیه‌ی قیمت‌گذاری
<اگر اعداد فعلی ناپایدارند، چه اعدادی پایدارند>
```

بخش «این پیشنهاد چه چیزی را می‌شکند» **اجباری** است.

اگر برای شبیه‌سازی به کدی نیاز داری، خودت بنویس و نتیجه‌اش را گزارش کن —
یا اگر لازم است روی داده‌ی واقعی اجرا شود، دقیقاً بگو چه چیزی را اجرا کنم.
