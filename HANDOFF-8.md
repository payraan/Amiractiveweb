# سند تحویل نارمون — فاز ۸: نسخه‌ی دمو و راه لانچ

> این سند را به‌عنوان اولین پیام چت جدید بفرست. جانشین `HANDOFF-7.md` است
> (آن را برای تاریخچه نگه دار — بخش‌های ۰ تا ۳ و ۷ و ۸ و ۱۰ آن هنوز معتبرند
> و اینجا تکرار نشده‌اند).
>
> **منبع حقیقت نهایی، خودِ ریپو است.** هر جا این سند با کد تناقض داشت، کد را
> ملاک بگیر و تناقض را گزارش کن.
>
> 🎭 **پلتفرم روی پروداکشن در نسخه‌ی دموست.** پول واقعی وارد نمی‌شود و بیرون
> نمی‌رود. یک متغیر محیطی آن را به حالت واقعی می‌برد.
>
> آخرین به‌روزرسانی: ۱ شهریور ۱۴۰۵

---

## ۰. اول این را بخوان

`HANDOFF-7.md` بخش ۰ (سبک کار)، ۳ (اعداد اقتصاد)، ۷ (دستورها) و ۸ (خطوط
قرمز محصول) هنوز کاملاً معتبرند. **آن‌ها را بخوان، اینجا تکرار نشده‌اند.**

خلاصه‌ی سبک کار:
- **پاسخ‌ها به فارسی.** مالک برنامه‌نویس نیست؛ قدم‌به‌قدم.
- **پیش از هر کامیت:** `npx tsc --noEmit`. لینت baseline = **۳۳** (۲۷ خطا +
  ۶ هشدار). بالا نرود. خطای تازه را **رفع کن، سرکوب نکن**.
- **کامپایل شدن یعنی کد ساخته شد، نه اینکه کار می‌کند.** هر مسیر پولی با
  `curl` و `psql` روی دیتابیس **محلی** تست شود. داده‌ی تستی بعدش پاک شود.
- **در مرورگر ببین.** هر تغییر دیداری در عرض ۳۷۵px.
- **پوش:** مالک اجازه‌ی پایدار داده؛ هر پوش را گزارش کن و سلامت پروداکشن را
  بسنج.

### درس‌های تازه‌ی این فاز

۲۰. **ادعای عددی را باید اجرا کرد، نه استدلال.** یک ممیز مستقل نوشت
    «volScale=1 روی forex باز هم EV منفی می‌دهد (≈−۸۵)». شبیه‌سازی با
    جدول واقعیِ کد گفت **+۴۰**. علامت برعکس بود — و آن یکی جایی بود که
    بیشترین اهمیت را داشت.

۲۱. **تحلیل ایستا سیاست را نمی‌بیند، فقط منطق را.** بحرانی‌ترین باگ این
    فاز (پول واقعی از هیچ) کدش کاملاً «درست» بود؛ فقط در حالتی که برایش
    نوشته شده بود اجرا نمی‌شد. نه ممیز دیدش، نه من — **تست هم‌زمانی با
    اجرای واقعی پیدایش کرد**.

۲۲. **ناوردهای حسابداری، سیاست را نمی‌سنجند.** آن باگ از هر ۴۲ چک سلامت
    رد شد چون دفترکل کاملاً سازگار بود. چک ۴۳ بعداً برای همین ساخته شد.

۲۳. **کادر SQL ریلوی به هر کوئری‌ای که `SELECT` داخلش باشد `LIMIT`
    می‌چسباند** — حتی داخل زیرکوئری. پس `UPDATE ... IN (SELECT ...)`
    آنجا می‌شکند. یا شناسه‌ها را مستقیم بنویس، یا از تب **Console**
    استفاده کن که psql واقعی است.

---

## ۱. مهم‌ترین تغییر این فاز: نسخه‌ی دمو

### چرا

درگاه Zovix پلن Enterprise دارد با این اعداد (از داشبورد):
`$0.25` به‌ازای هر آدرس کیف پول · `$2.10` به‌ازای هر واریز ·
`Minimum monthly platform usage: $300`.

⚠️ **مالک می‌گوید اپراتور درگاه گفته آن ۳۰۰ یک‌بار است نه ماهانه** (۴۰۰
شارژ می‌کنی، ۳۰۰ اعتبار می‌ماند تا مصرف شود). داشبورد چیز دیگری می‌گوید.
**این هنوز کتبی روشن نشده و باید بشود** — تفاوتش بین ۴۰۰ دلار و ۳٬۶۰۰
دلار در سال است.

تصمیم: با صفر کاربر واقعی، خرج‌کردن برای فعال‌کردن پرداخت، جوابِ سؤالی را
می‌خرد که هنوز نداریم. سؤالِ واقعی «آیا کسی استفاده می‌کند؟» است.

### چطور کار می‌کند

`src/lib/platform-mode.ts` — تک‌منبع حقیقت.

```
PLATFORM_MODE = demo | live      (پیش‌فرض: demo — fail-safe)
DEMO_ALLOWANCE = 100             (سهمیه‌ی ماهانه‌ی تتر مجازی)
```

⚠️ **پیش‌فرض عمداً دموست.** اگر متغیر ست نشود، بدترین حالت این است که پول
واقعی موقتاً حرکت نکند (برگشت‌پذیر). پیش‌فرضِ `live` یعنی موجودی مجازی
قابل برداشت شود — غیرقابل‌جبران.

**در حالت دمو چه بسته است:** ساخت آدرس واریز (که هزینه‌ی ۲۵ سنتی هم دارد)،
برداشت. **بوست** در دمو با پول مجازی کار می‌کند (`realOnly: !isDemo()`).

**سهمیه‌ی ماهانه** (`src/lib/demo-allowance.ts`): تا سقف پر می‌شود، بالاتر
را کم نمی‌کند، منتقل نمی‌شود. کلید یکتاسازی `kind='demo_allowance'` +
`ref=YYYY-MM`. **از `moveFunds` می‌گذرد**، نه `UPDATE` مستقیم.

**نمایش:** سایت (لایوت ریشه)، مینی‌اپ (`MiniApp` بالای همه‌ی تب‌ها)، ربات
(کارت کیف پول، پیش از هر عددی). متن از `modeBanner()` می‌آید — سخت‌کد
نیست.

### رفتن به حالت واقعی

یک متغیر در Railway: `PLATFORM_MODE=live`. **آزموده شده** — هر پنج رفتار
برعکس می‌شود (حالت، آدرس واریز، بنر، برداشت، سهمیه).

---

## ۲. ⚠️ کار نیمه‌تمام — اول این را تمام کن

**برگرداندن پول واقعیِ قفل‌شده در استخرها.**

مالک پیش از سوییچ به دمو، با پول واقعی روی شش بازار پیش‌بینی کرده بود:

```
بازارهای 27, 34, 35, 38, 39, 40   ·   مجموع ۲۷.۰۰ تتر واقعی
موجودی آزاد: ۳.۰۹   ·   واریز اولیه: ۳۰.۰۸
```

**کوئری اجرا شده و موفق بوده** (`Query ran successfully`):

```sql
UPDATE ir_markets
   SET status = 'settling', outcome = 'void',
       void_reason = 'demo_switch',
       settled_at = now() - interval '48 hours'
 WHERE id IN (27, 34, 35, 38, 39, 40)
   AND status IN ('open','locked');
```

کرون هر ۱۵ دقیقه بازارهای `settling` با پنجره‌ی گذشته را برمی‌دارد و
مسیر ابطال را اجرا می‌کند: **برگشت کامل، بدون کمیسیون، بدون سهم سازنده**.
چون `demo_stake = 0` بود، پول به‌صورت **واقعی** برمی‌گردد.

**تأیید کن** (این را در کادر SQL ریلوی بزن):

```sql
SELECT
  (SELECT round(COALESCE(SUM(usdt_balance),0),2) FROM players) AS free_balance,
  (SELECT round(COALESCE(SUM(stake-demo_stake),0),2) FROM ir_bets WHERE status='open') AS still_locked;
```

انتظار: `free_balance = 30.09` و `still_locked = 0.00`.

اگر بعد از دو تیک کرون هنوز قفل بود، اول ببین آن شش بازار الان چه وضعیتی
دارند (`SELECT id, status, outcome, settled_at FROM ir_markets WHERE id IN
(27,34,35,38,39,40);`) و بعد در لاگ Railway دنبال `@evt:ir.settled` بگرد.
کل این مسیر روی دیتابیس محلی با پول واقعیِ شبیه‌سازی‌شده آزموده شده و
درست کار می‌کند (`۱۳ → ۳۳` تتر، ۴۳ چک سبز).

---

## ۳. چک سلامت — حالا ۴۳ چک

```bash
railway run psql "$DATABASE_URL" -f scripts/health.sql
```

یا محتوای `scripts/health-paste.sql` را در کادر SQL ریلوی پیست کن.

⚠️ **چک ۴۳ تازه و بحرانی است:** «پول واقعی از هیچ ساخته نشده باشد». از یک
باگ واقعی آمد (بخش ۵). اگر قرمز شد، فوراً بررسی کن.

اسکریپت‌های دیگر: `scripts/snapshot.sql` (۱۰ نمای وضعیت)،
`scripts/diag.sql`، `scripts/loadtest-seed.sql` و `loadtest-clean.sql`
(⚠️ هر دو نگهبان دارند و روی هر دیتابیسی جز محلی خودشان را متوقف می‌کنند).

---

## ۴. آنچه در این فاز ساخته شد

| کامیت | چه چیزی |
|---|---|
| `2ae2866` | **رفع مرگ تسویه‌ی آرنا** — `SELECT DISTINCT` با `ORDER BY random()` در پستگرس غیرمجاز است؛ هر تیک کرون خطا می‌داد و آرنا اصلاً تسویه نمی‌شد |
| `65a4156` | **رسید پیش‌بینی** — لینک اعلان نتیجه به بن‌بست می‌خورد؛ حالا رسید باز می‌شود با دکمه‌ی اشتراک (متن **مهارت** را می‌گوید نه پول) |
| `cc07087` | دسته‌ی `politics` + رفع `Invalid Date` نمودارها |
| `0677e6e` `82da237` | **ثبت رفتار کاربر** (`app_events`) — سه پله: `list_view → market_open → predict` |
| `16ced36` | **تب تحلیل رفتار** با هفت نمودار |
| `3312c9e` | **بازار سریع** — ساخت و انتشار در یک قدم، با دکمه‌های زمانی به وقت تهران |
| `15b54a1` | **سهم نفرات اول** — ۳۰٪ کمیسیون به ده شرط‌بند اول، از استخر نه از کمیسیون |
| `bc5cf6b` | دفترچه‌ی جذب ادمین‌های کانال (`MARKETING-CREATORS.md`) |
| `bacf97d` | اعلان نتیجه‌ی کمبو + رفع بلعیدن خطا |
| `610e052` | کارت سود و زیان تتری با دو بازه |
| `b80468b` | رنگ دکمه‌های کارت بازار + زمان دقیق در کارنامه |
| `24ea05f` `2e1417e` `d1c9c95` | **نسخه‌ی دمو** — هسته، نمایش، بازطراحی کیف پول |
| `5c60e40` | چهار رفع + **پنل درآمد بازارساز** |
| `d5e9394` | **تست بار ۵۰ هزار کاربر** — سه ایندکس گمشده |
| `b3c1a00` | 🔴 **رفع نشتی پول واقعی در دمو** + چک ۴۳ |

---

## ۵. 🔴 باگ بحرانی که پیدا و رفع شد — این را بفهم

در تسویه‌ی برنده:

```js
const demoBack = Math.min(Number(b.demo_stake), amt);   // قبل
const demoBack = isDemo() ? amt : Math.min(Number(b.demo_stake), amt);  // بعد
```

قاعده‌ی قبلی برای حالت **واقعی** درست است (اصلِ بونوس بونوس می‌ماند، سودش
واقعی می‌شود). ولی در نسخه‌ی دمو **از هیچ بدهیِ واقعیِ قابل‌برداشت
می‌ساخت**: استخری تماماً مجازی (۹۰ تتر) پس از تسویه ۳۶.۴۹ تتر **واقعی**
به برنده‌ها داد.

**چرا نگهبان‌های دمو نگرفتندش:** آن‌ها **مرزها** را می‌بندند (واریز،
برداشت، آدرس). این پول در **داخل** سیستم ساخته می‌شد.

**چرا ۴۲ چک نگرفتند:** دفترکل کاملاً سازگار بود. **سیاست** غلط بود، نه
حسابداری.

هر پنج نقطه‌ی دیگرِ اعتبار عددی سنجیده شدند و پاک بودند.

---

## ۶. تست‌هایی که اجرا شده — تکرارشان لازم نیست

**تست بار ۵۰ هزار کاربر:** همه‌ی مسیرهای کاربر زیر ۰.۴ms. سه ایندکس
گمشده اضافه شد (`irb_player_idx`, `poly_player_idx`, `irm_creator_idx`) —
کارنامه‌ی شرط‌ها از ۱۴۲ بافر به ۶ بافر رفت.

**تست هم‌زمانی (با اجرای واقعی):**
- ۸ شرط هم‌زمان با موجودی برای یکی → ۱ موفق، موجودی **۰** نه منفی
- ۱۲ درخواست کیف پول هم‌زمان → دقیقاً **۱** سهمیه
- ۶ تسویه‌ی هم‌زمان یک بازار → ۱ موفق، ۵ `bad_status`، تراز دقیق

**امتیازدهی صفر-انتظار:** ۱۶ ترکیب (۴ کلاس دارایی × ۴ تایم‌فریم) با ضریب
اندازه‌گیری‌شده و ضریب پشتیبان — همه بین `−۴.۵۴` و `−۴.۹۴`. هدف `−۴.۷` ✓

---

## ۷. یافته‌های باز

| # | مورد | شدت |
|---|---|---|
| ۱ | تحلیل رفتار برای چند میلیون رویداد به **جدول تجمیع روزانه** نیاز دارد. پیش‌فرض تب به ۷ روز آمد به‌عنوان مسکن | 🟡 |
| ۲ | ثبت‌نام سایت اعتبارسنجی تلگرامی ندارد → چندحسابی | 🟡 |
| ۳ | سقف نرخ در حافظه است؛ با هر دیپلوی صفر می‌شود | 🟡 |
| ۴ | `WITHDRAW_FEE_USDT = 3` روی `MIN_WITHDRAW = 4` — سازنده‌ی کوچک ۶۰٪ کارمزد می‌دهد. **پیش از حالت واقعی باید تعیین تکلیف شود** | 🟡 |
| ۵ | `vol_scale` در لحظه‌ی ساخت راند قفل می‌شود (زمان‌بندی نوسان) | 🟡 |
| ۶ | همه‌ی دیتای نبض بازار به Yahoo گره خورده | 🟡 |
| ۷ | `robots.txt` و `sitemap.xml` وجود ندارند | 🟢 |
| ۸ | کاور بازار در سایت و مینی‌اپ دیده نمی‌شود | 🟢 |
| ۹ | مسیر `no_winners` سهم نفرات اول را پرداخت نمی‌کند — **عمدی، اثرش صفر است**، دلیلش در کد کامنت شده | ⚪ |

---

## ۸. کارهای بعدی به ترتیب

۱. **برگرداندن پول قفل‌شده** ← بخش ۲
۲. **جزوه‌ی آموزشی صفر تا صد** — مالک برای یوتیوب می‌خواهد: هم «چطور کار
   کنم» هم «چرا این‌طور طراحی شده»، هم مرجع متنی هم قسمت‌بندی ویدیویی با
   متن گفتاری. **حالا باید نسخه‌ی دمو را توضیح بدهد.**
۳. **کمپین ۴۵ روزه‌ی افتتاحیه** — با نسخه‌ی دمو **قوی‌تر** می‌شود: «۴۵ روز،
   بدون ریسک، با جایزه‌ی واقعی». پیش‌نیازها: اعلان‌ها زنده، یک هفته بازار
   ۲۴ ساعته‌ی پشت‌سرهم، بودجه‌ی جایزه، تاریخ صفر، هدف عددی موفقیت.
۴. **جذب ادمین‌های کانال** — `MARKETING-CREATORS.md` آماده است. پیش‌نیاز:
   `TG_ADMIN_IDS` در Railway ست باشد (وگرنه ثبت کانال بی‌صدا در صف می‌ماند).
۵. ممیزی سرتاسری در قالب کاربر (منوها، متون، هر سه سطح) — شروع نشده.

---

## ۹. ممیز مستقل

`AUDIT-BRIEF.md` در ریشه، متن آماده‌ای است برای دادن به یک ایجنت/مهندس
مستقل. مالک یک بار استفاده‌اش کرده و جوابش راستی‌آزمایی شد.

**نتیجه‌ی آن ممیزی:** ساختارش دقیق بود (ایندکس‌های یکتا، قفل‌ها،
`timingSafeEqual`، معادله‌ی ضدفارم — همه تأیید شدند). ولی **تنها جایی که
عدد داد، علامت را برعکس گفت** و یافته‌ی اصلی‌اش نتیجه‌گیری غلط داشت.

⚠️ **اگر دوباره از ایجنت دیگری نظر خواستی، جوابش را مثل جواب خودت بسنج.**
مالک صریحاً گفته: «ایجنت هم اشتباه می‌کند، پس همه‌چیز باید دابل‌چک شود.»
نظر گرفتن از دیگری خوب است — پذیرفتنش بدون اجرا، نه.

---

## ۱۰. اولین کارهای چت جدید

۱. `HANDOFF-7.md` بخش‌های ۰، ۳، ۷، ۸، ۱۰ را بخوان.
۲. `scripts/health.sql` را روی پروداکشن بگیر — **۴۳ چک، همه باید ۰ باشند.**
۳. وضعیت آن شش بازار را بررسی کن (بخش ۲) و پول را برگردان.
۴. `TG_ADMIN_IDS` را با مالک تأیید کن.
۵. بعد سراغ جزوه‌ی آموزشی برو.
