# سند تحویل نارمون — فاز ۳: پیش از لانچ

> این سند را به‌عنوان اولین پیام چت جدید بفرست. جانشین `HANDOFF-2.md` است
> (آن سند مربوط به فازی بود که تمام شد؛ برای تاریخچه نگهش دار).
>
> **منبع حقیقت نهایی، خودِ ریپو است.** هر جا این سند با کد تناقض داشت، کد را
> ملاک بگیر و تناقض را گزارش کن.
>
> ⏰ **لانچ نزدیک است.** بخش ۲ (کارهای باقی‌مانده) به ترتیب اولویت نوشته شده.
>
> 🔴 **اول بند ۲.۶ را بخوان.** دامنه `narmoon.io` منقضی شده و پنجره‌ی بازیابی
> حداکثر تا **۲۰۲۶/۰۸/۲۲** باز است. تنها کار این فهرست که مهلتش دست ما نیست.
>
> آخرین به‌روزرسانی: ۲۰۲۶/۰۸/۱۲ (بخش ۴.۱).

---

## ۰. نقش تو و سبک کار

تو Principal Engineer این پروژه‌ای. مالک برنامه‌نویس نیست و در سطح کپی‌پیست و
اجرای دستور ترمینال کار می‌کند.

- **پاسخ‌ها به فارسی.**
- **قبل از هر کامیت:** `npx tsc --noEmit`؛ در تغییرات بزرگ `npm run build`.
- **لینت baseline = ۳۲ مورد.** قبل از هر کامیت مقایسه کن بالا نرفته باشد. اگر
  خطای تازه‌ای ساختی، **رفعش کن، سرکوبش نکن**.
- **کد را با داده‌ی واقعی تست کن، نه فقط کامپایل.** روال ثابت: هر مسیر پولی با
  `curl` و `psql` روی دیتابیس محلی سرتاسر تست شود (سناریوی موفق + سوءاستفاده)،
  بعد کامیت. **داده‌ی تستی را بعد از تست پاک کن.**
- **دیتابیس پروداکشن را به `.env.local` اضافه نکن.** روال این پروژه پر از
  `DELETE` روی دیتابیس محلی است و یک فراموشی داده‌ی واقعی را پاک می‌کند.
  کوئری‌های پروداکشن را مالک در Railway → Postgres → Database → Data اجرا
  می‌کند و خروجی را می‌فرستد.
- **پوش:** مالک اجازه‌ی پایدار داده، ولی هر پوش را گزارش کن و پس از دیپلوی
  سلامت پروداکشن را بسنج (auto-deploy سالم است، ~۱ تا ۲ دقیقه).
- هر تغییر را قبل از اعمال با یکی‌دو جمله‌ی ساده‌ی فارسی توضیح بده.
- هیچ کلید/توکنی هاردکد نشود؛ `.env.local` در گیت‌ایگنور بماند.
- اگر چیزی با این سند نمی‌خواند، **اول گزارش بده**، خودسرانه بازنویسی نکن.

### درس‌های گران این فاز (تکرارشان نکن)

1. **تعداد را با مقدار اشتباه نگیر.** یک بار روت کندل را «سالم» اعلام کردم چون
   هر ۱۰ دارایی ۷۲۵ کندل می‌دادند. مقدارها را ندیده بودم: هر چهار دارایی دقیقاً
   `64055.01` برمی‌گرداندند، چون روت هاردکد بود و همه را بیت‌کوین می‌داد.
2. **پیش از اعلام «انجام شد»، در مرورگر ببین.** یک بار متن سکشن ترید را در
   `ArenaSection` عوض کردم، در حالی که صفحه‌ی اصلی `TradeSection` را رندر
   می‌کند. فقط بررسی در مرورگر جلوی گزارش غلط را گرفت.
3. **پنل خالی = پنل نامرئی.** دو بار همین الگو تکرار شد (کارنامه‌ی چالش و
   تاریخچه‌ی کمبو): بخش با شرط `length > 0` رندر می‌شد، پس کاربری که هنوز داده
   نداشت فکر می‌کرد قابلیت اصلاً وجود ندارد. **هر فهرستی باید حالت خالی داشته
   باشد.**
4. **جای‌نگهدار را با نثر اشتباه نگیر.** موقع پاک‌سازی خط تیره‌ها اول ۲۴۵ مورد
   شمردم؛ ۱۳۸ تای آن `v ?? "—"` و کامنت JSX بود که باید می‌ماند. نثر واقعی ۱۰۷
   مورد بود.
5. **کامپایل شدن یعنی کد ساخته شد، نه اینکه اجرا می‌شود.** میدل‌ور را در
   `middleware.ts` ریشه نوشتم؛ `tsc` و `build` هر دو سبز شدند و کد **هرگز اجرا
   نشد**. این پروژه پوشه‌ی `src` دارد، پس Next فقط **`src/middleware.ts`** را
   می‌بیند. فقط تست رفتاری (زدن روت و ندیدن ۴۲۹) این را گرفت.
6. **دامنه‌ی تست را با دامنه‌ی ادعا اشتباه نگیر.** یک بار گفتم «روی پروداکشن
   تأیید شد» در حالی که آنچه روی پروداکشن دیده بودم رسیدن کامیت و ۲۰۰ بودن
   صفحه‌ها بود؛ *رفتار* منطق سمت سرور فقط روی لوکال تست شده بود. اگر منطقی
   رشته‌ی قابل‌جستجویی در باندل کلاینت ندارد، «باندل را گشتم» دلیل نیست.
7. **اگر لوکال فقط یکی از حالت‌ها را می‌سازد، بقیه تست نشده‌اند.** تشخیص
   بلاک‌شدن ربات را «تست‌شده» گزارش کردم؛ ولی چون توکن لوکال نامعتبر است،
   همه‌ی تست‌ها فقط حالت «نامشخص» را می‌سنجیدند. مالک ربات را بلاک کرد و
   همه‌چیز عادی کار کرد. **قبل از گفتن «تست شد» بپرس کدام حالت‌ها اصلاً قابل
   ساختن بودند.**
8. **جای‌گذاری تنبل، تشخیص نیست.** علامتی که فقط وقتی زده می‌شود که ما اتفاقی
   کاری بکنیم (اینجا: ارسال پیام) ممکن است هرگز زده نشود. اگر قابلیتی باید در
   لحظه‌ی تصمیم درست باشد، همان لحظه بپرس.

---

## ۱. پلتفرم چیست

**نارمون** — پلتفرم فارسی‌زبان «هوش جمعی و بازارهای پیش‌بینی».

- ریپو: `https://github.com/payraan/Amiractiveweb` (برنچ `main`)
- دیپلوی فعلی: Railway — `https://amiractiveweb-production.up.railway.app`
- استک: Next.js 16 App Router، React 19، Tailwind v4، TypeScript، PostgreSQL
  با `pg` خام (بدون ORM). مسیر `@/*` → `src/*`.
- زبان: فارسی، RTL. کامنت‌های کد فارسی‌اند.
- ربات: `@NarmoonMarketBot`، مینی‌اپ روی `/app`.

**تز محصول:** امتیاز فقط از مهارت می‌آید، نه شانس و نه پول. واحد داخلی `MOON`
است و **فقط قابلیت باز می‌کند؛ هرگز امتیاز یا رتبه نمی‌خرد.**

### صفحه‌ها

`/` `/trade` `/predict` `/iran` `/iran/propose` `/iran/m/[id]` `/wallet`
`/profile` `/challenge` `/leaderboard` `/legal` `/login` `/referral` `/bot`
`/admin` `/app`

**ریدایرکت‌ها:** `/arena` → `/trade` · `/results` → `/bot#results` ·
`/broker` → `/bot#broker` · `/combos` → `/trade`

⚠️ `/bot` حالا چهار بخش دارد با لنگر: `#features` `#results` `#broker` `#faq`.
نتایج زنده و بروکر داخلش ادغام شده‌اند.

### معماری — چیزهایی که باید بدانی

- **`db()`** یک بلوک SQL idempotent را یک‌بار در هر cold start اجرا می‌کند.
  هیچ فریم‌ورک مهاجرتی نیست. ماژول‌ها جدول‌های خودشان را همین‌طور می‌سازند:
  `ensureIrTables()`، `ensureTelegramTables()`، `ensureChallengeTables()`،
  `ensureReferralTables()`، `ensurePolyTables()`، `ensureComboTables()`.
- **`currentPlayerId()`** یک شناسه‌ی هویت برای هر دو سطح (کوکی سایت یا هدر
  `x-tg-auth` مینی‌اپ). **هیچ روت اختصاصی مینی‌اپی وجود ندارد.**
- **هویت:** `tg_user_id` لنگر است و همیشه برنده. `tg_username` کلید ورود با
  رمز (nullable). `tg_handle` فقط برچسب. ادغام حساب فقط دستی از پنل ادمین.
- **قواعد پولی (تغییر نده):** کسر پول همیشه داخل ترنزاکشن با
  `SELECT … FOR UPDATE`. `moveFunds()` تنها راه تغییر موجودی.
  `recordRevenue()` برای درآمد. **پول واقعی فقط از وبهوک درگاه می‌آید؛ هر
  شارژ دستی ادمین یعنی حساب تستی (`is_demo`).**
- **همه‌ی دیتای upstream سمت سرور پروکسی می‌شود** (یاهو، پالی‌مارکت،
  Myfxbook). بازدیدکننده‌ی ایرانی نباید مستقیم به آن‌ها بخورد.

---

## ۲. کارهای باقی‌مانده — به ترتیب اولویت

### ۲.۱ ✅ قطع اتصال هنگام بلاک‌شدن ربات — **انجام شد (۲۰۲۶/۰۸/۱۲)**

پیاده‌سازی در `43a72ee`؛ شرح کامل در بخش ۴.۱. متن زیر برای تاریخچه مانده.

---

<details>
<summary>صورت‌مسئله‌ی اصلی</summary>

مالک پرسیده: «اگر کاربر حساب را وصل کند و بعد ربات را بلاک کند، اتصالش قطع
می‌شود؟» — **جواب فعلی: نه.**

`tg_user_id` در دیتابیس می‌ماند و بلاک‌شدن ربات هیچ اثری روی آن ندارد. مالک
می‌خواهد کاربر **مجبور باشد** تا وقتی از پلتفرم استفاده می‌کند ربات را نگه
دارد.

**چطور تشخیص داده می‌شود:** وقتی ربات بلاک شده باشد، `sendMessage` خطای
`Forbidden: bot was blocked by the user` برمی‌گرداند. `tgCall()` در
`src/lib/telegram.ts` این را در `error` می‌دهد.

**پیشنهاد پیاده‌سازی (با مالک تأیید کن):**
- ستون `tg_blocked_at TIMESTAMPTZ` روی `players`.
- هر جا `sendTelegram` خطای `bot was blocked` داد، ستون پر شود.
- یک بررسی دوره‌ای (همان کرون تسویه) که برای حساب‌های متصل یک
  `getChat` بزند و بلاک‌شده‌ها را علامت بزند.
- `hasLinkedTelegram()` حساب بلاک‌شده را متصل حساب نکند، پس چهار مسیر پولی
  خودکار قفل می‌شوند و کاربر پیام روشن می‌گیرد.

⚠️ **مراقب باش:** این تغییر می‌تواند کاربر واقعی را از پول خودش جدا کند. حتماً
پیام خطا صریح باشد («ربات را آنبلاک کنید») و **برداشت** را قفل نکن تا کسی پولش
گیر نیفتد — همان اصلی که `money-guard.ts` رویش نوشته شده (مرز روی ورود پول
است، نه خروج).

</details>

### ۲.۲ 🔴 پیام سراسری ربات (Broadcast) با صف

مالک می‌خواهد از ربات به همه‌ی کاربران دیتابیس پیام بفرستد (کمپین، پرومو).

**الزامات صریح مالک:**
- ارسال به ~۲۰ هزار کاربر **نباید سایت و مینی‌اپ را بخواباند** — یعنی worker یا
  صف، نه حلقه‌ی همزمان در یک ریکوئست.
- امکان ارسال **عکس + متن**: ادمین عکس را در ربات می‌فرستد، بعد رویش ریپلای
  می‌کند با یک کامند و متن.

**نکات فنی که باید رعایت شود:**
- تلگرام سقف ~۳۰ پیام در ثانیه دارد. باید throttle شود، وگرنه ۴۲۹ می‌گیری.
- Railway یک پروسه است؛ حلقه‌ی طولانی داخل route handler، ریکوئست‌های دیگر را
  گرسنه می‌کند. راه سالم: جدول `broadcast_jobs` + `broadcast_targets` و یک
  مصرف‌کننده که همان کرون هر ۱۵ دقیقه (یا کرون جدا با فاصله‌ی کوتاه‌تر) هر بار
  یک دسته (مثلاً ۵۰۰ نفر) را می‌فرستد و وضعیت را ذخیره می‌کند.
- ارسال باید idempotent باشد: اگر کرون وسط کار قطع شد، دوباره از همان‌جا ادامه
  دهد و کسی دو بار پیام نگیرد.
- کاربری که ربات را بلاک کرده، خطا می‌دهد؛ همان‌جا `tg_blocked_at` را پر کن
  (با ۲.۱ یکی می‌شود).

### ۲.۳ 🟡 بخش پشتیبانی در ربات و مینی‌اپ

کاربر باید بتواند راحت به پشتیبانی تلگرام پیام بدهد.
- در ربات: کامند `/support` یا دکمه.
- در مینی‌اپ: ورودی در تب پروفایل یا یک دکمه‌ی ثابت.
- آدرس پشتیبانی: `LINKS.telegramSupport` در `src/config/site.ts`.

### ۲.۴ 🟡 طیف رنگی پرتوهای هیرو

`src/components/ProbabilityHorizon.tsx` — پرتوهایی که از پشت کره می‌آیند الان
فقط سبز و طلایی‌اند. مالک می‌خواهد **آبی، قرمز، بنفش و سفید** هم اضافه شود.
تعادل بصری را حفظ کن؛ این اولین چیزی است که بازدیدکننده می‌بیند.

### ۲.۵ 🟡 بخش آکادمی (مالک گفته فعلاً دست نزن، ولی طراحی‌اش را بدان)

الان `آکادمی` در منو به `/#academy` اشاره می‌کند و **صفحه‌ای پشتش نیست**.

خواسته‌ی مالک: بخشی که خودش از **پنل ادمین** مقاله بسازد، با تصویر، و لینک
ویدیوی یوتیوب هم بگذارد — «حرفه‌ای‌ترین، استانداردترین و گوگل‌پسندانه‌ترین شکل
ممکن».

**نکاتی که باید در طراحی لحاظ شود:** جدول `articles` با اسلاگ فارسی‌فرندلی،
متادیتای SEO مجزا (title/description/OG image)، `JSON-LD` از نوع `Article`،
sitemap پویا، تاریخ انتشار و به‌روزرسانی، و ادیتور ساده در `/admin`.

### ۲.۶ 🔴 دامنه‌ی اصلی + SEO — **مهلت‌دار**

**وضعیت ۲۰۲۶/۰۸/۱۲:** دامنه `narmoon.io` است، مالِ خودِ ماست (ثبت‌کننده
«Narmoon»، ترکیه)، ولی **منقضی شده و در `redemptionPeriod` است**. تاریخ انقضا
۲۰۲۶/۰۶/۰۴ بود.

- سفارش خودکار بازیابی شکست خورد (`Unable to find domain information`) چون
  دامنه از داشبورد حساب خارج شده. مبلغ ۷۵.۹۸ دلار برگشت خورد.
- پشتیبانی Namecheap پذیرفت که **دستی** بازیابی کند: باید حساب پیش‌شارژ شود و
  هزینه‌ی کل **۱۳۵.۹۸ دلار** و **غیرقابل‌برگشت** است.
- ⏰ خودشان گفتند دامنه «حدود ۷۰ تا ۸۰ روز پس از انقضا» آزاد می‌شود. یعنی
  پنجره از ۲۰۲۶/۰۸/۱۱ باز است و **حداکثر تا ۲۰۲۶/۰۸/۲۲**. بعد از آن بازیابی
  ممکن نیست و دامنه به مزایده/drop-catcher می‌رود.
- Namecheap سرویس backorder ندارد؛ اگر بازیابی شکست بخورد باید سرویس بیرونی.

**تنها کاری در کل این فهرست که مهلتش دست ما نیست.**

وضعیت را با این دستور چک کن (منبع رسمی، نه داشبورد Namecheap):

```bash
whois narmoon.io | grep -iE "Domain Status|Expiry Date"
```

`redemptionPeriod` یعنی هنوز قابل بازیابی · `pendingDelete` یعنی دیر شده.

⚠️ پیش از اتصال دامنه، **حتما از داخل ایران تستش کن** — سایت قبلی روی همین
دامنه بوده و اگر آن موقع مسدود شده باشد، الان هم هست. این را قبل از لانچ باید
دانست، نه بعدش.

**SEO برای مالک خیلی مهم است.**

چک‌لیست:
- Custom Domain در Railway + DNS (پیشنهاد سند قبلی: Cloudflare با پروکسی روشن)
- `SITE_URL` در Railway به دامنه‌ی جدید (⚠️ وبهوک تلگرام به این وابسته است و
  بعد از تغییر باید **دوباره ثبت شود** — دکمه‌اش در `/admin` هست)
- `metadataBase` و همه‌ی لینک‌های هاردکد به دامنه‌ی جدید
- `robots.txt` و `sitemap.xml` (الان وجود ندارند — باید ساخته شوند)
- ریدایرکت ۳۰۱ از دامنه‌ی Railway به دامنه‌ی اصلی
- بررسی OG image ها روی دامنه‌ی جدید
- Google Search Console + تأیید مالکیت

---

## ۳. اقتصاد واقعی — اعداد قفل‌شده

**قبل از تغییر هرکدام از مالک بپرس.**

### بازار ایران (تنها بخش با پول واقعی) — `src/lib/iran.ts`

```
COMMISSION        = 0.03    // ۳٪ از حجم استخر، نه از سود
MIN_STAKE_USDT    = 1
MIN_ODDS          = 1.05    // زیر این، بازار باطل می‌شود
DISPUTE_HOURS     = 24
PROPOSE_FEE_USDT  = 1       // اگر بازار رد شود کامل برمی‌گردد
```

`MIN_WITHDRAW = 10` دیگر اینجا نیست: در `src/lib/wallet-rules.ts` است و تنها
منبع حقیقت همان‌جاست. آنجا نوشته شد و نه `iran.ts`، چون هر دو فرم برداشت
`"use client"` هستند و `iran.ts` به `pg` وصل است — ایمپورتش درایور دیتابیس را
به باندل مرورگر می‌کشد. `wallet-rules.ts` عمداً **هیچ ایمپورتی ندارد**؛ این
قید را نشکن.

`ضریب برد = (کل استخر × ۰.۹۷) ÷ مجموع پیش‌بینی‌های طرف برنده`

### امتیازی

```
WELCOME_CREDITS    = 10     // game.ts
POLY_FREE_PER_DAY  = 5      // poly.ts
POLY_EXTRA_COST    = 1      // poly.ts
REFERRAL_PERCENT   = 10     // referral.ts
REFERRAL_BONUS     = 5      // referral.ts — MOON هدیه به دعوت‌شده
REF_VOL_PCT        = 2.0    // game.ts — مرجع نوسان، *پورسانت نیست*
```

**امتیاز ترید و کمبو حالا اعشاری است** (`NUMERIC(14,4)`). گرد کردن فقط در
لحظه‌ی نمایش انجام می‌شود (`ROUND(...)::int` در روت‌ها). این cast **اجباری**
است، وگرنه درایور `pg` مقدار NUMERIC را رشته برمی‌گرداند و محاسبات کلاینت
می‌شکند.

### چالش پراپ — `src/lib/challenge.ts`

```
CONSISTENCY_PCT     = 35
ELIGIBLE_PROB_MIN   = 0.25
ELIGIBLE_PROB_MAX   = 0.75
MAX_COUNTED_PREDS   = 50
MAX_ENTRIES_PER_30D = 3
```

هفت تیر: `fx250/fx500/fx1k` و `pr5k/pr10k/pr25k/pr50k`.

⚠️ **تله‌ی شناسه‌ی تیر:** اگر روزی شناسه‌ای را عوض یا حذف کردی، حساب‌های قدیمی
`tierKnown=false` می‌گیرند: کارنامه‌شان می‌ماند ولی آستانه‌ها «نامعلوم» می‌شود و
وضعیت خودکار به‌روز نمی‌شود. این عمدی است. روی پروداکشن دو حساب با
`tier_id='c10k'` بودند که با `UPDATE` به `pr10k` نگاشت شدند.

---

## ۴. آنچه در این فاز انجام شد (خلاصه)

### باگ‌های اقتصادی که پیدا و رفع شدند

1. **بازار ایران تسویه‌ی خودکار نداشت.** `/api/predict/settle` فقط نبض بازار،
   ترید و کمبو را تسویه می‌کرد. `settleIrMarket` فقط از پنل ادمین دستی صدا
   زده می‌شد. ۱۰,۸۰۸ تتر در وضعیت `settling` گیر کرده بود.
   → `settleDueIrMarkets()` ساخته و به همان کرون وصل شد. پرداخت شد و تأیید شد.
2. **زنجیره‌ی فارم رفرال.** پورسانت MOON از هیچ ساخته می‌شد و ثبت‌نام سایت هیچ
   اعتبارسنجی تلگرامی نداشت. با curl واقعی بازتولید شد.
   → پورسانت فقط وقتی پرداخت می‌شود که **دعوت‌کننده هم** تلگرام وصل داشته باشد،
   و ورود به چالش `requireLinkedTelegram` گرفت.
3. **سوگیری گرد کردن در امتیاز.** `Math.round` جدا روی برد و باخت اعمال می‌شد؛
   چون احتمال دو طرف مکمل‌اند، همیشه یک طرف سود می‌برد. شبیه‌سازی: **+۰.۴۵
   امتیاز در هر پیش‌بینی بدون هیچ مهارتی**.
   → امتیاز اعشاری شد؛ EV حالا دقیقاً صفر است.
4. **روت کندل هاردکد بود:** `assetParam === "XAU" ? "XAU" : "BTC"` — نمودار هر
   ۴۲ دارایی بیت‌کوین را نشان می‌داد.
5. **تزریق HTML در پیام‌های تلگرام.** هیچ escaping نبود و سؤال بازار مستقیم
   داخل `<b>${question}</b>` با `parse_mode=HTML` می‌رفت. امکان جاگذاری
   `<a href>` در پست کانال رسمی (فیشینگ) وجود داشت. → `escapeHtml()`.
6. **کارنامه‌ی چالش با تیر ناشناخته ناپدید می‌شد** در حالی که خرید تازه با
   `active_exists` رد می‌شد — کاربر هم قفل بود هم کور.

### امنیت — تست‌شده و سالم

| تست | نتیجه |
|---|---|
| `initData` جعلی / منقضی / با `user.id` عوض‌شده | هر سه رد ✅ |
| توکن نشست با آیدی یا امضا یا انقضای دستکاری‌شده | هر سه ۴۰۱ ✅ |
| وبهوک بدون هدر / غلط / هم‌طول غلط / خالی | هر چهار ۴۰۱ ✅ |
| تزریق در `callback_data` (SQL، عدد غول‌پیکر، منفی) | همه بی‌اثر ✅ |
| تصادم هویت سایت و تلگرام روی یک هندل | حساب سایتی دست‌نخورد ✅ |
| قفل ردیف در چهار مسیر خرج‌کننده | `FOR UPDATE` قبل از شمارش ✅ |
| ریاضیات parimutuel و هر سه مسیر تسویه | تا شش رقم اعشار درست ✅ |

### متون

هر سه سند مالک اعمال شد: بازنویسی کامل صفحه‌ی اصلی، ترید، نبض بازار، بازار
ایران، کیف پول، پنل کاربری، چالش، ربات، نتایج، دعوت، لیدربورد، ورود، سه پیام
ربات، و مینی‌اپ. واژگان شرط‌بندی حذف شد (۲۲۶ جایگزینی)، ۱۰۷ خط تیره‌ی نثر
بازنویسی شد، و لحن به رسمی تبدیل شد — **جز شعار هیرو** که عمداً امری مفرد ماند.

---

## ۴.۱ پس از نگارش این سند (۲۰۲۶/۰۸/۱۲)

دو کامیت، هر دو روی پروداکشن.

### `4acf06d` — قواعد برداشت (یافته‌های B و E)

- **`src/lib/wallet-rules.ts` (جدید)** — تنها منبع حقیقتِ `MIN_WITHDRAW` و چک
  شکلی آدرس. **هیچ ایمپورتی ندارد**، چون سرور و هر دو فرم کلاینت می‌خوانندش.
  عدد قبلا در سه فایل و یک بار هم به‌صورت نثر در کامنت `iran.ts` تکرار شده بود.
- **`src/lib/withdraw-address.ts` (جدید)** — چک‌سام Base58Check روی سرور. این
  لایه‌ای است که «یک حرف اشتباه تایپ‌شده» را می‌گیرد؛ فرم‌ها فقط شکل را
  می‌سنجند چون `crypto` نود در مرورگر نیست.
- چک قبلی فقط `length >= 20` بود و آدرس اتریوم، آدرس بریده‌شده و ۲۲ کاراکتر
  زباله را قبول می‌کرد.
- شبکه‌ی ناشناخته عمدا به چک قدیمی برمی‌گردد، تا تغییر شبکه‌ی درگاه برداشت را
  کامل قفل نکند.

⚠️ **شکاف تست که باید بدانی:** اعتبارسنجی آدرس فقط روی **لوکال** رفتاری تست
شد (آدرس غلط → `bad_address`؛ آدرس معتبر → `insufficient_funds`، یعنی بدون
رسیدن به درگاه). روی پروداکشن عمدا زده نشد چون کلید واقعی Zovix آنجاست. آنچه
روی پروداکشن تأیید شد فقط رسیدن کامیت و سالم‌بودن صفحه‌ها بود. **اولین برداشت
واقعی هنوز انجام نشده و همچنان تنها مسیر تست‌نشده‌ی پلتفرم است.**

### `23ea94b` — سقف نرخ (یافته‌ی C)

- سقف نرخ روی ۱۶ مسیر. برای مسیرهای احراز‌شده **بر اساس کاربر، نه آی‌پی** —
  مخاطب ما پشت VPN است و ده‌ها نفر از یک آی‌پی خروجی می‌آیند.
- کلید سطل فقط **شناسه‌ی بازیکن** است: امضا وارد حافظه نمی‌شود، انقضا هم بیرون
  می‌ماند (وگرنه logout/login سقف را صفر می‌کرد)، و پیشوند `tg.` حذف می‌شود تا
  یک نفر با سایت و مینی‌اپ دو سهمیه نگیرد.
- **سه مسیر عمدا معاف‌اند** و *قبل* از قواعد سنجیده می‌شوند تا قاعده‌ی آینده
  تصادفا نگیردشان: وبهوک درگاه، وبهوک تلگرام، تسویه‌ی کرون. ۴۲۹ دادن به این سه
  یعنی واریز گم‌شده، پیام گم‌شده، و پول گیرکرده در `settling`.
- یک باگ پایداری در خود محافظ رفع شد: جارو به‌محض عبور از ۵۰۰۰ ورودی در **هر
  درخواست** کل نقشه را می‌پیمود. حالا حداکثر یک بار در دقیقه.
- ۱۷ فرم پیام فارسی `rate_limited` گرفتند.

⚠️ محدودیت پابرجا: شمارنده در حافظه است — یافته‌ی I در بخش ۵.

### `43a72ee` + `3320b21` — بلاک‌شدن ربات (بند ۲.۱)

ستون‌های `tg_blocked_at` و `tg_checked_at` روی `players` (در بلوک idempotent
همان `ensureTelegramTables`).

- **تشخیص فعال در مسیر پولی.** بلاک‌کردن ربات به تلگرام خبر می‌دهد نه به ما،
  پس منتظرِ شکستِ ارسال ماندن کافی نیست: ممکن است هرگز پیامی نفرستیم. هر
  تلاش برای خرج‌کردن با `sendChatAction` چت را می‌سنجد — چیزی در چت
  نمی‌گذارد ولی همان ۴۰۳ را می‌دهد. **هیچ بررسی دوره‌ای ساخته نشد**؛
  جاروی هر ۱۵ دقیقه روی ده‌ها هزار حساب گران و بی‌دلیل است.

  ⚠️ نسخه‌ی اول (`43a72ee`) دقیقاً همین را کم داشت و در تست مالک هیچ‌چیز قفل
  نشد: بازبینی زنده فقط برای حساب‌های *از قبل علامت‌خورده* اجرا می‌شد، یعنی
  فقط آزاد می‌کرد و هرگز تشخیص نمی‌داد. رفع در `3320b21`.

- **probe سه حالته و نامتقارن است — این مهم‌ترین جزئیات امنیتی اینجاست.**
  فقط رشته‌ی صریح تلگرام علامت می‌زند؛ فقط پاسخ موفق علامت را برمی‌دارد؛ هر
  چیز دیگر (تایم‌اوت، قطعی تلگرام، توکن غلط) وضعیت ذخیره‌شده را دست‌نخورده
  می‌گذارد. اگر «هر خطا» را بلاک بگیری، یک دقیقه اختلال در تلگرام **همه‌ی
  کاربران را از خرج‌کردن پول خودشان قفل می‌کند** — خطایی به‌مراتب بدتر از
  ردشدن یک بلاک‌کننده.

- **`tg_checked_at` هزینه را مهار می‌کند:** نمایش وضعیت (که با هر بارگذاری
  صفحه صدا زده می‌شود) نتیجه را ۱۰ دقیقه کش می‌کند؛ مسیر پولی همیشه تازه
  می‌پرسد. نتیجه‌ی `unknown` زمان را به‌روز نمی‌کند تا دفعه‌ی بعد دوباره
  تلاش شود.
- **فقط همان یک رشته علامت می‌زند.** `chat not found` مبهم است و
  `user is deactivated` حسابی است که کاربر نمی‌تواند درستش کند؛ قفل‌کردنش
  فقط بن‌بست می‌سازد.
- **مرز مصوب مالک:** چهار مسیر *ورودِ* پول قفل می‌شوند (شارژ MOON، پیش‌بینی
  بازار ایران، ساخت بازار، ورود به چالش). **برداشت هرگز.** روت برداشت با
  `requireLinkedTelegram(id, { evenIfBlocked: true })` صریحاً استثنا شده —
  اگر آن هم قفل می‌شد، یک بلاک ساده پول کاربر را حبس می‌کرد و کلید بازکردنش
  داخل اپ شخص ثالث بود.
- **دو راه بازگشت:** (۱) هر پیام یا کلیک در وبهوک علامت را پاک می‌کند، بدون
  هیچ تماس API. (۲) حساب علامت‌خورده در لحظه‌ی خرج‌کردن زنده بازبینی می‌شود با
  `sendChatAction` — چیزی در چت نمی‌گذارد ولی همان ۴۰۳ را می‌دهد.
- **وضعیت پیش از برخورد دیده می‌شود:** کارت اتصال در `/referral` به هشدار
  تبدیل می‌شود و صریحاً می‌گوید برداشت بسته نیست؛ نشان سبز نوار بالا هم
  خاموش می‌شود.
- ⚠️ **fail-open عمدی:** اگر `TG_BOT_TOKEN` نباشد، بازبینی زنده علامت را
  برمی‌دارد. این محدودیت ضدتقلب است نه مرز امنیتی، و قفل‌کردن بر پایه‌ی چیزی
  که قابل اثبات نیست بدتر است.

⚠️ **شکاف تست که باعث همان باگ شد:** خودِ پاسخ ۴۰۳ تلگرام هرگز لوکال دیده
نمی‌شود، چون `TG_BOT_TOKEN` در `.env.local` **نامعتبر است** (`getMe` → Not
Found). همه‌ی تست‌های لوکال حالت `unknown` را می‌سنجند، نه `blocked` واقعی.
الگوی تشخیص جدا روی هفت رشته‌ی واقعی تلگرام تست شد و بقیه‌ی زنجیره سرتاسر روی
دیتابیس محلی.

**این بخش فقط با تست مالک تأیید می‌شود** (بند ۴ بخش ۶). اگر باز هم قفل نشد،
اول این را بپرس: آیا `sendChatAction` واقعاً برای آن حساب ۴۰۳ می‌دهد؟ با
توکن پروداکشن:

```bash
curl -s "https://api.telegram.org/bot<TOKEN>/sendChatAction" \
  -d chat_id=<TG_USER_ID> -d action=typing
```

---

## ۵. یافته‌های باز (رفع نشده)

| # | مورد | شدت |
|---|---|---|
| A | `is_demo` از راه بُرد بازار منتقل نمی‌شود — پول دمو به حساب غیردمو می‌رسد و داشبورد درآمد آن را واقعی می‌شمارد | 🟡 |
| D | `getWithdrawal` در `zovix.ts` نوشته شده ولی **هیچ‌جا صدا زده نمی‌شود** — برداشت گیرکرده روی `submitted` یخ می‌زند | 🟡 |
| F | ثبت‌نام سایت هیچ اعتبارسنجی تلگرامی ندارد (چندحسابی ممکن) | 🟡 |
| I | سقف نرخ **در حافظه** نگه داشته می‌شود، پس با هر دیپلوی صفر می‌شود و روی چند اینستنس کار نمی‌کند. جلوی سوءاستفاده‌ی معمولی را می‌گیرد، ولی سد محکمی نیست. راه‌حل واقعی: شمارنده در Postgres. لایه‌ی بعدی، Cloudflare است (بند ۲.۶) | 🟡 |
| G | بازار ایران تاریخچه‌ی احتمال ندارد (نمودار زمانی ممکن نیست) | 🟢 |
| H | دسته‌های بازار ایران (`economy/sports/crypto/social/other`) با دامنه‌ی فاز یک سند قدیمی نمی‌خواند؛ `sports` باز است | 🟢 |

**بسته‌شده‌ها:** B و E و C در ۲۰۲۶/۰۸/۱۲ رفع شدند — بخش ۴.۱ را ببین.

**پیشنهاد ترتیب باقی‌مانده:** D (تا برداشت گیرکرده بی‌صاحب نماند)، بعد A، بعد F.

---

## ۶. کارهای مالک (بیرون از کد)

1. 🔴 **مهلت‌دار — حساب Namecheap را ۱۴۰ دلار شارژ کن** و به پشتیبانی بگو
   بازیابی `narmoon.io` را انجام دهد (۱۳۵.۹۸ دلار، غیرقابل‌برگشت). جزئیات و
   مهلت در بند ۲.۶.
2. ✅ کرون Railway ست شده (سرویس `curl`، هر ۱۵ دقیقه) — کار می‌کند
3. **ربات را در کانال ادمین کن** تا حالت `live` نظرسنجی کار کند
4. **تست بلاک‌کردن ربات** (بند ۲.۱ — تنها بخشی که لوکال قابل تست نبود): ربات
   را از حساب خودت بلاک کن → شارژ MOON باید پیام «ربات را بلاک کرده‌اید»
   بدهد و برداشت باید کار کند → آنبلاک کن و `/start` بزن → فوراً باز شود.
   و در Railway → Postgres → Data این را بزن (باید یک ردیف برگرداند):

   ```sql
   SELECT column_name FROM information_schema.columns
   WHERE table_name='players' AND column_name='tg_blocked_at';
   ```
5. **اولین برداشت واقعی با مبلغ کم** — تنها مسیری که عمداً تست نشده، چون درگاه
   با کلیدهای واقعی کار می‌کند. اعتبارسنجی جدید آدرس هم فقط روی لوکال تست شده
   (بخش ۴.۱)، پس این تست دو چیز را با هم می‌سنجد.
6. **نام گیت** — همه‌ی کامیت‌ها با `نام شما <ایمیل شما>` ثبت شده‌اند
7. `MYFXBOOK_ACCOUNT_ID` روی Railway ست نیست؛ پروداکشن از پیش‌فرض هاردکد
   `"12121235"` استفاده می‌کند — **آیا این آیدی واقعی است؟** (مالک گفته فعلاً
   بماند برای بعد)

---

## ۷. راهنمای «کجا ببینم» (سؤال‌های تکراری مالک)

- **کارنامه‌ی کمبو:** `/trade` → تب **«کمبو»** (تب چهارم بالای جدول). فهرست
  زیر فرم ساخت است. اگر تیکتی نداشته باشی، متن حالت خالی نشان داده می‌شود.
  کد: `ComboBuilder.tsx` که داخل `TradeTerminal.tsx` رندر می‌شود.
- **کارنامه‌ی چالش پراپ:** `/challenge` (بالای صفحه) و پایین `/trade`.
  در مینی‌اپ: تب «پراپ».
- **نبض بازار در مینی‌اپ:** تب «نبض بازار» (تب سوم از راست).
- **کارنامه‌ی زنده و بروکر:** `/bot#results` و `/bot#broker`.

---

## ۸. دستورها

```bash
npm run dev      # لوکال روی :3000
npm run build    # بیلد production
npm run lint     # eslint — baseline ۳۲ مورد
npx tsc --noEmit # تایپ‌چک
```

دیتابیس لوکال: `postgresql://amiractive@localhost:5432/narmoon`

⚠️ میدل‌ور در **`src/middleware.ts`** است، نه ریشه. چون پروژه پوشه‌ی `src`
دارد، فایلِ ریشه بی‌صدا نادیده گرفته می‌شود و `tsc` و `build` هم سبز می‌مانند.

⚠️ **`TG_BOT_TOKEN` در `.env.local` نامعتبر است** (`getMe` → Not Found). هر
چیزی که به پاسخ واقعی تلگرام وابسته باشد لوکال قابل تست نیست. اول این را چک
کن، وگرنه ساعت‌ها دنبال باگی می‌گردی که وجود ندارد:

```bash
curl -s "https://api.telegram.org/bot$(grep '^TG_BOT_TOKEN=' .env.local | cut -d= -f2-)/getMe"
```

⚠️ **کلیدهای Zovix در `.env.local` واقعی‌اند.** هر تستی که به `createWithdrawal`
برسد یک برداشت واقعی می‌سازد. روال امن: موجودی کاربر تستی را **صفر** بگذار تا
چک موجودی — که *پیش* از تماس با درگاه است — جلویش را بگیرد.

متغیرهای محیطی: `DATABASE_URL`, `SESSION_SECRET` (اجباری), `ADMIN_SECRET`,
`ADMIN_PASSWORD`, `PGSSL`, `SETTLE_KEY`, `ZOVIX_*`, `TG_BOT_TOKEN`,
`TG_BOT_USERNAME`, `TG_WEBHOOK_SECRET`, `SITE_URL`, `MYFXBOOK_*`,
`NEXT_PUBLIC_DEV_FIXTURES` (فقط لوکال — پالی‌مارکت از ایران در دسترس نیست).

### کوئری‌های سلامت (باید همیشه صفر برگردانند)

```sql
-- موجودی در برابر دفترکل
SELECT p.id, p.usdt_balance, COALESCE(SUM(w.amount),0) AS ledger
FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
GROUP BY p.id, p.usdt_balance
HAVING p.usdt_balance <> COALESCE(SUM(w.amount),0);

-- استخر بازار در برابر مجموع پیش‌بینی‌ها  ← مهم‌ترین
SELECT m.id, m.yes_total, m.no_total,
       COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0) AS real_yes,
       COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0)  AS real_no
FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
GROUP BY m.id, m.yes_total, m.no_total
HAVING m.yes_total <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0)
    OR m.no_total  <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0);
```

---

## ۹. خطوط قرمز محصول (تصمیم قطعی مالک — تغییر نده)

- ادعای «سود تضمینی» هرگز.
- وعده‌ی «از پیش‌بینی پول دربیار» در پیام برند هرگز.
- ایردراپ و توکن نه. یوزر و لیدربورد فیک نه.
- **امتیازدهی صفر-انتظار حفظ شود:** پیش‌بینی تصادفی به‌طور میانگین منفی
  می‌گیرد. هر تغییری در فرمول امتیاز باید این را حفظ کند.
- استخر parimutuel تتری بازار ایران با تصمیم صریح مالک مجاز است؛ مکانیزم نقدی
  مشابه برای بازی‌های امتیازی ساخته نمی‌شود.
- ورود با گوگل و KYC هر دو **رد شدند**.
- کلمه‌ی «شرط»، «باخت»، «بازی» و «چلنج» از متن کاربر حذف شده — برنگردان.
  (`شرط` فقط به معنای «قید» مجاز است، مثل «شرایط قبولی».)
