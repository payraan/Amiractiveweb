# سند تحویل نارمون — فاز ۴: لانچ از تلگرام

> این سند را به‌عنوان اولین پیام چت جدید بفرست. جانشین `HANDOFF-3.md` است
> (آن را برای تاریخچه نگه دار).
>
> **منبع حقیقت نهایی، خودِ ریپو است.** هر جا این سند با کد تناقض داشت، کد را
> ملاک بگیر و تناقض را گزارش کن.
>
> 🎯 **تصمیم راهبردی مالک: لانچ از تلگرام شروع می‌شود، نه از سایت.** ربات و
> مینی‌اپ ویترین اصلی‌اند؛ سایت پشتیبان است. اولویت هر تصمیمی از همین می‌آید.
>
> آخرین به‌روزرسانی: ۲۰۲۶/۰۸/۱۴

---

## ۰. نقش تو و سبک کار

تو Principal Engineer این پروژه‌ای. مالک برنامه‌نویس نیست و در سطح کپی‌پیست و
اجرای دستور ترمینال کار می‌کند.

- **پاسخ‌ها به فارسی.**
- **قبل از هر کامیت:** `npx tsc --noEmit`؛ در تغییرات بزرگ `npm run build`.
- **لینت baseline = ۳۲ مورد.** قبل از هر کامیت مقایسه کن بالا نرفته باشد. اگر
  خطای تازه‌ای ساختی، **رفعش کن، سرکوبش نکن**.
- **کد را با داده‌ی واقعی تست کن، نه فقط کامپایل.** هر مسیر پولی با `curl` و
  `psql` روی دیتابیس محلی سرتاسر تست شود (سناریوی موفق + سوءاستفاده)، بعد
  کامیت. **داده‌ی تستی را بعد از تست پاک کن.**
- **در مرورگر ببین.** برای هر تغییر دیداری، صفحه را در عرض واقعی موبایل
  (۳۷۵px) باز کن و نگاه کن. نصف باگ‌های این فاز فقط این‌طور پیدا شدند.
- **دیتابیس پروداکشن را به `.env.local` اضافه نکن.** کوئری‌های پروداکشن را
  مالک در Railway → Postgres → Data اجرا می‌کند.
- **پوش:** مالک اجازه‌ی پایدار داده، ولی هر پوش را گزارش کن و پس از دیپلوی
  سلامت پروداکشن را بسنج (auto-deploy سالم است، ~۱ تا ۲ دقیقه).
- هر تغییر را پیش از اعمال با یکی‌دو جمله‌ی ساده‌ی فارسی توضیح بده.
- **هر جا تصمیم سلیقه‌ای یا اقتصادی بود، از مالک بپرس** — نه تصمیم‌های فنیِ
  معمول، بلکه جایی که دو خوانش متفاوت به کار متفاوت می‌رسد.
- اگر چیزی با این سند نمی‌خواند، **اول گزارش بده**، خودسرانه بازنویسی نکن.

### درس‌های گران (تکرارشان نکن)

1. **تعداد را با مقدار اشتباه نگیر.** روت کندل را «سالم» اعلام کردم چون هر ۱۰
   دارایی ۷۲۵ کندل می‌دادند؛ همه بیت‌کوین بودند.
2. **پیش از اعلام «انجام شد»، در مرورگر ببین.**
3. **پنل خالی = پنل نامرئی.** هر فهرستی باید حالت خالی داشته باشد.
4. **جای‌نگهدار را با نثر اشتباه نگیر.**
5. **کامپایل شدن یعنی کد ساخته شد، نه اینکه اجرا می‌شود.** میدل‌ور را در
   `middleware.ts` ریشه نوشتم؛ `tsc` و `build` سبز شدند و کد **هرگز اجرا
   نشد**. این پروژه پوشه‌ی `src` دارد، پس Next فقط **`src/middleware.ts`** را
   می‌بیند.
6. **دامنه‌ی تست را با دامنه‌ی ادعا اشتباه نگیر.** اگر منطقی رشته‌ی
   قابل‌جستجویی در باندل کلاینت ندارد، «باندل را گشتم» دلیل نیست.
7. **اگر لوکال فقط یکی از حالت‌ها را می‌سازد، بقیه تست نشده‌اند.** تشخیص
   بلاک‌شدن ربات را «تست‌شده» گزارش کردم در حالی که توکن محلی نامعتبر بود و
   همه‌ی تست‌ها فقط حالت «نامشخص» را می‌سنجیدند.
8. **جای‌گذاری تنبل، تشخیص نیست.** علامتی که فقط وقتی زده می‌شود که ما اتفاقی
   کاری بکنیم، ممکن است هرگز زده نشود.
9. 🆕 **جای‌گزینی متنی روی JSX، نیمی از کار را بی‌صدا جا می‌گذارد.** یک الگو با
   تورفتگی نوار داغ خورد و با فهرست اصلی نخورد؛ نتیجه: نصف صفحه ترجمه‌نشده
   ماند و **دیف هم درست به نظر می‌رسید**. بعد از هر تغییر تکرارشونده،
   `grep` بزن و **همه‌ی** جایگاه‌ها را بشمار.
10. 🆕 **خطا را نبلع.** نسخه‌ی اول ترجمه هر خطایی را به `null` تبدیل می‌کرد؛
    «۴۰ ناموفق» دیده می‌شد و هیچ‌کس نمی‌دانست چرا. یک شمارنده بدون علت، از
    خودِ مشکل بدتر است. علت واقعی را همیشه تا سطح رابط بالا بیاور.
11. 🆕 **نام مدل و نام API را هاردکد نکن.** `gemini-2.0-flash` وجود نداشت و
    فقط کشف خودکار نجاتش داد.

---

## ۱. پلتفرم چیست

**نارمون** — پلتفرم فارسی‌زبان هوش جمعی و بازارهای پیش‌بینی.

- ریپو: `https://github.com/payraan/Amiractiveweb` (برنچ `main`)
- دیپلوی: Railway — `https://amiractiveweb-production.up.railway.app`
- ربات: `@NarmoonMarketBot` · مینی‌اپ روی `/app`
- استک: Next.js 16 App Router، React 19، Tailwind v4، TypeScript، PostgreSQL
  با `pg` خام (بدون ORM). مسیر `@/*` → `src/*`.
- زبان: فارسی، RTL. کامنت‌های کد فارسی‌اند.

**تز محصول:** امتیاز فقط از مهارت می‌آید. واحد داخلی `MOON` است و **فقط
قابلیت باز می‌کند؛ هرگز امتیاز یا رتبه نمی‌خرد.**

### سه سطح، یک دیتابیس

| سطح | مسیر | احراز هویت |
|---|---|---|
| سایت | `/` و بقیه‌ی صفحه‌ها | کوکی `amir_session` |
| مینی‌اپ | `/app` | هدر `x-tg-auth` |
| ربات | وبهوک `/api/tg/webhook` | `tg_user_id` |

**`currentPlayerId()`** هر سه را به یک شناسه می‌رساند. **هیچ روت اختصاصی
مینی‌اپی وجود ندارد** — همه همان API سایت را می‌زنند. این عمدی است و باید
بماند: دو مسیر پولی موازی یعنی روزی یکی‌شان یک بررسی کمتر دارد.

### معماری — چیزهایی که باید بدانی

- **`db()`** یک بلوک SQL idempotent را یک‌بار در هر cold start اجرا می‌کند.
  هیچ فریم‌ورک مهاجرتی نیست. ماژول‌ها جدول‌هایشان را همین‌طور می‌سازند:
  `ensureIrTables`، `ensureTelegramTables`، `ensureChallengeTables`،
  `ensureReferralTables`، `ensurePolyTables`، `ensureComboTables`،
  `ensureFlowTable`، `ensureBroadcastTables`، `ensureTranslationTable`.
- **هویت:** `tg_user_id` لنگر است و همیشه برنده. ادغام حساب فقط دستی از ادمین.
- **قواعد پولی (تغییر نده):** کسر پول همیشه داخل ترنزاکشن با
  `SELECT … FOR UPDATE`. `moveFunds()` تنها راه تغییر موجودی.
  `recordRevenue()` برای درآمد. **پول واقعی فقط از وبهوک درگاه می‌آید.**
- **همه‌ی دیتای upstream سمت سرور پروکسی می‌شود** (یاهو، پالی‌مارکت،
  Myfxbook، Gemini). بازدیدکننده‌ی ایرانی نباید مستقیم به آن‌ها بخورد.
- **میدل‌ور در `src/middleware.ts` است، نه ریشه.**

### ماژول‌های مشترک — قبل از نوشتن کد تازه ببینشان

| فایل | چه چیزی |
|---|---|
| `lib/wallet-rules.ts` | `MIN_WITHDRAW` و چک شکلی آدرس. **بدون ایمپورت** (کلاینت هم می‌خواند) |
| `lib/withdraw-address.ts` | چک‌سام آدرس ترون (سرور) |
| `lib/withdrawal.ts` | **تنها مسیر برداشت** — سایت و ربات هر دو |
| `lib/ledger-labels.ts` | برچسب فارسی تراکنش‌ها. **بدون ایمپورت** |
| `lib/profile.ts` | داده‌ی پنل کاربری — سایت، مینی‌اپ، ربات |
| `lib/search.ts` | نرمال‌سازی جست‌وجوی فارسی + `displayTitle` |
| `lib/dates.ts` | `remaining()` و `closingSoon()` |
| `lib/translate.ts` | ترجمه‌ی عنوان بازارهای خارجی |
| `lib/bot-menu.ts` | متن و کارت‌های ربات |
| `lib/bot-wallet.ts` | کیف پول داخل ربات |
| `lib/bot-flow.ts` | حافظه‌ی گفت‌وگوی چندمرحله‌ای |
| `lib/broadcast.ts` | پیام سراسری |

⚠️ **الگوی تکرارشونده‌ی این پروژه:** هر جا یک مقدار یا برچسب در بیش از یک
فایل کپی شود، دیر یا زود از هم جدا می‌شوند. سه بار در همین فاز اتفاق افتاد
(`MIN_WITHDRAW` در ۳ جا، برچسب دفترکل در ۴ جا، `remaining()` در ۲ جا).

---

## ۲. آنچه در فاز ۳ انجام شد

۲۶ کامیت. خلاصه‌ی چیزی که ساخته شد:

**پول و امنیت:** اعتبارسنجی واقعی آدرس ترون با چک‌سام · سقف نرخ روی ۱۶ مسیر
بر اساس شناسه‌ی بازیکن نه آی‌پی · قفل مسیرهای ورودِ پول برای کاربری که ربات
را بلاک کرده (**برداشت هرگز قفل نمی‌شود**).

**ربات:** منوی کامل با دکمه‌های `web_app` که مینی‌اپ را داخل تلگرام باز
می‌کنند · کیف پول کامل در چت (موجودی، تاریخچه، واریز، برداشت سه‌مرحله‌ای) ·
پروفایل · پشتیبانی · راهنمای ده‌موضوعی · تصویر و ویدیو روی هر کارت · پیام
سراسری با صف برای ۵۰ هزار نفر.

**مینی‌اپ:** کارت‌ها از ۲۱۰ به ۹۶ پیکسل · نوار «داغ‌ترین‌ها» · جست‌وجو در هر
سه تب با نرمال‌سازی فارسی · نبض بازار با تایم‌فریم بالای صفحه و نمودار روند ·
کندل واقعی ۴ و ۱۲ ساعته · فارسی‌سازی خودکار ۴۵۰ عنوان پالی‌مارکت.

**سایت:** پرتوهای هیرو شش‌رنگ.

---

## ۳. کارهای این فاز — به ترتیب

### ۳.۱ 🔴 کارت پیش‌بینی تلگرامی برای بازارهای ترید

**سؤال مالک، و جواب: بله، و زیرساختش از قبل هست.**

بازار ایران این را دارد: `marketPoll()` در `lib/telegram.ts` متن و دکمه
می‌سازد، `sendMarketPoll()` پست می‌کند، `ir_market_posts` رد پیام‌ها را نگه
می‌دارد، و `refreshMarketPosts()` روی کرون درصدها را تازه می‌کند. ادمین از
`/api/admin/ir-poll` پست می‌گیرد.

**خواسته:** همین برای بازارهای ترید (پالی‌مارکت) که حالا فارسی‌اند.

**تفاوت‌هایی که باید حل شوند:**
- شناسه‌ی بازار پالی‌مارکت **رشته** است نه عدد؛ جدول و لینک عمیق باید بپذیرند.
- پالی‌مارکت `bettors` ندارد، `volume` دارد. کارت باید متن متفاوتی بگیرد.
- `parseStartParam` در `MiniApp.tsx` فقط `market_` و `join_` را می‌شناسد؛
  باید `trade_<id>` اضافه شود و به تب ترید برود.
- عنوان باید **فارسیِ ترجمه‌شده** باشد (`displayTitle`)، نه انگلیسی.
- `ir_market_posts` معادل ندارد؛ یا جدول دوم یا یک ستون `kind`.

**دو حالت کارت که از قبل تصمیم‌گیری شده و باید حفظ شود:**
- `forward` — دکمه‌های لینک. بعد از فوروارد کار می‌کنند ولی درصدها یخ می‌زنند.
- `live` — دکمه‌های callback. رأی درجا می‌گیرند ولی **در فوروارد حذف
  می‌شوند**. فقط وقتی ربات در کانال ادمین باشد.

این دوگانگی محدودیت خود تلگرام است، نه انتخاب ما.

### ۳.۲ 🔴 تجربه‌ی ورود مینی‌اپ (از تحلیل مینی‌اپ‌های خارجی)

مالک هشت اسکرین‌شات از مینی‌اپ‌های موفق فرستاد. تحلیل و گلچین:

**الف) صفحه‌ی پذیرش قوانین پیش از ورود** (STON.fi)
یک مودال با متن حقوقی و دکمه‌ی «تأیید و موافقم» پیش از اولین استفاده.
**ارزش برای نارمون بالاست:** این پلتفرم پول واقعی دارد و مالک ایرانی است؛ یک
پذیرش صریح ثبت‌شده (تاریخ + نسخه‌ی متن در دیتابیس) هم حقوقی مفید است و هم
حرفه‌ای به نظر می‌رسد. یک بار نشان داده می‌شود، نه هر بار.

**ب) آموزش چندمرحله‌ای اول ورود** (Blum, Major)
اسلایدهای افقی با نوار پیشرفت بالا، تصویر، و دکمه‌ی «بعدی».
**ارزش بالا:** کاربر تازه نمی‌داند «امتیاز صفر-انتظار» یعنی چه. سه تا چهار
اسلاید: نارمون چیست · امتیاز چطور حساب می‌شود · بازار ایران در برابر ترید ·
کیف پول. **قابل رد شدن باشد** و فقط یک بار.

**ج) بنر در صفحه‌ها** (Major, Blum, Crypto Bot)
کارت افقی رنگی بالای فهرست‌ها. **ارزش متوسط:** بدون محتوای واقعی فقط فضا
می‌خورد. اگر ساخته شود باید از دیتابیس بیاید (بازار داغ، کمپین) نه هاردکد.

**د) چیدمان کیف پول** (Crypto Bot)
موجودی بزرگ وسط، سه دکمه‌ی گرد زیرش (واریز/برداشت/تاریخچه)، بعد فهرست.
**ارزش بالا و ارزان:** کیف پول مینی‌اپ ما همین اجزا را دارد، فقط چیدمانش
معمولی است.

**ه) منوی ربات با دکمه‌های رنگی** (BETEWIN, PocketFi)
مالک این را خیلی پسندیده.
⚠️ **پیش از قول دادن، تأیید کن:** تا جایی که می‌دانم **Bot API فیلدی برای
رنگ دکمه ندارد** — نه برای inline و نه برای reply keyboard. آنچه در آن
اسکرین‌شات‌ها رنگی به نظر می‌رسد احتمالاً کلاینت غیررسمی یا تم کاربر است.
**اول مستندات فعلی Bot API را چک کن.** اگر واقعاً نبود، جایگزین صادقانه:
ایموجی رنگی در ابتدای متن دکمه (🟢🔴🔵) که همان حس را می‌دهد و کارت
نظرسنجی بازار ایران از قبل همین را می‌کند.

**و) صفحه‌کلید ثابت پایین چت** (Storm Trade Bot)
`ReplyKeyboardMarkup` با شش گزینه در دو ستون که **همیشه** زیر فیلد تایپ
می‌ماند. **ارزش بالا:** کاربر برای رسیدن به منو مجبور نیست دنبال پیام قدیمی
بگردد. کنار منوی inline فعلی می‌نشیند، جایگزینش نمی‌شود.

**ز) نوار ناوبری پایین مینی‌اپ** — از قبل داریم. ✅

### ۳.۳ 🔴 یکپارچگی سه سطح

پیش از لانچ باید ثابت شود که سایت، ربات و مینی‌اپ **یک اقتصاد** دارند:
- یک پیش‌بینی از هر سطح، در هر سه سطح دیده شود.
- موجودی و MOON و امتیاز در هر سه یکی باشد.
- رفرال، چالش، لیدربورد، کمبو، نبض بازار، ترید، بازار ایران — همه.
- **کوئری‌های سلامت بخش ۸ باید همیشه صفر برگردانند.**

### ۳.۴ 🔴 تست پایداری و امنیت با ۵۰ هزار کاربر مجازی

مالک تست دقیق و مو‌به‌مو خواسته. پیشنهاد دامنه:
- ساخت ۵۰ هزار کاربر مجازی در دیتابیس **محلی** (نه پروداکشن).
- بار همزمان روی مسیرهای پولی و خواندنی؛ سنجش زمان پاسخ و استخر اتصال.
- ⚠️ **تله‌ی شناخته‌شده:** سقف نرخ در حافظه است و با هر دیپلوی صفر می‌شود
  (یافته‌ی I). زیر بار باید دیده شود که جارویش گلوگاه نمی‌شود.
- سناریوهای سوءاستفاده: برداشت همزمان، شرط همزمان روی یک بازار، رفرال
  حلقه‌ای، چندحسابی، توکن جعلی، سقف نرخ.
- **پیش از هر تست، داده‌ی تستی را از پروداکشن جدا نگه دار.**

### ۳.۵ 🟡 بازبینی همه‌ی متن‌ها

مالک **به‌شدت** حساس است که متن‌ها رباتیک و «هوش‌مصنوعی‌وار» نباشند.
- فهرست گلوله‌ای با ساختار گرامری یکسان، جمله‌های متقارن، «چه چیزی در دسترس
  شماست» — همه علامت متن تولیدشده‌اند.
- واژگان شرط‌بندی («شرط»، «باخت»، «بازی»، «چلنج») حذف شده؛ برنگردان.
  (`شرط` فقط به معنای «قید» مجاز است.)
- عنوان‌های ترجمه‌شده‌ی پالی‌مارکت را نمونه‌گیری کن؛ اصطلاحات فنی و اسم بازی‌ها
  جایی است که ترجمه‌ی ماشینی می‌لغزد (نمونه‌ی دیده‌شده: `Inhibitord`).
  اصلاح دستی از `/admin` → تب ترجمه‌ها.

### ۳.۶ 🟡 تنظیمات نهایی ربات و مینی‌اپ (آخرین قدم پیش از لانچ)

از BotFather: عکس پروفایل · توضیح کوتاه و بلند · متن about · دکمه‌ی منو ·
تنظیم Mini App با نام و آیکون · دستورها (از `/admin` ثبت می‌شود).

### ۳.۷ 🟢 مارکتینگ تلگرامی — ایده‌ها برای مشورت با مالک

مالک گفته این برایش بسیار مهم است و ایده می‌خواهد:
- **کارت بازار به‌عنوان محتوای ویروسی:** هر بازار یک کارت قابل فوروارد است.
  این ارزان‌ترین کانال رشد است و بند ۳.۱ مستقیم به آن خدمت می‌کند.
- **پاداش سازنده‌ی بازار** (از سند فاز ۲): ادمین کانال بازار می‌سازد و سهمی
  از فعالیت رویش می‌گیرد. موتور رشد اصلی.
- **پخش سراسری** ساخته شده — برای کمپین آماده است.
- **رفرال** ساخته شده؛ پورسانت فقط با تلگرام متصل.
- **هدیه‌ی عضویت کانال** (`/bonus`) ساخته شده.
- ایده‌های تازه پیش از پیاده‌سازی با مالک چک شوند.

---

## ۴. اقتصاد — اعداد قفل‌شده

**قبل از تغییر هرکدام از مالک بپرس.**

### بازار ایران (تنها بخش با پول واقعی) — `src/lib/iran.ts`

```
COMMISSION        = 0.03    // ۳٪ از حجم استخر، نه از سود
MIN_STAKE_USDT    = 1
MIN_ODDS          = 1.05    // زیر این، بازار باطل می‌شود
DISPUTE_HOURS     = 24
PROPOSE_FEE_USDT  = 1       // اگر بازار رد شود کامل برمی‌گردد
```

`MIN_WITHDRAW = 10` در `lib/wallet-rules.ts` است و تنها منبع حقیقت همان‌جاست.
آنجا نوشته شد و نه `iran.ts`، چون فرم‌های برداشت `"use client"` هستند و
`iran.ts` به `pg` وصل است. **`wallet-rules.ts` باید بدون ایمپورت بماند.**

`ضریب برد = (کل استخر × ۰.۹۷) ÷ مجموع پیش‌بینی‌های طرف برنده`

### امتیازی

```
WELCOME_CREDITS    = 10     // game.ts
POLY_FREE_PER_DAY  = 5      // poly.ts
REFERRAL_PERCENT   = 10     // referral.ts
REFERRAL_BONUS     = 5      // referral.ts
GROUP_BONUS_CREDITS= 20     // telegram.ts
```

تایم‌فریم‌های نبض بازار (`game.ts`): ۲۴س رایگان×۲ · ۱۲س ۲ MOON · ۴س ۳ MOON ·
۱س ۴ MOON.

**امتیاز ترید و کمبو اعشاری است** (`NUMERIC(14,4)`). گرد کردن فقط در لحظه‌ی
نمایش با `ROUND(...)::int` — این cast **اجباری** است، وگرنه `pg` رشته
برمی‌گرداند و محاسبات کلاینت می‌شکند.

### چالش پراپ — `src/lib/challenge.ts`

```
CONSISTENCY_PCT=35 · ELIGIBLE_PROB_MIN=0.25 · ELIGIBLE_PROB_MAX=0.75
MAX_COUNTED_PREDS=50 · MAX_ENTRIES_PER_30D=3
```

هفت تیر: `fx250/fx500/fx1k` و `pr5k/pr10k/pr25k/pr50k`.
⚠️ شناسه‌ی تیر را عوض نکن؛ حساب‌های قدیمی `tierKnown=false` می‌گیرند.

---

## ۵. یافته‌های باز

| # | مورد | شدت |
|---|---|---|
| A | `is_demo` از راه بُرد بازار منتقل نمی‌شود — پول دمو به حساب غیردمو می‌رسد | 🟡 |
| D | `getWithdrawal` در `zovix.ts` هیچ‌جا صدا زده نمی‌شود — برداشت گیرکرده روی `submitted` یخ می‌زند | 🟡 |
| F | ثبت‌نام سایت اعتبارسنجی تلگرامی ندارد (چندحسابی ممکن) | 🟡 |
| I | سقف نرخ **در حافظه** است؛ با هر دیپلوی صفر می‌شود. راه‌حل واقعی: شمارنده در Postgres. لایه‌ی بعدی: Cloudflare | 🟡 |
| G | بازار ایران تاریخچه‌ی احتمال ندارد (نمودار زمانی ممکن نیست) | 🟢 |
| H | دسته‌های بازار ایران با دامنه‌ی فاز یک نمی‌خواند؛ `sports` باز است | 🟢 |

---

## ۶. کارهای مالک (بیرون از کد)

1. 🔴 **دامنه‌ی `narmoon.io`** — منقضی شده بود و در `redemptionPeriod`.
   بازیابی دستی ۱۳۵.۹۸ دلار بود و پنجره حدود ۲۰۲۶/۰۸/۲۲ بسته می‌شد.
   **وضعیت نامعلوم — از مالک بپرس.** چک با:
   ```bash
   whois narmoon.io | grep -iE "Domain Status|Expiry Date"
   ```
   `redemptionPeriod` = هنوز قابل بازیابی · `pendingDelete` = دیر شده.
   ⚠️ پیش از اتصال دامنه، **از داخل ایران تستش کن** — سایت قبلی روی همین
   دامنه بوده و ممکن است مسدود باشد.
2. ✅ کرون Railway هر ۱۵ دقیقه — کار می‌کند
3. ✅ `GEMINI_API_KEY` ست شده؛ ۴۵۰ عنوان ترجمه شد
4. **`TG_ADMIN_IDS`** برای پخش سراسری — وضعیت نامعلوم، بپرس
5. **ربات را در کانال ادمین کن** تا حالت `live` کارت‌ها کار کند
6. **اولین برداشت واقعی با مبلغ کم** — تنها مسیری که عمداً تست نشده
7. `MYFXBOOK_ACCOUNT_ID` ست نیست؛ پیش‌فرض هاردکد `"12121235"` — واقعی است؟

---

## ۷. SEO و دامنه (بعد از تلگرام)

مالک لانچ را از تلگرام شروع می‌کند، پس SEO اولویت دوم است. ولی هنوز:
`robots.txt` و `sitemap.xml` **وجود ندارند** · `metadataBase` و لینک‌های
هاردکد باید به دامنه‌ی جدید بروند · بعد از تغییر `SITE_URL` **وبهوک تلگرام
باید دوباره ثبت شود** (دکمه‌اش در `/admin`) · Search Console.

---

## ۸. دستورها و محیط

```bash
npm run dev      # لوکال روی :3000
npm run build    # بیلد production
npm run lint     # eslint — baseline ۳۲ مورد
npx tsc --noEmit # تایپ‌چک
```

دیتابیس لوکال: `postgresql://amiractive@localhost:5432/narmoon`

⚠️ میدل‌ور در **`src/middleware.ts`** است، نه ریشه.

⚠️ **`TG_BOT_TOKEN` در `.env.local` نامعتبر است** (`getMe` → Not Found). هر
چیزی که به پاسخ واقعی تلگرام وابسته باشد لوکال قابل تست نیست. اول چک کن:

```bash
curl -s "https://api.telegram.org/bot$(grep '^TG_BOT_TOKEN=' .env.local | cut -d= -f2-)/getMe"
```

⚠️ **کلیدهای Zovix در `.env.local` واقعی‌اند.** هر تستی که به
`createWithdrawal` برسد یک برداشت واقعی می‌سازد. روال امن: موجودی کاربر تستی
را **صفر** بگذار تا چک موجودی — که *پیش* از تماس با درگاه است — جلویش را
بگیرد.

⚠️ **مینی‌اپ بدون `initData` باز نمی‌شود.** برای دیدن کامپوننت‌ها یک صفحه‌ی
موقت در `src/app/app/zzpreview/page.tsx` بساز که کامپوننت را مستقیم رندر کند
— و **پیش از کامیت پاکش کن**. (پوشه‌ی با `_` در Next از مسیریابی خارج است، پس
اسمش نباید با `_` شروع شود.)

متغیرها: `DATABASE_URL`, `SESSION_SECRET` (اجباری), `ADMIN_SECRET`,
`ADMIN_PASSWORD`, `PGSSL`, `SETTLE_KEY`, `ZOVIX_*`, `TG_BOT_TOKEN`,
`TG_BOT_USERNAME`, `TG_WEBHOOK_SECRET`, `TG_ADMIN_IDS`, `SITE_URL`,
`GEMINI_API_KEY`, `GEMINI_MODEL` (اختیاری), `MYFXBOOK_*`,
`NEXT_PUBLIC_DEV_FIXTURES`.

### کوئری‌های سلامت (باید همیشه صفر برگردانند)

```sql
-- موجودی در برابر دفترکل
SELECT p.id, p.usdt_balance, COALESCE(SUM(w.amount),0) AS ledger
FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
GROUP BY p.id, p.usdt_balance
HAVING p.usdt_balance <> COALESCE(SUM(w.amount),0);

-- استخر بازار در برابر مجموع پیش‌بینی‌ها  ← مهم‌ترین
SELECT m.id, m.yes_total, m.no_total,
       COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0) AS real_yes,
       COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0)  AS real_no
FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
GROUP BY m.id, m.yes_total, m.no_total
HAVING m.yes_total <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0)
    OR m.no_total  <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0);
```

---

## ۹. خطوط قرمز محصول (تصمیم قطعی مالک — تغییر نده)

- ادعای «سود تضمینی» هرگز. وعده‌ی «از پیش‌بینی پول دربیار» هرگز.
- ایردراپ و توکن نه. یوزر و لیدربورد فیک نه.
- **امتیازدهی صفر-انتظار حفظ شود:** پیش‌بینی تصادفی به‌طور میانگین منفی
  می‌گیرد. هر تغییری در فرمول امتیاز باید این را حفظ کند.
- استخر parimutuel تتری بازار ایران با تصمیم صریح مالک مجاز است؛ مکانیزم
  نقدی مشابه برای بازی‌های امتیازی ساخته نمی‌شود.
- ورود با گوگل و KYC هر دو رد شدند.
- **برداشت هرگز به دلیل بلاک‌بودن ربات قفل نمی‌شود.** مرز روی ورود پول است،
  نه خروج.
- واژگان شرط‌بندی برنگردد.
