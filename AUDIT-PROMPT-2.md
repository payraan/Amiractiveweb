# ممیزی دور دوم — مسیر پول، پس از بازنویسی

تو ممیز مستقلی. دور اول این ریپو ممیزی شد و نتیجه‌اش اعمال شد. از آن موقع
تا حالا **هسته‌ی مسیر پول بازنویسی شده** — ۷۷۲ خط در ۱۱ فایل، همه در یک روز،
و هیچ‌کس جز نویسنده‌اش ندیده. این ممیزی برای همان است.

پلتفرم پول واقعی کاربران را نگه می‌دارد و تا چند روز دیگر عمومی می‌شود.

**پاسخ به فارسی.**

---

## ۰. قاعده‌ی اصلی

**ادعای بدون شاهد قبول نیست.** هر یافته باید داشته باشد:

1. فایل و شماره‌ی خط
2. سناریوی مشخص با **عدد واقعی**: «کاربر A با موجودی X این کار را می‌کند،
   نتیجه Y می‌شود، در حالی که باید Z باشد»
3. کدام خط این را ممکن می‌کند

اگر مطمئن نیستی بنویس «مشکوک، تأیید نشده». **یک یافته‌ی قطعی از ده حدس
ارزشمندتر است.**

⚠️ دور اول یک یافته‌ی غلط داشت که «قطعی» علامت خورده بود: علت خطای hydration
به `Footer.tsx` و `new Date().getFullYear()` نسبت داده شده بود، در حالی که
سال روی سرور و کلاینت یکی است و اصلا mismatch نمی‌سازد. علت واقعی بعدا پیدا
شد (یک فایل موقت که سرور و کلاینت دو کامپوننت متفاوت رندر می‌کردند). **حدس
را قطعی علامت نزن.**

---

## ۱. دامنه‌ی این ممیزی

فقط دیف `324ba07..HEAD` روی این فایل‌ها. بقیه‌ی ریپو خارج از دامنه است.

```
src/lib/iran.ts                          +302   ← مهم‌ترین
src/lib/withdrawal-sync.ts               +200   ← کاملا تازه
src/lib/deposit-address.ts                +81   ← کاملا تازه
src/app/api/admin/withdrawals/route.ts    +90   ← کاملا تازه
src/app/api/admin/revenue/route.ts        +80
src/app/api/ir/bet/route.ts               +19
src/app/api/wallet/route.ts               +20
src/app/api/admin/topup/route.ts          +20
src/lib/db.ts                             +20
src/lib/withdrawal.ts                      +9
src/app/api/wallet/buy-credits/route.ts    +3
```

---

## ۲. قاعده‌ی تازه‌ای که باید بشکنی

پول دمو (بونوس) از پول واقعی جدا شد. قاعده‌ی مصوب مالک:

- بونوس در ستون `players.demo_balance` است، پول واقعی در `usdt_balance`.
- **خرج همیشه اول از دمو.** روی همه‌چیز: شرط، خرید MOON، کارمزد ساخت بازار.
- **برگشتِ اصلِ پول به دمو، و هر چه بالاتر از اصل بود سود است و واقعی
  می‌شود.** کاربری که با بونوس ۱۰۰ برد و ۱۳۷ گرفت، ۱۰۰ دمو می‌ماند و ۳۷
  واقعی و قابل برداشت.
- **برداشت فقط `usdt_balance` را می‌بیند.** پرچم `realOnly` در `moveFunds`.
- `ir_bets.demo_stake` سهم دموی اصلِ هر شرط را نگه می‌دارد.
- `platform_revenue.demo_amount` سهم دموی هر درآمد.

### کاری که ازت می‌خواهم

**این حساب را با عدد دنبال کن، نه با نگاه.** چند سناریو را دستی حساب کن و
با کد تطبیق بده. مخصوصا این‌ها:

1. کاربری که هم دمو دارد هم واقعی (مثلا ۴۰ دمو + ۶۰ واقعی) و ۸۰ شرط
   می‌بندد. `demo_stake` چقدر ثبت می‌شود؟ اگر ببرد و ۱۲۰ بگیرد، تقسیمش چه
   می‌شود؟ آیا مجموع دمو + واقعی درست می‌ماند؟
2. **ضریب زیر ۱.** بازار خیلی نامتوازن؛ برنده کمتر از اصلش می‌گیرد. کد
   `Math.min(demo_stake, amt)` می‌زند. آیا این درست است یا کاربر بی‌سروصدا
   دمو به واقعی تبدیل می‌کند (یا برعکس)؟
3. **بازار باطل** (`MIN_ODDS`) و **بازار بدون برنده**: در دومی کمیسیون کسر
   می‌شود و `backDemo = demo_stake × (1 − COMMISSION)`. آیا سهم دمویی که به
   کمیسیون می‌رود درست حساب شده؟
4. **گرد کردن.** `round6` و `NUMERIC(18,6)`. آیا جایی هست که مجموع
   پرداختی‌ها از استخر بیشتر شود، یا سهم دمو از اصل بیشتر شود؟ با اعداد
   بدقلق امتحان کن (مثلا استخر ۱۰۰ با ۳ برنده‌ی نامساوی).
5. آیا مسیری هست که **پول دمو به واقعیِ قابل‌برداشت تبدیل شود** بدون اینکه
   واقعا سودی ساخته شده باشد؟ این مهم‌ترین سؤال این ممیزی است.

---

## ۳. جاهای دیگری که می‌خواهم دقیق نگاه کنی

1. 🔴 **`withdrawal-sync.ts`** — کاملا تازه. `refundWithdrawal` ادعای مالکیت
   را با خودِ `UPDATE ... WHERE status IN ('submitted','stuck')` می‌گیرد.
   آیا مسیری هست که دو بار پول برگردد؟ آیا `stuck` می‌تواند به اشتباه روی
   برداشتی بنشیند که درگاه واقعا انجامش داده؟
2. 🔴 **مهاجرت یک‌باره در `ensureIrTables`** — موجودی‌ها را به دمو منتقل
   می‌کند اگر هیچ واریز درگاهی ثبت نشده باشد. آیا حالتی هست که این روی
   دیتابیسی با پول واقعی اجرا شود؟ آیا اجرای هم‌زمانِ دو نمونه‌ی سرور
   مشکل‌ساز است؟
3. 🟡 **`deposit-address.ts`** — آدرس واریز در دیتابیس ذخیره می‌شود. اگر
   درگاه روزی برای همان `client_id` آدرس **متفاوتی** بدهد چه می‌شود؟ (سه
   فراخوان پشت‌سرهم تست شد و یکی بود، ولی این تضمین نیست.)
4. 🟡 **`recordRevenue`** — حالا `demoAmount` می‌گیرد. آیا فراخوانی هست که
   یادش رفته باشد و در نتیجه درآمد دمو «واقعی» ثبت شود؟ (دقیقا همان باگی که
   دور اول پیدا شد، فقط با شکل تازه.)
5. 🟡 **قفل خودکار بازار** در `settleDueIrMarkets` — `UPDATE ... SET
   status='locked' WHERE status='open' AND closes_at < now()`. آیا با
   مسیرهای دیگر (اعتراض، تسویه‌ی دستی ادمین) تداخل دارد؟

---

## ۴. آنچه قبلا تست شده — دوباره کشف نکن

با داده‌ی واقعی روی Postgres محلی، سبز:

- چرخه‌ی کامل بونوس: ۱۰۰ دمو → شرط ۶۰ → برد → پرداختی ۹۷ = ۶۰ دمو + ۳۷
  واقعی. برداشت ۱۳۷ و ۳۸ رد شد؛ مرز دقیقا روی سود واقعی.
- ۵۰۰ شرط هم‌زمان از ۵۰۰ کاربر روی یک بازار: همه موفق، استخر دقیقا
  ۱۲۵۰/۱۲۵۰، صفر ناهماهنگی.
- ۱۰ فراخوان هم‌زمان `refundWithdrawal`: دقیقا یکی موفق، یک ردیف دفترکل.
- ۸ کلیک هم‌زمان ادمین روی همان برداشت: دقیقا یکی موفق.
- مهاجرت: پس از ثبت اولین واریز واقعی، کاملا خاموش می‌ماند.
- پنل درآمد با سه ردیف (کاملا واقعی، کاملا دمو، مخلوط): واقعی=۱۵۰
  دمو=۸۰ همه=۲۳۰.
- شش کوئری سلامت روی **دیتابیس پروداکشن**: همه صفر.

---

## ۵. کوئری‌های سلامت فعلی

```sql
-- ۱. موجودی (واقعی + دمو) = جمع دفترکل
SELECT count(*) FROM (
  SELECT p.id FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
  GROUP BY p.id, p.usdt_balance, p.demo_balance
  HAVING p.usdt_balance + p.demo_balance <> COALESCE(SUM(w.amount),0)) x;

-- ۲. موجودی دمو = جمع ستون demo دفترکل
SELECT count(*) FROM (
  SELECT p.id FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
  GROUP BY p.id, p.demo_balance
  HAVING p.demo_balance <> COALESCE(SUM(w.demo),0)) x;

-- ۳. استخر بازار = مجموع شرط‌ها
SELECT count(*) FROM (
  SELECT m.id FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
  GROUP BY m.id, m.yes_total, m.no_total
  HAVING m.yes_total <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0)
      OR m.no_total  <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0)) x;

-- ۴. سهم دمو هرگز از اصل بیشتر نباشد
SELECT count(*) FROM ir_bets WHERE demo_stake > stake;
SELECT count(*) FROM platform_revenue WHERE abs(demo_amount) > abs(amount);

-- ۵. هیچ موجودی منفی نباشد
SELECT count(*) FROM players WHERE usdt_balance < 0 OR demo_balance < 0;
```

**اگر کوئری سلامتی به ذهنت می‌رسد که این‌ها نمی‌گیرندش، بنویسش —
ارزشمندترین چیزی است که می‌توانی بدهی.** مخصوصا کوئری‌ای که بگوید «پول دمو
بی‌اجازه واقعی شده».

---

## ۶. چیزهایی که عمدی‌اند — یافته نیستند

- استخر parimutuel تتری با تصمیم صریح مالک.
- شارژ دستی ادمین همیشه دمو است.
- پول دمو می‌تواند در استخر نقدینگی برود؛ اگر کاربر واقعی ببرد، پلتفرم تتر
  واقعی می‌پردازد. این یارانه‌ی بازاریابیِ مصوب است، نه باگ — ولی اگر جایی
  حسابداری‌اش را پنهان می‌کند، آن یافته است.
- سقف ~۹ شرط در ثانیه روی هر بازار (اندازه‌گیری شده، از قفل ردیف بازار).
  بازارهای مختلف رقابت ندارند. اگر راه کم‌کردن رفت‌وبرگشت‌های داخل ترنزاکشن
  را می‌بینی بگو، ولی خودِ عدد یافته نیست.
- نبود ORM، نبود فریم‌ورک مهاجرت، نبود تست خودکار — وضع فعلی.
- baseline لینت ۳۲ مورد.

---

## ۷. خروجی

```
## 🔴 بحرانی
### ۱. <عنوان>
فایل: src/lib/xxx.ts:۱۲۳
سناریو: کاربر با ۴۰ دمو و ۶۰ واقعی ... نتیجه: ... باید: ...
چرا: خط ۱۲۳ ...
اطمینان: قطعی | مشکوک
پیشنهاد: ...

## 🟡 متوسط
## 🟢 کیفیت
## بررسی‌شده و سالم
<فهرست چیزهایی که نگاه کردی و سالم بودند — تا پوشش معلوم باشد>
```

بخش آخر اجباری است. اگر جایی را نگاه نکردی، صریح بگو.

اگر برای رسیدن به نتیجه‌ی قطعی لازم است کوئری یا کدی اجرا شود، **دقیقا
بنویس چه چیزی** را اجرا کنم و نتیجه‌اش را برایت می‌فرستم.
