# سند تحویل نارمون — فاز ربات و مینی‌اپ تلگرام

> این سند را به‌عنوان اولین پیام چت جدید بفرست. هدفش این است که چت جدید بدون
> خواندن کل تاریخچه، دقیقاً بداند پلتفرم چیست، چه چیزی ساخته شده، اعداد اقتصادی
> واقعی کدام‌اند، و قدم بعدی چیست.
>
> **منبع حقیقت نهایی، خودِ ریپو است.** هر جا این سند با کد تناقض داشت، کد را
> ملاک بگیر و تناقض را به مالک گزارش بده.

---

## ۰. نقش تو و سبک کار

تو Principal Engineer این پروژه‌ای. مالک پروژه برنامه‌نویس نیست و در سطح
کپی‌پیست و اجرای دستور ترمینال کار می‌کند.

قواعد همکاری که در طول پروژه جا افتاده و باید ادامه پیدا کند:

- **پاسخ‌ها به فارسی.**
- **قبل از هر کامیت:** `npx tsc --noEmit`؛ در تغییرات بزرگ `npm run build`.
- **کد را با داده‌ی واقعی تست کن، نه فقط کامپایل.** روال ثابت این پروژه این
  بوده که هر مسیر پولی با curl و psql روی دیتابیس محلی سرتاسر تست شود
  (سناریوی موفق + سناریوهای سوءاستفاده)، بعد کامیت.
- **داده‌ی تستی را بعد از تست از دیتابیس پاک کن.**
- **پوش فقط بعد از تأیید مالک.** کامیت کوچک با پیام مشخص.
- هر تغییر را قبل از اعمال با یکی‌دو جمله‌ی ساده‌ی فارسی توضیح بده (چه و چرا).
- هیچ کلید/توکنی هاردکد نشود؛ `.env.local` در گیت‌ایگنور بماند.
- اگر چیزی در کدبیس با این سند نمی‌خواند، **اول گزارش بده**، خودسرانه بازنویسی نکن.

---

## ۱. پلتفرم چیست

**نارمون (Narmoon)** — پلتفرم فارسی‌زبان «هوش جمعی و بازارهای پیش‌بینی».

- ریپو: `https://github.com/payraan/Amiractiveweb` (برنچ `main`)
- دیپلوی: Railway — `https://amiractiveweb-production.up.railway.app`
  (دامنه‌ی اصلی هنوز وصل نشده)
- استک: **Next.js 16 App Router، React 19، Tailwind v4، TypeScript، PostgreSQL
  با `pg` خام (بدون ORM)**. مسیر `@/*` → `src/*`.
- زبان: فارسی، RTL (`lang="fa" dir="rtl"`). کامنت‌های کد هم فارسی‌اند.
- بدون framer-motion و three.js (عمداً رد شده‌اند: حجم باندل، SEO، مخاطب ایران).

**شعار فعلی روی هیرو:**
> هوش جمعی، اندازه‌گیری شد.
> یک نفر یک نظر دارد؛ هزاران نفر یک سیگنال می‌سازند.

**تز محصول:** امتیاز فقط از مهارت می‌آید، نه شانس و نه پول. امتیاز خریدنی نیست.

**واحد داخلی: `MOON`** (قبلاً «کردیت» بود و نماد ◆ داشت — هر دو حذف شده‌اند).
MOON فقط قابلیت باز می‌کند و هرگز امتیاز یا رتبه نمی‌خرد.

مالک در استانبول است و مخاطب اصلی داخل ایران — این دوگانه روی همه‌ی تصمیم‌های
حقوقی، فیلترینگ و geo اثر دارد.

---

## ۲. چهار محصول و اولویتشان

اولویت راهبردی مالک: **بازار ایران ۶۰٪ · بقیه ۴۰٪**.

| # | محصول | مسیر | اقتصاد |
|---|---|---|---|
| ۱ | **بازار ایران** | `/iran` | **تتر واقعی** — parimutuel |
| ۲ | **ترید** (بازار خارجی) | `/trade` | امتیاز — دیتای زنده‌ی پالی‌مارکت |
| ۳ | **نبض بازار** | `/predict` | امتیاز — حدس قیمت روی ۴۲ دارایی |
| ۴ | **چلنج پراپ** | `/challenge` | ورودی MOON، جایزه‌ی حساب واقعی |

کمبو (`/combos` → ریدایرکت به تب کمبو در `/trade`) زیرمجموعه‌ی ترید است.

⚠️ **«آرنای پیش‌بینی» دیگر وجود ندارد** — به «ترید» تغییر نام داده. اگر جایی
این نام را دیدی، باقی‌مانده‌ی قدیمی است.

---

## ۳. اقتصاد واقعی — اعداد قفل‌شده

اینها از `src/lib/iran.ts`، `src/lib/game.ts`، `src/lib/poly.ts` و
`src/lib/challenge.ts` استخراج شده‌اند. **قبل از تغییر هرکدام، از مالک بپرس.**

### بازار ایران (تنها بخش با پول واقعی)

```
COMMISSION        = 0.03    // ۳٪ از حجم استخر، نه از سود
MIN_STAKE_USDT    = 1       // حداقل شرط
MIN_ODDS          = 1.05    // زیر این، بازار باطل می‌شود
DISPUTE_HOURS     = 24      // پنجره‌ی اعتراض پس از ثبت نتیجه
PROPOSE_FEE_USDT  = 1       // هزینه‌ی ساخت بازار (اگر رد شود کامل برمی‌گردد)
MIN_WITHDRAW      = 10      // حداقل برداشت (کارمزد شبکه)
```

**فرمول:** `ضریب برد = (کل استخر × ۰.۹۷) ÷ مجموع شرط‌های طرف برنده`

**سه مسیر تسویه** (در `settleIrMarket`):
1. **عادی** — برنده‌ها به نسبت سهم پرداخت می‌گیرند؛ کمیسیون درآمد پلتفرم.
2. **باطل به‌خاطر ضریب پایین** (`wouldBeVoid`) — برگشت **کامل بدون کمیسیون**.
3. **بدون برنده** (هیچ‌کس روی گزینه‌ی برنده شرط نبسته) — برگشت **با کسر کمیسیون**.

> مورد ۳ یک باگ واقعی بود که رفع شد: قبلاً کل استخر بی‌سروصدا نصیب پلتفرم می‌شد.

### امتیازی

```
WELCOME_CREDITS    = 10     // MOON هدیه‌ی ثبت‌نام
POLY_FREE_PER_DAY  = 5      // پیش‌بینی رایگان روزانه در ترید
POLY_EXTRA_COST    = 1      // MOON برای هر پیش‌بینی اضافه
REF_VOL_PCT        = 2.0    // پورسانت رفرال
```

امتیاز ترید صفر-انتظار است: `برد = +(۱۰۰ − احتمال٪)`، `باخت = −احتمال٪`.
امتیاز نبض بازار با جدول کالیبره‌شده — حدس کور به‌طور میانگین منفی می‌گیرد.
**این خاصیت را هنگام دست‌زدن به امتیازدهی حفظ کن.**

### بسته‌های MOON

هشت بسته در `CREDIT_PACKS` (`src/lib/game.ts`)، از ۲۰ MOON / ۲.۵ تتر تا
۳۰۰۰ MOON / ۱۲۵ تتر. **خرید آنی از موجودی کیف پول** انجام می‌شود
(`/api/wallet/buy-credits`) — دیگر از تلگرام و دستی نیست.

### چلنج پراپ

```
CONSISTENCY_PCT     = 35     // سهم بهترین روز از کل سود
ELIGIBLE_PROB_MIN   = 0.25   // فقط پیش‌بینی در این بازه‌ی احتمال شمرده می‌شود
ELIGIBLE_PROB_MAX   = 0.75
MAX_COUNTED_PREDS   = 50
MAX_ENTRIES_PER_30D = 3
```

هفت تیر در `CHALLENGES`: `fx250/fx500/fx1k` (حساب معاملاتی) و
`pr5k/pr10k/pr25k/pr50k` (حساب پیش‌بینی).

---

## ۴. معماری

### دیتابیس و مهاجرت

**هیچ فریم‌ورک مهاجرتی وجود ندارد.** `src/lib/db.ts` یک pool سراسری دارد و
`db()` یک بلوک SQL idempotent را یک‌بار در هر cold start اجرا می‌کند
(`CREATE TABLE IF NOT EXISTS` + `ALTER ... ADD COLUMN IF NOT EXISTS`).

ماژول‌ها جدول‌های خودشان را همین‌طور می‌سازند:
- `ensureIrTables()` در `iran.ts` — کیف پول، بازار ایران، اعتراض، درآمد، دمو
- `ensureTelegramTables()` در `telegram.ts` — ستون‌های تلگرام و کدهای اتصال
- `ensureChallengeTables()` در `challenge.ts`

**هر تغییر اسکیمای جدید باید SQL idempotent باشد و به همین بلوک‌ها اضافه شود.**

SSL از روی هاست `DATABASE_URL` تشخیص داده می‌شود (لوکال و `.internal` بدون SSL)؛
`PGSSL=off` فرار اضطراری است.

### جدول‌های موجود

```
players            بازیکنان (شامل usdt_balance, credits, tg_user_id, is_demo, showcase)
wallet_ledger      دفترکل تغییرناپذیر کیف پول — مبنای حسابرسی
ir_markets         بازارهای ایران
ir_bets            شرط‌های تتری
ir_disputes        اعتراض به نتیجه
platform_revenue   دفترکل درآمد پلتفرم (با پرچم is_demo)
predictions        نبض بازار
rounds             راندهای نبض بازار
poly_predictions   ترید (پالی‌مارکت)
combo_tickets      تیکت کمبو
combo_legs         پاهای کمبو
player_challenges  چلنج پراپ
credit_topups      شارژ دستی MOON
referral_earnings  پورسانت رفرال
tg_link_codes      کد یک‌بارمصرف اتصال تلگرام
```

ستون‌های کلیدی `players`:
`id, tg_username, display_name, password_hash, total_points, streak, last_played,
created_at, credits, usdt_balance, referral_code, referred_by, tg_user_id,
tg_linked_at, group_bonus_at, is_demo, showcase`

ستون‌های `ir_markets`:
`id, creator_id, question, category, source_note, closes_at, status, outcome,
yes_total, no_total, bettors, settled_at, void_reason, created_at, fee_usdt`

### چرخه‌ی عمر بازار ایران

```
pending → open → locked → settling → settled
   ↓                          ↓
 void (رد پیشنهاد)          void (ضریب پایین یا بدون برنده)
```

- **pending** — ساخته شده، ۱ تتر گرفته شده، منتظر تأیید انسانی
- **open** — منتشر، شرط‌پذیر
- **locked** — بسته، منتظر نتیجه
- **settling** — نتیجه ثبت شده، پنجره‌ی ۲۴ ساعته‌ی اعتراض
- **settled** — پرداخت شد، برگشت‌ناپذیر
- **void** — باطل، پول برگشت

### احراز هویت (fail-closed)

- **نشست بازیکن:** کوکی HMAC `playerId.expiry.sig` به نام `amir_session`
  (`src/lib/session.ts`). `SESSION_SECRET` اجباری است؛ بدون آن همه‌ی نشست‌ها رد
  می‌شوند. **هرگز fallback نگذار** — این یک باگ واقعی بود (دنباله‌ی
  `DATABASE_URL` کم‌آنتروپی بود و نشست جعل‌شدنی).
- **رمز:** scrypt (`src/lib/auth.ts`). یوزرنیم = هندل تلگرام، نرمال‌شده lowercase بدون `@`.
- **ادمین:** کوکی ۱۲ ساعته‌ی جدا `amir_admin` با `ADMIN_SECRET` (fallback به
  `SESSION_SECRET`)، آن هم fail-closed.
- **ربات:** روت‌های `/api/bot/*` با هدر `BOT_API_KEY`.

### الگوی روت API

همه زیر `src/app/api/*/route.ts`، همه با همین شکل:
`export const dynamic = "force-dynamic"` → `await cookies()` → `verifySession()` →
۴۰۱ → اعتبارسنجی بدنه → ۴۰۰ → `NextResponse.json({ ok, ... })` با کد خطای
snake_case کوتاه (`not_authed`, `bad_asset`, `insufficient_funds`).

### قواعد پولی

- کسر پول همیشه داخل ترنزاکشن با `SELECT … FOR UPDATE` روی ردیف بازیکن.
- `moveFunds(client, playerId, amount, kind, ref)` تنها راه تغییر موجودی است —
  موجودی و دفترکل را با هم می‌نویسد و اگر منفی شود throw می‌کند.
- `recordRevenue(client, kind, amount, opts)` هر برداشت پلتفرم را ثبت می‌کند و
  `is_demo` را **خودکار** از روی حساب پرداخت‌کننده می‌خواند.

### دیتای بیرونی همیشه سمت سرور پروکسی می‌شود

کندل‌های Yahoo (`candles.ts`)، پالی‌مارکت (`poly.ts`) و Myfxbook (`myfxbook.ts`)
را سرور با کش TTL درون‌حافظه‌ای می‌گیرد. **این عمدی است: بازدیدکننده‌ی ایرانی
نباید مستقیم به این‌ها بخورد.** هرگز به کلاینت منتقلشان نکن.

### کیف پول

درگاه **Zovix** (`src/lib/zovix.ts`) — درخواست‌های form-urlencoded با امضای
HMAC-SHA256. **رشته‌ی امضاشده باید بایت‌به‌بایت همان بدنه‌ی ارسالی باشد، پس
هرگز به JSON تبدیلش نکن.**

وبهوک واریز (`/api/wallet/webhook`) سه لایه محافظت دارد:
1. توکن مخفی در آدرس (`?t=…`) — این توکن را **خودمان می‌سازیم**، از Zovix نمی‌آید
2. تأیید متقابل: قبل از هر شارژ، خودمان از API درگاه می‌پرسیم txid واقعی است
3. ایندکس یکتا روی txid — شارژ تکراری ممکن نیست

### تفکیک پول دمو از واقعی

قاعده‌ی قطعی: **پول واقعی فقط از وبهوک درگاه می‌آید. هر شارژ دستی ادمین یعنی
حساب تستی.** حساب `is_demo = true` می‌خورد و درآمدش هم همان پرچم را می‌گیرد.
دفترکل درآمد سه حالت دارد و **پیش‌فرضش «واقعی» است**.

---

## ۵. چه چیزی ساخته شده (وضعیت فعلی ≈ ۹۵٪)

### صفحه‌ها
`/` `/trade` `/predict` `/iran` `/iran/propose` `/iran/m/[id]` `/wallet`
`/profile` `/challenge` `/leaderboard` `/legal` `/login` `/referral` `/results`
`/bot` `/broker` `/admin` `/arena` `/arena/m/[id]` `/combos`

### قابلیت‌های کامل‌شده
- بازار ایران: ترمینال سه‌ستونی، اکسپلور با ۵ مرتب‌سازی، ساخت بازار با
  **تقویم شمسی** (`src/lib/jalali.ts` — مبدل خودنوشته، روی ۱۲٬۰۰۰ روز راستی‌آزمایی شده)
- **اعتراض واقعی:** فقط شرکت‌کننده‌های همان بازار، فقط در پنجره‌ی ۲۴ ساعته،
  یک بار برای هر نفر — و **تا اعتراض باز هست تسویه قفل است**
- کیف پول: واریز/برداشت تتر، خرید آنی MOON، دفترکل کامل
- پنل کاربری `/profile`: سود و زیان تفکیک‌شده (تتری جدا از امتیازی)، نمودار رشد
  موجودی، **۲۳ نشان در ۴ سطح** با انتخاب حداکثر ۳ تا برای نمایش کنار نام
- پنل چلنج: جدول شش شرط قبولی با وضعیت زنده، کارنامه‌ی برد/باخت، نمودار روزانه
- پنل ادمین: شارژ MOON/تتر، آمار، بازار ایران با صف اعتراض، **دفترکل درآمد با
  تفکیک دمو/واقعی و نمودار**، جدول کاربران با نمودار رشد
- اشتراک‌گذاری بازار: `/iran/m/[id]` + تصویر پیش‌نمایش OG

### نکته‌ی فنی مهم درباره‌ی تصویر OG
موتور Satori نه woff2 می‌خواند، نه فونت متغیر، نه شکل‌دهی فارسیِ Estedad را
پشتیبانی می‌کند. راه‌حل: `src/fonts/vazirmatn-700.ttf` (نمونه‌ی ثابت). ضمناً
Satori نیم‌فاصله را مرز کلمه می‌گیرد و دو نیمه را جابه‌جا می‌کند، پس در تصویر
نیم‌فاصله حذف می‌شود.

### مشکلات شناخته‌شده‌ی باز
1. **⚠️ باگ احراز تلگرام** — بخش ۷ (اولویت اول همین فاز)
2. **کرون تسویه ست نشده** — `/api/predict/settle` با `SETTLE_KEY` محافظت می‌شود
   ولی تنبل است و فقط با باز شدن صفحه اجرا می‌شود. **قبل از ورود کاربر واقعی
   باید کرون واقعی ست شود.**
3. **بازار ایران نمودار زمانی ندارد** — تاریخچه‌ی احتمال ذخیره نمی‌شود. برای
   «موج پیش‌بینی» و «شاخص اطمینان جمعی» باید اسنپ‌شات دوره‌ای ذخیره شود.
4. **rate limit فقط روی login/auth/admin** است، نه مسیرهای پولی؛ و درون‌حافظه‌ای
   است (با هر دیپلوی ریست می‌شود).
5. **برداشت گیرکرده** بازیابی خودکار ندارد (`getWithdrawal` نوشته شده ولی صدا زده نمی‌شود).
6. آدرس برداشت فقط از نظر طول اعتبارسنجی می‌شود، نه فرمت TRON.
7. `~۲۰` خطای lint از قبل موجود است (setState in effect و…) — مربوط به این فاز نیست.

### متغیرهای محیطی

```
DATABASE_URL           (اجباری)
SESSION_SECRET         (اجباری — بدون آن هیچ‌کس وارد نمی‌شود)
ADMIN_SECRET, ADMIN_PASSWORD, PGSSL, SETTLE_KEY
ZOVIX_BASE_URL / ZOVIX_API_KEY / ZOVIX_API_SECRET
ZOVIX_USDT_NETWORK / ZOVIX_WEBHOOK_TOKEN
BOT_TOKEN / BOT_USERNAME / BOT_API_KEY / GROUP_ID / SITE_URL   ← هنوز ست نشده
MYFXBOOK_ACCOUNT_ID
```

---

## ۶. ⚠️ تناقض‌های دو سند پیشنهادی با پلتفرم واقعی

مالک دو سند از ایجنت‌های دیگر گرفته. **مفاهیمشان خوب است ولی اعداد و
اسکیمایشان با واقعیت نمی‌خواند.** اینها را قبل از هر کاری بخوان:

| موضوع | سند پیشنهادی | **واقعیت پلتفرم** |
|---|---|---|
| تقسیم کارمزد | ۹۶٪ استخر / ۲.۵٪ پلتفرم / ۱٪ سازنده / ۰.۵٪ رفرال | **۹۷٪ استخر / ۳٪ پلتفرم. سهم سازنده و رفرال روی بازار ایران وجود ندارد** |
| نام جدول بازار | `markets` | **`ir_markets`** |
| نام جدول شرط | `bets` | **`ir_bets`** |
| نام جدول اعتراض | `market_disputes` | **`ir_disputes`** |
| گزینه‌ها | `option_a_label` / `option_b_label` (چندگزینه‌ای) | **دوگزینه‌ای ثابت: `yes` / `no` با ستون‌های `yes_total` / `no_total`** |
| ستون‌های کارمزد | `commission_bps`, `creator_fee_bps`, `referral_bps` | **وجود ندارند — کارمزد ثابت `COMMISSION` در کد** |
| حداقل مشارکت | «۲ نفر یا بیشتر» | **حذف شده — عمداً، برای شروع سرد پلتفرم** |
| پنجره‌ی اعتراض | «۱۲ تا ۲۴ ساعت» | **دقیقاً ۲۴ ساعت (`DISPUTE_HOURS`)** |
| `trust_score`, `creator_enabled`, `bond_amount` | پیشنهاد شده | **وجود ندارند** |
| Redis | توصیه شده | **وجود ندارد؛ اضافه نکن مگر مالک تأیید کند** |
| دسته‌ها | ورزش، طلا/ارز، کریپتو، سینما، خبری | **`IR_CATEGORIES` در `src/lib/ir-categories.ts`: اقتصاد، ورزش، کریپتو، اجتماعی، سایر** |

**دستور کار:** مفهوم و UX هر دو سند را بگیر، ولی **اقتصاد و اسکیما را از
پلتفرم واقعی بردار**. اگر مالک واقعاً سهم سازنده یا رفرال می‌خواهد، این یک
**تغییر اقتصادی است و باید جداگانه تصمیم‌گیری و در دفترکل درآمد پیاده شود** —
نه اینکه در ساخت ربات قاچاقی وارد شود.

نکاتی از سندها که **درست‌اند و باید رعایت شوند**:
- تلگرام فقط لایه‌ی توزیع است، نه سیستم موازی. یک Source of Truth.
- مینی‌اپ رابط اصلی است؛ ربات لایه‌ی دستور، اعلان و deep link.
- اعتبارسنجی `initData` با HMAC و رد کردن timestamp کهنه — هرگز اعتماد کور.
- Deep link با attribution دقیق (`market_`, `ref_`, `channel_`, `creator_`).
- هویت بصری مینی‌اپ باید عیناً سایت باشد.

---

## ۷. ⚠️ باگ امنیتی که باید در همین فاز بسته شود

**همین الان ثبت‌نام فقط یوزرنیم + رمز است و هیچ اعتبارسنجی واقعی تلگرام ندارد.**
هر کسی می‌تواند با هر آیدی دلخواه — حتی آیدی شخص دیگر — ثبت‌نام کند.

چرا مهم است: ضدتقلبِ همه‌ی مکانیزم‌های پاداش (رفرال، چلنج، لیدربورد، سهم
سازنده) به «یک نفر = یک حساب» گره خورده، و تنها لنگر واقعی این ادعا هویت
تلگرام است.

**زیرساختی که از قبل هست و باید استفاده شود:**
- ستون‌های `players.tg_user_id` (یکتا)، `tg_linked_at`، `group_bonus_at`
- جدول `tg_link_codes` — کد یک‌بارمصرف ۱۵ دقیقه‌ای
- `src/lib/telegram.ts`: `createLinkCode`, `consumeLinkCode`, `grantGroupBonus`,
  `getTgStatus`, `sendTelegram`, `notifyPlayer`, `botKeyValid`
- روت‌های `/api/bot/link` و `/api/bot/group` با احراز `BOT_API_KEY`
- کامپوننت `TelegramConnect` سمت کاربر
- لینک عمیق `?start=link_<code>`
- پاداش عضویت گروه: `GROUP_BONUS_CREDITS = 20`

**همه‌ی این‌ها ساخته شده ولی غیرفعال است، چون
`BOT_TOKEN/BOT_USERNAME/BOT_API_KEY/GROUP_ID/SITE_URL` در Railway ست نشده‌اند.**

آنچه باید تصمیم‌گیری و ساخته شود:
1. آیا اتصال تلگرام **اجباری** می‌شود یا اختیاری با محدودیت؟ (پیشنهاد: برای
   برداشت، رفرال، چلنج و ساخت بازار اجباری؛ برای بازی رایگان اختیاری)
2. تکلیف حساب‌های موجودِ بدون تلگرام چه می‌شود؟
3. مسیر ورود از مینی‌اپ باید **بدون رمز** باشد: `initData` معتبر = هویت اثبات‌شده.
   این هم‌زمان باگ را می‌بندد و هم ثبت‌نام را یک‌کلیکی می‌کند.

---

## ۸. کاری که در چت جدید باید انجام شود

**هدف: مینی‌اپ تلگرام = نسخه‌ی تلگرامی بازار ایران.** نه یک محصول جدید.

### اصول غیرقابل‌مذاکره
1. **یک دیتابیس، یک حساب، یک کیف پول، یک امتیاز.** بازاری که در مینی‌اپ ساخته
   می‌شود باید در `/iran` سایت هم لیست شود و برعکس.
2. **هیچ منطق پولی موازی نساز.** از `moveFunds`، `recordRevenue`،
   `settleIrMarket` و جدول‌های موجود استفاده کن.
3. **اعداد اقتصادی همان‌هاست** — بخش ۳ این سند.
4. تم و رنگ‌بندی دقیقاً همان سایت (طلایی/مشکی، فونت استعداد و وزیرمتن).
5. RTL، فارسی، موبایل-فرست، حس اپلیکیشن نه صفحه‌ی وب.

### ترتیب پیشنهادی کار
1. **ممیزی ریپو** و گزارش معماری — قبل از یک خط کد.
2. **ست‌کردن env تلگرام در Railway** و فعال‌کردن زیرساخت موجود (مالک باید انجام دهد).
3. **احراز هویت مینی‌اپ** با `initData` + بستن باگ بخش ۷.
4. **ربات:** `/start`، deep link، اعلان تسویه و اعتراض.
5. **مینی‌اپ:** فهرست بازارها → صفحه‌ی بازار → ثبت پیش‌بینی → کیف پول → پروفایل.
6. **ساخت بازار از مینی‌اپ** با همان قواعد (۱ تتر، منبع اجباری، تأیید انسانی).
7. **اشتراک‌گذاری در کانال/گروه** — پیام با inline keyboard، نه poll بومی.
   (poll بومی هویت رأی‌دهنده را نمی‌دهد؛ `callback_query` آیدی عددی می‌دهد.)

### خطوط قرمز محصول (تصمیم قطعی مالک — تغییر نده)
- ادعای «سود تضمینی» هرگز.
- وعده‌ی «از پیش‌بینی پول دربیار» در پیام برند هرگز.
- ایردراپ و توکن نه. یوزر و لیدربورد فیک نه.
- صفحه‌ی شفافیت با استدلال عددی («حدس تصادفی به‌طور میانگین امتیاز منفی
  می‌گیرد») حفظ شود.
- استخر parimutuel تتری بازار ایران **با تصمیم صریح مالک** ساخته شده و مجاز
  است؛ ولی مکانیزم نقدی مشابه برای بازی‌های امتیازی ساخته نمی‌شود.

---

## ۹. کارهای اولویت دوم (بعد از تلگرام)

- کرون واقعی تسویه (مورد ۲ بخش ۵)
- ذخیره‌ی تاریخچه‌ی احتمال بازارهای ایران → نمودار زمانی، «موج پیش‌بینی»،
  «شاخص اطمینان جمعی»
- rate limit روی مسیرهای پولی + بازیابی برداشت گیرکرده + اعتبارسنجی آدرس TRON
- دامنه‌ی اصلی + Cloudflare + متمرکزسازی لینک‌های هاردکد در `src/config/site.ts`
- سهم سازنده‌ی بازار و بوست اکسپلور (نیازمند تصمیم اقتصادی مالک)
- آکادمی و ابزار تحلیل ژورنال

---

## ۱۰. دستورها

```bash
npm run dev      # لوکال روی :3000
npm run build    # بیلد production
npm run lint     # eslint
npx tsc --noEmit # تایپ‌چک — قبل از هر کامیت
```

تست suite وجود ندارد. `myfxbook-test.mjs` در ریشه یک probe دستی است.

دیتابیس لوکال: `postgresql://amiractive@localhost:5432/narmoon`
(رمز ادمین لوکال در `.env.local`).

---

**آخرین وضعیت:** همه‌چیز روی `main` پوش شده و روی Railway زنده است. آخرین
کامیت: `c47ebdc`. بیلد تمیز، `tsc` بدون خطا، هیچ صفحه‌ای روی موبایل سرریز افقی
ندارد.
