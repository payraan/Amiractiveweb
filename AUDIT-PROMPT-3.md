# ممیزی دور سوم — ربات تلگرام، احراز هویت، و پخش سراسری

تو ممیز مستقلی. دو دور قبل روی **مسیر پول** بودند و هر دو یافته‌ی واقعی
دادند. این دور روی سطحی است که هیچ‌کدام لمسش نکرده‌اند و اتفاقاً همان کانالی
است که لانچ از آن شروع می‌شود.

**پاسخ به فارسی.**

---

## ۰. قاعده

**ادعای بدون شاهد قبول نیست.** هر یافته: فایل و خط، سناریوی مشخص با ورودی
واقعی، و اینکه کدام خط آن را ممکن می‌کند. اگر مطمئن نیستی بنویس «مشکوک».

⚠️ هر دو دور قبل یک یافته‌ی غلط داشتند که «قطعی» علامت خورده بود:
- دور اول: علت خطای hydration را به `Footer.tsx` نسبت داد؛ سال روی سرور و
  کلاینت یکی است و اصلاً mismatch نمی‌سازد.
- دور دوم: پیشنهاد قفلِ درست بود، ولی **بن‌بست ABBA** را ندید — مسیر شرط
  قفل را بازیکن→بازار می‌گرفت و پیشنهادِ داده‌شده تسویه را بازار→بازیکن
  می‌کرد. اگر بدون بررسی اعمال می‌شد، تسویه‌ها به بن‌بست می‌خوردند.

**درسِ هر دو یکی است: راه‌حل را هم به‌اندازه‌ی مسئله بررسی کن.** اگر پیشنهاد
می‌دهی، بگو چه چیزی را ممکن است بشکند.

---

## ۱. دامنه

```
src/app/api/tg/webhook/route.ts   714   ← مهم‌ترین
src/lib/bot-menu.ts               610
src/lib/broadcast.ts              456
src/lib/bot-wallet.ts             262
src/lib/tg-auth.ts                188
src/app/api/tg/auth/route.ts       95
src/lib/bot-flow.ts                94
src/middleware.ts
```

مسیر پول (`iran.ts`، `withdrawal*.ts`، `zovix.ts`) در دو دور قبل ممیزی شده
و خارج از دامنه است — مگر جایی که ربات به آن وصل می‌شود.

---

## ۲. چرا این سطح مهم است

- **لانچ از تلگرام شروع می‌شود.** ربات ویترین اصلی است، نه سایت.
- **برداشت پول واقعی از داخل چت انجام می‌شود** — گفت‌وگوی سه‌مرحله‌ای
  (مبلغ → آدرس → تأیید) با حافظه‌ی حالت در `bot-flow.ts`.
- **پخش سراسری به ۵۰ هزار نفر** با صف و زنجیره‌ی خودکار.
- **`initData` تلگرام تنها درِ ورود به حساب است.** هر ضعفی آنجا یعنی
  دسترسی به حساب و کیف پول دیگران.
- وبهوک **عمداً از سقف نرخ معاف است** (۴۲۹ دادن به تلگرام یعنی گم‌شدن پیام)
  — پس هر مسیری که از داخل ربات به عملیات سنگین یا پولی می‌رسد، محافظ
  خودش را لازم دارد.

---

## ۳. سؤال‌های مشخصی که می‌خواهم جواب بدهی

### 🔴 احراز هویت
1. `verifyTelegramInitData` در `tg-auth.ts` — آیا راهی هست که با
   `initData` **بدون امضای معتبر** یا با امضای کهنه وارد شد؟ پنجره‌ی زمانی
   چقدر است و آیا replay ممکن است؟
2. توکن نشست مینی‌اپ (`signTgSession`) — عمرش، و اینکه اگر لو برود چه
   کاری می‌شود کرد.
3. `webhookSecretValid` — اگر رمز ست نشده باشد چه می‌شود؟ (باید fail-closed
   باشد.) آیا مسیری هست که آپدیت جعلی پذیرفته شود و به نام آیدی دلخواه
   حساب بسازد یا وصل کند؟

### 🔴 گفت‌وگوی برداشت در چت
4. `bot-flow.ts` حالت گفت‌وگو را نگه می‌دارد. آیا کاربر می‌تواند وسط
   گفت‌وگو حالت را دستکاری کند — مثلاً مبلغ را تأیید کند ولی آدرس دیگری
   بنشیند، یا یک تأیید کهنه را دوباره بفرستد؟
5. آیا گفت‌وگوی برداشت از **گروه** هم پیش می‌رود؟ (نباید.)
6. اگر دو پیام هم‌زمان از یک کاربر برسد، حالت چه می‌شود؟

### 🔴 پخش سراسری
7. `broadcast.ts` به ۵۰ هزار نفر می‌فرستد و خودش را زنجیر می‌کند. اگر وسط
   زنجیره پروسه بمیرد (هر دیپلوی) چه می‌شود؟ آیا پیام تکراری ممکن است؟
8. `isBroadcastAdmin` — آیا راهی هست که غیرادمین پخش بزند؟
9. سقف نرخ تلگرام (۳۰ پیام در ثانیه) رعایت می‌شود؟ اگر ربات بلاک شود یا
   خطا بدهد، صف چه می‌کند؟

### 🟡 مسیریابی وبهوک
10. `callback_data` با پیشوندهای `b:` `w:` `m:` `h:` `v:` `t:` مسیریابی
    می‌شود. آیا مقدار دلخواهی هست که به مسیر اشتباه برود، یا `callback_data`
    جعلی بتواند کار کسی دیگر را انجام بدهد؟ (تلگرام `callback_data` را از
    خودِ پیام می‌گیرد، ولی مهاجم می‌تواند دکمه‌ی خودش را بسازد.)
11. `keyboardCommand` متن دکمه را به دستور ترجمه می‌کند. اگر کاربر **دقیقاً
    همان متن** را دستی تایپ کند چه می‌شود؟ (احتمالاً بی‌خطر، ولی تأیید کن.)

---

## ۴. آنچه قبلاً تست شده — دوباره کشف نکن

- توکن با امضای دستکاری‌شده، شناسه‌ی عوض‌شده با امضای قبلی، کوکی جعلی → ۴۰۱.
- ۹ روت پولی بدون احراز → ۴۰۱.
- ورود واقعی با `initData` امضاشده‌ی معتبر: حساب ساخته می‌شود، هدیه‌ی دعوت
  یک بار داده می‌شود، رفرال خودی مسدود است.
- سقف نرخ روی `ir-bet` فعال است؛ با ۱۲ هزار هویت متفاوت افت نکرد.
- هر سه سطح (سایت، مینی‌اپ، ربات) به یک `playerId` می‌رسند و ۱۰ روت
  بازیکن‌محور پاسخ یکسان می‌دهند.

---

## ۵. چیزهایی که عمدی‌اند

- وبهوک از سقف نرخ میدل‌ور معاف است (دلیلش بالا گفته شد). سقف برداشت به‌جای
  آن از **جدول** شمرده می‌شود تا از داخل ربات هم قابل دور زدن نباشد.
- کارت بازار دو حالت دارد (`forward` لینکی، `live` با callback) — محدودیت
  خود تلگرام.
- رنگ دکمه ممکن نیست (Bot API 10.2 فیلدی ندارد).
- ناوبری منو با **ویرایش همان پیام** انجام می‌شود، نه پیام تازه.
- لحن: مسیر پول رسمی، بقیه خودمانی (`src/lib/tone.ts`).

---

## ۶. خروجی

```
## 🔴 بحرانی
### ۱. <عنوان>
فایل: src/...:۱۲۳
سناریو: کاربر ... می‌فرستد → ... نتیجه: ...
چرا: خط ۱۲۳ ...
اطمینان: قطعی | مشکوک
پیشنهاد: ...
این پیشنهاد چه چیزی را ممکن است بشکند: ...

## 🟡 متوسط
## 🟢 کیفیت
## بررسی‌شده و سالم
```

بخش «این پیشنهاد چه چیزی را ممکن است بشکند» **اجباری** است — دلیلش در بند ۰.

اگر برای نتیجه‌ی قطعی لازم است چیزی اجرا شود، دقیقاً بنویس چه دستوری، و
نتیجه‌اش را برایت می‌فرستم.
