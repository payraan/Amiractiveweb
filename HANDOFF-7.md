# سند تحویل نارمون — فاز ۷: لانچ و مارکتینگ ویروسی

> این سند را به‌عنوان اولین پیام چت جدید بفرست. جانشین `HANDOFF-6.md` است
> (آن را برای تاریخچه نگه دار).
>
> **منبع حقیقت نهایی، خودِ ریپو است.** هر جا این سند با کد تناقض داشت، کد را
> ملاک بگیر و تناقض را گزارش کن.
>
> 🟢 **پلتفرم از نظر فنی آماده‌ی لانچ است.** ربات، مینی‌اپ و سایت زنده‌اند،
> مسیر پول از دو طرف اثبات شده، و در فاز ۶ چند حفره‌ی جدی اقتصادی بسته شد.
>
> 🔴 **تمرکز این فاز: مارکتینگ.** مالک بدون سرمایه‌گذار می‌خواهد این پلتفرم
> را در ایران وایرال کند. این نیاز به هم‌فکری خلاقانه دارد، نه کد بیشتر.
>
> آخرین به‌روزرسانی: ۲۹ مرداد ۱۴۰۵ / ۲۰ اوت ۲۰۲۶

---

## ۰. نقش تو و سبک کار

تو Principal Engineer این پروژه‌ای، **و در این فاز شریک مارکتینگ هم هستی.**
مالک برنامه‌نویس نیست و در سطح کپی‌پیست و اجرای دستور ترمینال کار می‌کند.

- **پاسخ‌ها به فارسی.**
- **قبل از هر کامیت:** `npx tsc --noEmit`؛ در تغییرات بزرگ `npm run build`.
- **لینت baseline = ۳۳ مورد** (۲۷ خطا + ۶ هشدار). بالا نرود. خطای تازه را
  **رفع کن، سرکوب نکن**.
- **کد را با داده‌ی واقعی تست کن، نه فقط کامپایل.** هر مسیر پولی با `curl` و
  `psql` روی دیتابیس **محلی** سرتاسر تست شود، بعد کامیت. **داده‌ی تستی را
  بعد از تست پاک کن.**
- **در مرورگر ببین.** برای هر تغییر دیداری، صفحه را در عرض ۳۷۵px باز کن.
- **پوش:** مالک اجازه‌ی پایدار داده؛ هر پوش را گزارش کن و پس از دیپلوی
  (~۱ تا ۲ دقیقه) سلامت پروداکشن را بسنج.
- **هر جا تصمیم سلیقه‌ای یا اقتصادی بود، از مالک بپرس.**

### دسترسی‌ها

```bash
postgresql://amiractive@localhost:5432/narmoon   # دیتابیس محلی
```

رمز دیتابیس پروداکشن و `SETTLE_KEY` در فاز ۶ عوض شدند. رشته‌ی اتصال
پروداکشن را **هرگز در چت نگیر** — فقط نتیجه‌ی دستور را بخواه.

### درس‌های گران (تکرارشان نکن)

۱. **تعداد را با مقدار اشتباه نگیر.**
۲. **پیش از اعلام «انجام شد»، در مرورگر ببین.** `grep` جای دیدن را نمی‌گیرد.
۳. **پنل خالی = پنل نامرئی.** هر فهرستی حالت خالی داشته باشد.
۴. **کامپایل شدن یعنی کد ساخته شد، نه اینکه اجرا می‌شود.**
۵. **جای‌گزینی متنی روی JSX نیمی از کار را بی‌صدا جا می‌گذارد.** بشمار.
۶. **خطا را نبلع.** شمارنده‌ی بدون علت از خودِ مشکل بدتر است.
۷. **جست‌وجوی خراب، «پاک است» نیست.** برای فارسی از پایتون استفاده کن نه grep.
۸. **۲۰۰ گرفتن یعنی سرور نمرد، نه اینکه کار درست انجام شد.**
۹. **راه‌حل را به‌اندازه‌ی مسئله بررسی کن.**
۱۰. **نتیجه‌ی شبیه‌سازی به پارامترهای ناگفته حساس است.**
۱۱. **Postgres کل دستور را پیش از اجرا تجزیه می‌کند.**
۱۲. **از یک ماشین، «خالی» ممکن است یعنی «بلاک شده».**
۱۳. **لاگی که دیده نشود، از نبودنش بدتر است.**
۱۴. **قابلیتی که دکمه ندارد، وجود ندارد.**
۱۵. **تلگرام `/start` را برای چتِ از قبل باز دوباره نمی‌فرستد.**
۱۶. 🆕 **کوئریِ سنجش را هم باید سنجید.** یک بار اعلام کردم «فهرست دستورها
    ثبت نشده» در حالی که `getMyCommands` را بدون `scope` صدا زده بودم و
    سطل دیگری را می‌خواندم. ثبت درست بود؛ **سنجش من غلط بود.**
۱۷. 🆕 **`try/catch` داخل ترنزاکشن Postgres هیچ کاری نمی‌کند.** پس از هر
    خطا کل ترنزاکشن باطل است. اگر یک عمل جانبی نباید مسیر اصلی را بکشد،
    **`SAVEPOINT` لازم است**، نه `try/catch`.
۱۸. 🆕 **`LIMIT` بدون `ORDER BY` گرسنگی می‌سازد.** تسویه‌ی ترید ۱۵ بازار
    اول را می‌گرفت و بازارهای بلندمدت آن جاها را برای همیشه اشغال
    می‌کردند.
۱۹. 🆕 **فرضِ نانوشته در فرمول، خطرناک‌ترین باگ است.** `√288` فرض می‌کرد
    هر دارایی ۲۴ ساعت معامله می‌شود؛ سهام ۷۸ کندل در روز دارد و نوسانش
    ۱.۹۲ برابر باد کرده بود.

---

## ۱. پلتفرم چیست

**نارمون** — اولین بازار پیش‌بینی فارسی‌زبان.

- ریپو: `https://github.com/payraan/Amiractiveweb` (برنچ `main`)
- دیپلوی: Railway — `https://amiractiveweb-production.up.railway.app`
- ربات: `@NarmoonMarketBot` · مینی‌اپ نام‌دار: `t.me/NarmoonMarketBot/market`
- کانال: `@CashflowFactorys` (`TG_GROUP_ID=-1002127501102`)
- استک: Next.js 16.2.10، React 19، Tailwind v4، TypeScript، PostgreSQL با
  `pg` خام. مسیر `@/*` → `src/*`. بدون تست خودکار.

**تز محصول:** امتیاز فقط از مهارت می‌آید. `MOON` فقط قابلیت باز می‌کند و
**هرگز امتیاز یا رتبه نمی‌خرد.**

### سه سطح، یک دیتابیس

| سطح | مسیر | احراز هویت |
|---|---|---|
| سایت | `/` و بقیه | کوکی `amir_session` |
| مینی‌اپ | `/app` | هدر `x-tg-auth` |
| ربات | وبهوک `/api/tg/webhook` | `tg_user_id` |

`currentPlayerId()` هر سه را به یک شناسه می‌رساند. **هیچ روت اختصاصی
مینی‌اپی وجود ندارد** و نباید ساخته شود.

---

## ۲. آنچه در فاز ۶ ساخته شد

| قابلیت | جزئیات |
|---|---|
| **هدیه‌ی عضویت واقعی** | `/bonus` حالا با `getChatMember` عضویت را می‌سنجد. قبلا به همه MOON می‌داد. یک‌کلیکی در سایت، مینی‌اپ و ربات. |
| **مانیتورینگ سرتاسری** | `tgCall` مرکزی ابزار گرفت، `catch` خالی وبهوک بسته شد، لاگ دسترسی در میدل‌ور. راهنما در `MONITORING.md`. |
| **یادآوری پیش از بسته‌شدن بازار** | ۴۸ و ۲۴ ساعت قبل، با دکمه‌ی «همین حالا ببند» در ربات. |
| **پنل دعوت در مینی‌اپ و ربات** | `/invite` و تب پروفایل. لینک به مینی‌اپ، نه سایت. |
| **ثبت کانال سازندگان** | ادمین کانالش را ثبت می‌کند، مالک تأیید می‌کند، کارمزد ساخت بازار صفر می‌شود. |
| **سهم سازنده‌ی بازار** | ۳٪ از دعوت‌شده‌های خودش، ۱.۵٪ از بقیه — **اضافه بر** ۳٪ پلتفرم. |
| **کد بونوس کمپین** | `/newcode CODE moon 50 200 7` · کاربر با `/code` یا کیف پول مینی‌اپ. |
| **اعلان نتیجه‌ی پیش‌بینی** | هر سه بازی. صف `tg_outbox` با `SAVEPOINT`. |
| **خط پیش‌بینی کشیدنی** | روی نمودار نبض بازار، با نوارهای امتیاز. |
| **ناوبری یکسان مینی‌اپ** | `BackLink` در هشت زیرصفحه. |

### 🔴 حفره‌های اقتصادی که بسته شدند

سه ایراد که همه در یک جهت کار می‌کردند — امتیاز مفت به بازیکن بی‌مهارت:

۱. **بورس بسته:** شرط باز بودن سهام فقط «دوشنبه تا جمعه» بود بدون ساعت.
   ۱۷ ساعت از ۲۴ ساعت، قیمت یخ بود → ۱۰۰ امتیاز تضمینی. با ۲۴۰ MOON
   (~۲۰ دلار) می‌شد صدر جدول را خرید.
۲. **اریب نوسان:** `dailyVol` همیشه `√288` می‌زد. سهام ۷۸ کندل در روز
   دارد → نوسان ۱.۹۲ برابر باد کرده → آستانه‌های دو برابر گشاد.
۳. **گیره‌ی ضریب:** کف ۰.۲ روی جفت‌ارزها می‌گرفت (نوسان واقعی EURUSD
   ۰.۲۷٪ → ضریب ۰.۱۳).

**نتیجه، با شبیه‌سازی مونت‌کارلو** — امید امتیاز بازیکن بی‌مهارت که باید
≈ ۴.۶− باشد:

| دارایی | قبل | بعد |
|---|---|---|
| AAPL | +۱۹.۱۳ | −۴.۵۴ |
| EURUSD | +۱۱.۶۲ | −۴.۹۳ |
| طلا | −۱.۵۴ | −۴.۷۴ |
| BTC | −۴.۷۱ | −۴.۸۹ |

**۲۲ دارایی از ۴۲ مزرعه‌ی امتیاز بودند.** حالا هر چهار کلاس روی هدف‌اند.

⚠️ ضریب در لحظه‌ی ساخت راند قفل می‌شود، پس **راندهای بازِ فعلی با ضریب
قدیمی تسویه می‌شوند**. اگر می‌خواهی تمیز شروع کنی، پیش از لانچ راندهای باز
را پاک کن.

---

## ۳. اقتصاد — اعداد قفل‌شده

```
COMMISSION              = 0.03    # ۳٪ پلتفرم از حجم استخر
CREATOR_SHARE_REFERRED  = 0.03    # سهم سازنده از دعوت‌شده‌های خودش
CREATOR_SHARE_OTHER     = 0.015   # سهم سازنده از بقیه
MIN_STAKE_USDT = 1 · MIN_ODDS = 1.05 · DISPUTE_HOURS = 24
PROPOSE_FEE_USDT = 1 (ادمین و کانال تأییدشده: رایگان)
BOOST_PRICE_USDT = 5 · BOOST_HOURS = 24
WITHDRAW_FEE_USDT = 3 · MIN_WITHDRAW = 4
WELCOME_CREDITS = 10 · POLY_FREE_PER_DAY = 5
REFERRAL_PERCENT = 10 · REFERRAL_BONUS = 5 · GROUP_BONUS = 20
REF_VOL_PCT = 2.0 · VOL_SCALE_MIN = 0.05 · VOL_SCALE_MAX = 8.0
```

### ⚠️ نگهبان ضدفارمِ سهم سازنده

اگر سازنده با حساب‌های دعوت‌شده‌ی خودش روی هر دو طرف بازار خودش شرط ببندد:

```
خالص = S×(۱ − p − c) + S×c − S = −p×S
```

**نرخ سازنده (`c`) از معادله حذف می‌شود** — پرداخت می‌شود و همان لحظه
برمی‌گردد. پس هر عددی باشد، خالصِ فارم برابر کمیسیون پلتفرم و منفی است.
دو قید واقعی:

۱. `COMMISSION > 0` بماند.
۲. سهم سازنده همیشه **از استخر** برداشته شود، هرگز ساخته نشود.

---

## ۴. یافته‌های باز

| # | مورد | شدت |
|---|---|---|
| ۱ | **زمان‌بندی نوسان:** ضریب در ساخت راند قفل می‌شود؛ پیش‌بینی بعد از جهش نوسان (وقتی بازار آرام گرفته) امید امتیاز را از ۴.۶− به ۱۰+ تا ۲۰+ می‌برد. بستنش یعنی «قانون وسط بازی عوض شد» — فعلا فقط رصد شود. | 🟡 |
| ۲ | ورودی چالش از MOONِ رفرال/هدیه عملا رایگان می‌شود | 🟡 |
| ۳ | هدف چالش پراپ (۳۰۰) شل است — با یک پیش‌بینی در روز ۱۱.۴٪ بدون مهارت قبول می‌شوند | 🟡 |
| ۴ | کاور بازار در سایت و مینی‌اپ دیده نمی‌شود (فقط تلگرام) | 🟡 |
| ۵ | سقف نرخ در حافظه است؛ با هر دیپلوی صفر می‌شود | 🟡 |
| ۶ | آدرس واریز برای همیشه کش می‌شود | 🟡 |
| ۷ | تأیید برداشت در Zovix دستی است | 🟡 |
| ۸ | همه‌ی داده‌ی نبض بازار به یاهو گره خورده — تک‌نقطه‌ی اتکا | 🟡 |
| ۹ | `robots.txt` و `sitemap.xml` وجود ندارند | 🟢 |
| ۱۰ | فهرست دستور قدیمی در scope گروه‌ها `/wallet` را پیشنهاد می‌دهد | 🟢 |
| ۱۱ | برچسب دکمه‌ی منوی ربات «نارموون» است (یک «و» اضافه) — BotFather | 🟢 |
| ۱۲ | چندحسابی: ثبت‌نام سایت هیچ اعتبارسنجی تلگرامی ندارد | 🟡 |

---

## ۵. کوئری‌های سلامت — باید همیشه صفر برگردانند

```sql
-- ۱. موجودی (واقعی + دمو) = جمع دفترکل
SELECT count(*) FROM (
  SELECT p.id FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
  GROUP BY p.id, p.usdt_balance, p.demo_balance
  HAVING p.usdt_balance + p.demo_balance <> COALESCE(SUM(w.amount),0)) x;

-- ۲. موجودی دمو = جمع ستون demo دفترکل
SELECT count(*) FROM (
  SELECT p.id FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
  GROUP BY p.id, p.demo_balance
  HAVING p.demo_balance <> COALESCE(SUM(w.demo),0)) x;

-- ۳. استخر بازار = مجموع پیش‌بینی‌ها  ← مهم‌ترین
SELECT count(*) FROM (
  SELECT m.id FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
  GROUP BY m.id, m.yes_total, m.no_total
  HAVING m.yes_total <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0)
      OR m.no_total  <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0)) x;

-- ۴. سهم سازنده = جمع سهم تک‌تک شرط‌ها
SELECT count(*) FROM (
  SELECT m.id FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
  GROUP BY m.id, m.creator_cut
  HAVING round(m.creator_cut,6) <> round(COALESCE(SUM(b.creator_cut),0),6)) x;

-- ۵. سهم دمو هرگز از اصل بیشتر نباشد
SELECT count(*) FROM ir_bets WHERE demo_stake > stake;

-- ۶. هیچ موجودی منفی نباشد
SELECT count(*) FROM players WHERE usdt_balance < 0 OR demo_balance < 0;
```

---

## ۶. 🚀 تمرکز این فاز — مارکتینگ بدون سرمایه

مالک بدون سرمایه‌گذار می‌خواهد نارمون را در ایران وایرال کند. **این
مهم‌ترین بخش این سند است.**

### آنچه از قبل ساخته شده و باید استفاده شود

- **کارت بازار قابل فوروارد** — ارزان‌ترین کانال رشد. هر بازار یک کارت با
  دکمه‌ی «بله/خیر» است که مستقیم به مینی‌اپ می‌رود.
- **پخش سراسری** با صف مقاوم — ۲۵ پیام در ثانیه، برای ۵۰ هزار نفر.
- **بوست** — ۵ تتر، بازار به همه‌ی کاربران می‌رسد.
- **کد بونوس کمپین** — `/newcode`.
- **اقتصاد سازنده** — ادمین کانال بازار می‌سازد (رایگان پس از تأیید کانال)
  و از فعالیت اعضایش سهم می‌گیرد.
- **رفرال** — ۱۰٪ از شارژ MOONِ دعوت‌شده‌ها.

### واقعیتی که بر همه‌چیز اثر دارد

🔴 **کشور در جنگ است.** درگیری آمریکا و اسرائیل با ایران از اسفند ۱۴۰۴،
آتش‌بس شکننده، تحریم تازه، دلار ۱۸۷ هزار، تورم ۶۶٪.

**قاعده‌ی محتوا (اصلاح‌شده ۲۹ مرداد ۱۴۰۵ — تصمیم صریح مالک):**

نسخه‌ی قبلی این سند موضوع سیاسی را ممنوع می‌دانست، در حالی که بازارهای
سیاسی و ژئوپلیتیک از قبل ساخته شده و منتشر شده بودند (مثلا بازار ۵۱ —
بازگشایی تنگه‌ی هرمز). **سند اشتباه بود، نه محصول.** قاعده‌ی درست:

- **بازار سیاسی و ژئوپلیتیک مجاز است** — دسته‌ی `politics` برای همین
  اضافه شد. ورزش هم مجاز است و دسته‌اش (`sports`) از قبل وجود داشت.
- **آنچه بیرون می‌ماند:** بازار روی مرگ یا آسیبِ افرادِ مشخص، و
  موضع‌گیری خودِ پلتفرم. «آیا تنگه‌ی هرمز بازگشایی می‌شود» یک پرسش
  ژئوپلیتیک با اثر روشن بر بازار است؛ «آیا فلانی کشته می‌شود» چیز
  دیگری است — هم از نظر انسانی و هم از نظر ریسک حقوقیِ کاربر ایرانی.
- لحن پلتفرم بی‌طرف می‌ماند: سؤال می‌پرسیم، طرف نمی‌گیریم. مخاطب در
  آن کشور زندگی می‌کند.

### خطوط قرمز مارکتینگ

- ادعای «سود تضمینی» هرگز. وعده‌ی «از پیش‌بینی پول دربیار» هرگز.
- یوزر و لیدربورد فیک نه. عدد ساختگی روی تصویر تبلیغاتی نه.
- **تعداد کاربران پلتفرم هرگز عمومی نمی‌شود.**
- واژگان ممنوع: «شرط‌بندی» «قمار» «باخت» «بازنده» «بازی».
- ایردراپ و توکن نه.

### آنچه چت جدید باید انجام دهد

۱. **قیف رشد را با مالک بچین:** از دیدن یک کارت در کانال تا اولین
   پیش‌بینی، تا دعوت نفر بعدی. هر قدم اصطکاکش سنجیده شود.
۲. **محتوای ویروسی طراحی کن** که خودِ محصول تولیدش می‌کند — کارت بازار،
   کارنامه‌ی قابل اشتراک، نتیجه‌ی پیش‌بینی.
۳. **ادمین‌های کانال را هدف بگیر.** اقتصاد سازنده ساخته شده؛ حالا باید
   به آن‌ها فروخته شود.
۴. **کمپین پیش‌لانچ** — مالک پروژه‌ی جدا «امپراطوری برندینگ» دارد با
   ساختار ده‌نقشی و کمپین ۷ روزه‌ی سؤال بله/خیر دستی.

---

## ۷. دستورها و محیط

```bash
npm run dev      # لوکال روی :3000
npm run build    # بیلد production
npm run lint     # eslint — baseline ۳۳
npx tsc --noEmit # تایپ‌چک
```

⚠️ میدل‌ور در **`src/middleware.ts`** است، نه ریشه.

⚠️ **`npm run build` و بعد `npm run dev` بدون پاک‌کردن `.next` باعث ۴۰۴
شدن همه‌ی روت‌ها می‌شود.** اگر دیدی همه‌چیز ۴۰۴ است: `rm -rf .next`.

⚠️ **کلیدهای Zovix و توکن ربات در `.env.local` واقعی‌اند.** هر تستی که به
`createWithdrawal` برسد یک برداشت واقعی می‌سازد.

⚠️ **مینی‌اپ بدون `initData` باز نمی‌شود.** برای دیدن کامپوننت‌ها صفحه‌ی
موقت `src/app/app/zz*/page.tsx` بساز و **پیش از کامیت پاکش کن**.

متغیرها: `DATABASE_URL` `SESSION_SECRET` `ADMIN_SECRET` `ADMIN_PASSWORD`
`PGSSL` `PGPOOL_MAX` `SETTLE_KEY` `LOG_LEVEL` `ZOVIX_*` `TG_BOT_TOKEN`
`TG_BOT_USERNAME` `TG_WEBHOOK_SECRET` `TG_ADMIN_IDS` `TG_GROUP_ID`
`SITE_URL` `GEMINI_API_KEY` `MYFXBOOK_*` `NEXT_PUBLIC_DEV_FIXTURES`

---

## ۸. خطوط قرمز محصول (تصمیم قطعی مالک)

- ادعای «سود تضمینی» هرگز. وعده‌ی «از پیش‌بینی پول دربیار» هرگز.
- ایردراپ و توکن نه. یوزر و لیدربورد فیک نه.
- **امتیازدهی صفر-انتظار حفظ شود.**
- استخر parimutuel تتری بازار ایران با تصمیم صریح مالک مجاز است.
- ورود با گوگل و KYC هر دو رد شدند.
- **برداشت هرگز به دلیل بلاک‌بودن ربات قفل نمی‌شود.**
- **پول دمو هرگز قابل برداشت نیست؛ فقط سودش.**
- **تعداد کاربران پلتفرم هرگز عمومی نمی‌شود.**
- واژگان شرط‌بندی برنگردد.

---

## ۹. اولین کارهای چت جدید

۱. **کوئری‌های سلامت بخش ۵ را روی پروداکشن بگیر.**
۲. **لاگ Railway را بخواه** (`@level:error` و `@evt:cron.tick`).
۳. **بپرس راندهای بازِ نبض بازار پاک شده‌اند یا نه** (بخش ۲، ضریب قدیمی).
۴. **سه کار کوچک باقی‌مانده‌ی BotFather:** آیکون اسپلش
   (`public/tg/splash.svg` آماده است)، رنگ‌ها `#0A0A0C`، و اصلاح
   «نارموون» → «نارمون».
۵. **بعد سراغ مارکتینگ برو — تمرکز اصلی این فاز همان است.**
