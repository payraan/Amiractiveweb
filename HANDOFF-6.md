# سند تحویل نارمون — فاز ۶: لانچ و رشد

> این سند را به‌عنوان اولین پیام چت جدید بفرست. جانشین `HANDOFF-5.md` است
> (آن را برای تاریخچه نگه دار).
>
> **منبع حقیقت نهایی، خودِ ریپو است.** هر جا این سند با کد تناقض داشت، کد را
> ملاک بگیر و تناقض را گزارش کن.
>
> 🟢 **مسیر پول از دو طرف بسته و اثبات شده است.** واریز و برداشت واقعی هر دو
> روی زنجیره انجام شده‌اند. فاز ۵ اینجا تمام شد.
>
> 🔴 **تمرکز این فاز: لانچ عمومی ۱ شهریور ۱۴۰۵ (۲۳ اوت ۲۰۲۶) و رشد.**
> مالک بیشتر روی رودمپ، مارکتینگ، ایده‌پردازی و ساخت بازار کار می‌کند تا کد.
>
> آخرین به‌روزرسانی: ۲۴ مرداد ۱۴۰۵ / ۱۵ اوت ۲۰۲۶

---

## ۰. نقش تو و سبک کار

تو Principal Engineer این پروژه‌ای، **و در این فاز شریک استراتژی هم هستی.**
مالک برنامه‌نویس نیست و در سطح کپی‌پیست و اجرای دستور ترمینال کار می‌کند.

- **پاسخ‌ها به فارسی.**
- **قبل از هر کامیت:** `npx tsc --noEmit`؛ در تغییرات بزرگ `npm run build`.
- **لینت baseline = ۳۳ مورد** (۲۷ خطا + ۶ هشدار). بالا نرود. خطای تازه را
  **رفع کن، سرکوب نکن**. (فاز ۵ عدد ۳۲ نوشته بود؛ غلط بود.)
- **کد را با داده‌ی واقعی تست کن، نه فقط کامپایل.** هر مسیر پولی با `curl` و
  `psql` روی دیتابیس **محلی** سرتاسر تست شود (سناریوی موفق + سوءاستفاده)،
  بعد کامیت. **داده‌ی تستی را بعد از تست پاک کن.**
- **در مرورگر ببین.** برای هر تغییر دیداری، صفحه را در عرض ۳۷۵px باز کن.
- **پوش:** مالک اجازه‌ی پایدار داده؛ هر پوش را گزارش کن و پس از دیپلوی
  (~۱ تا ۲ دقیقه) سلامت پروداکشن را بسنج.
- هر تغییر را پیش از اعمال با یکی‌دو جمله‌ی ساده‌ی فارسی توضیح بده.
- **هر جا تصمیم سلیقه‌ای یا اقتصادی بود، از مالک بپرس.**
- اگر چیزی با این سند نمی‌خواند، **اول گزارش بده**، خودسرانه بازنویسی نکن.

### دسترسی‌ها

```bash
# دیتابیس محلی
postgresql://amiractive@localhost:5432/narmoon
```

🔴 **رمز دیتابیس پروداکشن و `SETTLE_KEY` هر دو در چت فاز ۵ لو رفته‌اند و
هنوز عوض نشده‌اند.** اولین کاری که باید از مالک بخواهی همین است. رشته‌ی
اتصال را **هرگز در چت نگیر** — فقط نتیجه‌ی دستور را بخواه.

### درس‌های گران (تکرارشان نکن)

۱. **تعداد را با مقدار اشتباه نگیر.**
۲. **پیش از اعلام «انجام شد»، در مرورگر ببین.** `grep` جای دیدن را نمی‌گیرد.
۳. **پنل خالی = پنل نامرئی.** هر فهرستی حالت خالی داشته باشد.
۴. **کامپایل شدن یعنی کد ساخته شد، نه اینکه اجرا می‌شود.**
۵. **جای‌گزینی متنی روی JSX نیمی از کار را بی‌صدا جا می‌گذارد.** بشمار.
۶. **خطا را نبلع.** شمارنده‌ی بدون علت از خودِ مشکل بدتر است.
۷. **جست‌وجوی خراب، «پاک است» نیست.** برای فارسی از پایتون استفاده کن نه grep.
۸. **۲۰۰ گرفتن یعنی سرور نمرد، نه اینکه کار درست انجام شد.**
۹. **راه‌حل را به‌اندازه‌ی مسئله بررسی کن.**
۱۰. **نتیجه‌ی شبیه‌سازی به پارامترهای ناگفته حساس است.**
۱۱. **Postgres کل دستور را پیش از اجرا تجزیه می‌کند.**
۱۲. 🆕 **از یک ماشین، «خالی» ممکن است یعنی «بلاک شده».** درگاه Zovix پشت
    Cloudflare است؛ چند درخواست پشت‌سرهم از یک IP باعث چالش می‌شود و پاسخ
    HTML می‌آید نه JSON. یک بار تشخیص غلط دادم که «کلید مال پروژه‌ی دیگری
    است» در حالی که فقط IP بلاک شده بود. **برای سنجش درگاه، از سرور بپرس نه
    از لپ‌تاپ.**
۱۳. 🆕 **لاگی که دیده نشود، از نبودنش بدتر است** — توهم پوشش می‌سازد. سه
    روز ۲۶۳ لاگ نوشته شد و همه در Railway خالی دیده می‌شدند، چون نام فیلد
    غلط بود.
۱۴. 🆕 **قابلیتی که دکمه ندارد، وجود ندارد.** بوست و کاور هر دو ساخته شدند و
    روزها بلااستفاده ماندند چون هیچ‌جای رابط قابل زدن نبودند.
۱۵. 🆕 **تلگرام `/start` را برای چتِ از قبل باز دوباره نمی‌فرستد.** هر مسیری
    که به لینک عمیق `?start=` تکیه کند، برای کاربر قدیمی کار نمی‌کند.

---

## ۱. پلتفرم چیست

**نارمون** — اولین بازار پیش‌بینی فارسی‌زبان.

> ⚠️ نام برند **«نارمون»** است، نه «نارموون». اگر جایی دومی را دیدی
> (مثلا دکمه‌ی منوی BotFather) به مالک بگو اصلاحش کند.

- ریپو: `https://github.com/payraan/Amiractiveweb` (برنچ `main`)
- دیپلوی: Railway — `https://amiractiveweb-production.up.railway.app`
- ربات: `@NarmoonMarketBot` · مینی‌اپ روی `/app`
- استک: Next.js **16.2.10** App Router، React 19، Tailwind v4، TypeScript،
  PostgreSQL با `pg` خام (بدون ORM). مسیر `@/*` → `src/*`. بدون تست خودکار.
- اندازه: **~۳۷٬۲۰۰ خط** TS/TSX · **۵۲ روت API** · **۲۴ جدول** · ۴۹ ماژول lib

**تز محصول:** امتیاز فقط از مهارت می‌آید. `MOON` فقط قابلیت باز می‌کند و
**هرگز امتیاز یا رتبه نمی‌خرد.**

### سه سطح، یک دیتابیس

| سطح | مسیر | احراز هویت |
|---|---|---|
| سایت | `/` و بقیه | کوکی `amir_session` |
| مینی‌اپ | `/app` | هدر `x-tg-auth` |
| ربات | وبهوک `/api/tg/webhook` | `tg_user_id` |

`currentPlayerId()` هر سه را به یک شناسه می‌رساند. **هیچ روت اختصاصی
مینی‌اپی وجود ندارد** و نباید ساخته شود.

---

## ۲. نقشه‌ی کد — کجا چه چیزی است

### ماژول‌های حیاتی (به ترتیب اهمیت)

| فایل | خط | چه چیزی |
|---|---|---|
| `lib/iran.ts` | ۸۰۷ | **قلب پول** — `moveFunds`، `recordRevenue`، تسویه، اسکیما، همه‌ی اعداد اقتصادی بازار ایران |
| `lib/telegram.ts` | ۱۰۵۳ | تماس با Bot API، `marketPoll`، `sendScreen`، انواع دکمه |
| `lib/bot-menu.ts` | ۶۲۰ | متن و منوی ربات، `mainKeyboard`، صفحه‌های خوش‌آمد/راهنما/پشتیبانی |
| `lib/broadcast.ts` | ۵۱۴ | پخش سراسری — صف مقاوم، ارسال موازی، سقف نرخ |
| `lib/challenge.ts` | ۴۷۹ | چالش پراپ |
| `lib/poly.ts` | ۴۰۵ | بازار خارجی (پالی‌مارکت) و امتیازدهی صفر-انتظار |
| `lib/withdrawal.ts` | ۲۲۳ | **تنها مسیر برداشت** — سایت، مینی‌اپ و ربات |
| `lib/withdrawal-sync.ts` | ۲۸۰ | آشتی برداشت با درگاه + `refundWithdrawal` |
| `lib/deposit-sync.ts` | ۲۱۹ | **تنها مسیر واریز** — خواندن دوره‌ای از درگاه |
| `lib/zovix.ts` | ۲۱۴ | کلاینت درگاه کریپتویی |
| `lib/log.ts` | ۱۸۰ | 🆕 لاگ ساخت‌یافته |

### فایل‌های «بدون ایمپورت» — قانون مقدس

این‌ها عمداً **هیچ ایمپورتی ندارند** تا کامپوننت‌های `"use client"` هم
بتوانند بخوانندشان (چون `iran.ts` درایور `pg` را به باندل مرورگر می‌کشد):

`wallet-rules.ts` · `ledger-labels.ts` · `poly-scoring.ts` · `revenue-kinds.ts`
· `ir-categories.ts` · `tone.ts`

⚠️ `game.ts` **در این فهرست نیست** — دو ایمپورت سبک دارد (`config/site` و
`lib/assets`) که هیچ‌کدام وابستگی سرور ندارند، پس همچنان در مرورگر امن است.
ولی اگر روزی چیز سنگینی به آن اضافه شد، باندل مرورگر می‌ترکد. حواست باشد.

⚠️ **هر مقدار یا برچسبی که در بیش از یک فایل کپی شود، دیر یا زود از هم جدا
می‌شود.** در فاز ۵ چهار بار و در فاز ۶ سه بار این اتفاق افتاد (برچسب درآمد،
لینک پشتیبانی در ۱۱ فایل، `floorUsdt` در سه سطح). اگر دیدی مقداری دو جا
هست، **همان لحظه یکی‌اش کن.**

### رابط کاربری

**سایت (۲۱ صفحه):** `/` `/iran` `/iran/m/[id]` `/iran/propose` `/arena`
`/predict` `/trade` `/combos` `/challenge` `/wallet` `/profile` `/leaderboard`
`/referral` `/results` `/broker` `/bot` `/legal` `/login` `/admin` `/app`

**مینی‌اپ (۱۵ صفحه):** Markets · MarketDetail · MyBets · Propose · Pulse ·
PulseDetail · Trade · TradeDetail · Challenge · Wallet · Deposit · Withdraw ·
BuyMoon · Profile · Gate

**کامپوننت‌های سنگین:** `iran/IranTerminal.tsx` (ترمینال بازار ایران، ~۸۰۰
خط) · `wallet/WalletPanel.tsx` · `predict/TradeTerminal.tsx`

---

## ۳. قواعد پولی — تغییر نده

- کسر پول **همیشه** داخل ترنزاکشن با `SELECT … FOR UPDATE`.
- **ترتیب قفل سراسری: اول بازار، بعد بازیکن.** برعکسش بن‌بست ABBA می‌سازد.
- `moveFunds()` تنها راه تغییر موجودی. مقدار **مطلق** می‌نویسد نه دلتا.
- `recordRevenue()` برای درآمد، همیشه با `demoAmount`.
- **پول واقعی فقط از `deposit-sync` می‌آید.**
- **میدل‌ور در `src/middleware.ts` است، نه ریشه.**
- `total_points` از نوع `NUMERIC(14,4)` — هر `SELECT` باید `ROUND(...)::int`
  بزند وگرنه `pg` **رشته** می‌دهد.

### پول دمو

- بونوس در `players.demo_balance`، پول واقعی در `usdt_balance`.
- **خرج همیشه اول از دمو** — روی شرط، خرید MOON، کارمزد ساخت بازار.
- **استثنا: بوست فقط از پول واقعی** (`realOnly`) — چون قرار است درآمد باشد.
- **برگشت اصلِ پول به دمو، سود به واقعی.**
- **برداشت فقط `usdt_balance` را می‌بیند.**
- ⚠️ **مهاجرت یک‌باره خاموش شده** (چون واریز واقعی ثبت شده). دیگر برنمی‌گردد.

### 🆕 قاعده‌ای که سه بار شکست

**هر فرمی که خرج می‌کند باید همان عددی را نشان بدهد که سرور می‌پذیرد.**

سه بار این شکست: دکمه‌ی برداشت (`toFixed` رو به بالا گرد می‌کرد)، فرم
پیش‌بینی (فقط پول واقعی را می‌دید در حالی که خرج از دمو می‌رود)، و کارمزد
ساخت بازار (همان). هر بار کاربر با دکمه‌ی مرده یا خطای بی‌دلیل روبه‌رو شد.

---

## ۴. مسیر پول — کامل و اثبات‌شده

### واریز

```
کاربر تتر می‌فرستد → Zovix ثبت می‌کند →
کرون هر ۱۵ دقیقه deposit/index را می‌خواند →
creditDeposit() با یکتاسازی روی txid → moveFunds
```

⚠️ **وبهوک درگاه در پلن رایگان قفل است و هرگز شلیک نمی‌شود.** روت
`/api/wallet/webhook` نگه داشته شده تا اگر پلن ارتقا یافت بدون تغییر کار کند.

### برداشت

```
کاربر مبلغ می‌زند → کل مبلغ از حسابش کم می‌شود →
مبلغ منهای کارمزد به Zovix →
کرون تا دیدن txid روی زنجیره پیگیری می‌کند
```

⚠️ **مدرک «پول رفت» شناسه‌ی تراکنش است، نه رشته‌ی وضعیت.** Zovix همان
لحظه‌ی ثبت `SUCCESS` می‌دهد در حالی که هنوز تأیید دستی نشده.

### اعداد قفل‌شده

```
COMMISSION        = 0.03    # ۳٪ از حجم استخر
MIN_STAKE_USDT    = 1
MIN_ODDS          = 1.05
DISPUTE_HOURS     = 24
PROPOSE_FEE_USDT  = 1       # ادمین: رایگان
BOOST_PRICE_USDT  = 5       # ادمین: رایگان · ۲۴ ساعت
WITHDRAW_FEE_USDT = 3       # کارمزد شبکه TRON، به ما نمی‌رسد
MIN_WITHDRAW      = 4       # محاسبه می‌شود: کارمزد + حداقل ارسال درگاه
```

⚠️ **کارمزد ۳ تتری از خودِ مبلغ کم می‌شود، نه از استخر.** پیش از فاز ۶
برعکس بود و هر برداشت ۳ تتر از پول کاربران دیگر آب می‌کرد.

### درگاه Zovix — واقعیت‌های آزموده‌شده

| موضوع | وضعیت |
|---|---|
| کارمزد TRON | ۳ تتر · TON ۰.۵ · BSC ۱ · Solana ۱ · ETH ۴ |
| حداقل ارسال | ۱ تتر |
| سقف نرخ API | حدود ۵ درخواست پشت‌سرهم، بعد ۴۲۹ و چالش Cloudflare |
| وضعیت‌های برداشت | `PENDING` `PENDING_ADMIN` `SENT_TO_BLOCKCHAIN` `PENDING_CONFIRM` `SUCCESS` `FAILED` `CANCELLED_BY_ADMIN` `CANCELLED_BY_USER` `REJECTED_BY_AML` |
| تأیید برداشت | **دستی توسط ادمین Zovix** (قول داده اتوماتش کند) |
| انتقال داخلی | رکورد `deposit` می‌سازد، ولی وبهوک نمی‌فرستد |

---

## ۵. بوست — موتور درآمد و رشد

**۵ تتر (برای ادمین رایگان) → ۲۴ ساعت در پنل طلایی + ارسال به همه‌ی کاربران.**

- فقط سازنده، فقط بازار باز، بوست روی بوست **تمدید** می‌شود.
- **پرداخت فقط از پول واقعی.**
- ⚠️ **بوست فقط دیده‌شدن می‌خرد** — نه ضریب، نه تسویه، نه شانس برد. لحظه‌ای
  که پول بتواند نتیجه را عوض کند، تز محصول شکسته است.
- پس از خرید، یک تیک پخش **فوری** زده می‌شود (نه انتظار تا کرون بعدی).
- کارت پخش از همان `marketPoll` ساخته می‌شود که کارت کانال را می‌سازد.

### کاور ۱۶:۹

- **فقط ادمین** — تصویر بدون بازبینی به پیام همگانی می‌رود. سه لایه بسته شد.
- از راه ربات آپلود می‌شود و `file_id` تلگرام ذخیره می‌گردد — **هیچ آپلود و
  هاستی لازم نیست** و ۵۰ هزار پیام با کاور صفر بار روی سرور می‌گذارد.
- ⚠️ کارِ پخش کاور را **در لحظه‌ی ساخت** snapshot می‌کند. کاورِ بعد از بوست
  در پیام رفته دیده نمی‌شود.
- ⚠️ **کاور در سایت و مینی‌اپ نمایش داده نمی‌شود** — فقط در تلگرام. برای وب
  یک روت پروکسی لازم است که `file_id` را به آدرس موقت تبدیل کند. (مالک گفت
  در آپدیت‌های بعدی.)

### ظرفیت پخش

- ارسال **دسته‌ای ۲۰تایی موازی** با سقف عمدی ۲۵ پیام در ثانیه.
- صف مقاوم: دیپلوی یا کرش وسط کار، از همان‌جا ادامه می‌دهد.
- کلید `(job_id, tg_user_id)` تضمین می‌کند هیچ‌کس دو بار پیام نمی‌گیرد.
- 🟡 **گلوگاه واقعی duty cycle است:** بودجه‌ی ۱۰ ثانیه در هر تیک ۱۵ دقیقه‌ای.
  با ۵۰ هزار کاربر همچنان کند است. اگر مخاطب بزرگ شد، یا بودجه را بالا ببر یا
  کرون را تندتر کن.

---

## ۶. لاگ‌مانیتورینگ 🆕

`lib/log.ts` — خروجی JSON تک‌خطی به stdout. Railway ایندکسش می‌کند.

```
@evt:deposit.credited     همه‌ی واریزهای موفق
@evt:withdraw.*           کل مسیر برداشت
@level:error              فقط خطاها
@playerId:7               همه‌چیزِ یک کاربر
@ms:>2000                 هر چیزی که کند بوده
```

⚠️ **`message` و `level` نام‌های رزرو Railway هستند.** اگر نام دیگری بگذاری،
Railway خط را پارس می‌کند، متنی پیدا نمی‌کند، و **خط خالی** نشان می‌دهد.

**رویدادهای موجود:** `deposit.credited` · `deposit.skipped` · `deposit.list_failed`
· `withdraw.requested` · `withdraw.submitted` · `withdraw.completed` ·
`withdraw.gateway_rejected` · `withdraw.gateway_failed` · `withdraw.unknown_status`
· `boost.purchased` · `boost.broadcast_failed` · `broadcast.throttled` ·
`webhook.*` · `ratelimit.blocked` · `cron.tick` · `server.unhandled`

`instrumentation.ts` با قلاب `onRequestError` هر خطای سروری را می‌گیرد.

⚠️ **راز لاگ نکن.** لاگر کلیدهای حساس را `[redacted]` می‌کند و `txid` و آدرس
را کوتاه، ولی آن تور آخر است نه مجوز.

---

## ۷. کوئری‌های سلامت — باید همیشه صفر برگردانند

```sql
-- ۱. موجودی (واقعی + دمو) = جمع دفترکل
SELECT count(*) FROM (
  SELECT p.id FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
  GROUP BY p.id, p.usdt_balance, p.demo_balance
  HAVING p.usdt_balance + p.demo_balance <> COALESCE(SUM(w.amount),0)) x;

-- ۲. موجودی دمو = جمع ستون demo دفترکل
SELECT count(*) FROM (
  SELECT p.id FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
  GROUP BY p.id, p.demo_balance
  HAVING p.demo_balance <> COALESCE(SUM(w.demo),0)) x;

-- ۳. استخر بازار = مجموع پیش‌بینی‌ها  ← مهم‌ترین
SELECT count(*) FROM (
  SELECT m.id FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
  GROUP BY m.id, m.yes_total, m.no_total
  HAVING m.yes_total <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0)
      OR m.no_total  <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0)) x;

-- ۴. سهم دمو هرگز از اصل بیشتر نباشد
SELECT count(*) FROM ir_bets WHERE demo_stake > stake;
SELECT count(*) FROM platform_revenue WHERE abs(demo_amount) > abs(amount);

-- ۵. هیچ موجودی منفی نباشد
SELECT count(*) FROM players WHERE usdt_balance < 0 OR demo_balance < 0;
```

⚠️ **اجرای `settle` روی لوکال، واریزهای واقعی درگاه را در دیتابیس محلی
می‌نشاند** (چون کلید Zovix در `.env.local` واقعی است). بی‌خطر است ولی
گیج‌کننده — اگر موجودی محلی ناگهان پر شد، علتش همین است.

---

## ۸. یافته‌های باز

| # | مورد | شدت |
|---|---|---|
| ۱ | **رمز دیتابیس و `SETTLE_KEY` لو رفته‌اند** | 🔴 |
| ۲ | تأیید برداشت در Zovix دستی است — اگر ادمین رد کند، کاربر باید صبر کند تا کرون برگرداند | 🟡 |
| ۳ | کاور در سایت و مینی‌اپ دیده نمی‌شود (فقط تلگرام) | 🟡 |
| ۴ | duty cycle پخش برای ۵۰ هزار کاربر کافی نیست | 🟡 |
| ۵ | آدرس واریز برای همیشه کش می‌شود | 🟡 |
| ۶ | غبار اعشار در تسویه | 🟡 |
| ۷ | ورودی چالش از MOONِ رفرال/هدیه عملاً رایگان می‌شود | 🟡 |
| ۸ | سقف نرخ در حافظه است؛ با هر دیپلوی صفر می‌شود | 🟡 |
| ۹ | هدف چالش پراپ (۳۰۰) شل است — با یک پیش‌بینی در روز، ۱۱.۴٪ بدون مهارت قبول می‌شوند | 🟡 |
| ۱۰ | `TG_ADMIN_IDS` در `.env.local` با آیدی تستی بازنویسی شده | 🟢 |
| ۱۱ | `robots.txt` و `sitemap.xml` وجود ندارند | 🟢 |
| ۱۲ | چندحسابی: سقف ۳ ورود به `tg_user_id` گره خورده | 🟢 |

---

## ۹. 🚀 تمرکز این فاز — لانچ و رشد

**مالک در این فاز بیشتر روی مارکتینگ کار می‌کند تا کد.** انتظار دارد تو در
این سه چیز شریکش باشی:

### الف) رودمپ و ایده‌پردازی

- **۸۰ بازار آماده** در سه فایل تحویل شده (۳۰ اقتصادی/ورزشی، ۳۰ سیاسی،
  ۲۰ ترکیبی). همه با منبع رسمی و تاریخ دقیق.
- ⚠️ **همه را هم‌زمان باز نکن.** ۸۰ بازار روی چند ده کاربر یعنی هر استخر
  خالی. **۶ تا ۸ بازار در روز لانچ.**
- ⚠️ **آستانه‌ها را پیش از باز کردن با قیمت روز بسنج.** بازاری که ۹۰/۱۰ باز
  شود نقدینگی نمی‌گیرد.

### ب) کمپین پیش‌لانچ (سند برندینگ)

مالک یک پروژه‌ی جدا در claude.ai دارد به نام «امپراطوری برندینگ». نسخه‌ی
بازنویسی‌شده‌اش تحویل شده و شامل:
- ساختار **ده نقش** (استراتژی، کپی‌رایتر، سناریونویس، تحلیلگر، منتقد…)
- **کمپین ۷ روزه‌ی پیش‌لانچ**: هر روز یک سؤال بله/خیر دستی در استوری و
  توییتر، بدون بردن اسم نارمون. روز هشتم: «هفت روز دستی این کار را
  می‌کردیم، از امروز یک پلتفرم دارد.»

### ج) واقعیتی که بر همه‌چیز اثر دارد

🔴 **کشور در جنگ است.** از ۹ اسفند ۱۴۰۴ درگیری آمریکا و اسرائیل با ایران.
آتش‌بس شکننده، هرمز بسته، تحریم تازه، دلار ۱۸۷ هزار، تورم سالانه ۶۶٪.

**قاعده‌ی محتوا:** اثر جنگ بر **بازار** بله؛ موضع‌گیری سیاسی، تلفات، حملات و
افراد **نه**. مخاطب در آن کشور زندگی می‌کند.

### مرزی که در محصول نگه داشته شده

مالک صریحاً درخواست بازار سیاسی داد و ساخته شد، **به‌جز**: تلفات، وقوع
حمله‌ی مشخص، و بازداشت افراد. اولی پاداش را به مرگ آدم‌ها گره می‌زند؛ دومی و
سومی کاربر ایرانیِ داخل کشور را با حساب هویت‌دار در معرض ریسک می‌گذارد.

---

## ۱۰. خطوط قرمز محصول (تصمیم قطعی مالک)

- ادعای «سود تضمینی» هرگز. وعده‌ی «از پیش‌بینی پول دربیار» هرگز.
- ایردراپ و توکن نه. یوزر و لیدربورد فیک نه.
- **امتیازدهی صفر-انتظار حفظ شود.**
- استخر parimutuel تتری بازار ایران با تصمیم صریح مالک مجاز است.
- ورود با گوگل و KYC هر دو رد شدند.
- **برداشت هرگز به دلیل بلاک‌بودن ربات قفل نمی‌شود.**
- **پول دمو هرگز قابل برداشت نیست؛ فقط سودش.**
- **تعداد کاربران پلتفرم هرگز عمومی نمی‌شود.**
- واژگان ممنوع: «شرط‌بندی» «قمار» «باخت» «بازنده» «بازی».

---

## ۱۱. دستورها و محیط

```bash
npm run dev      # لوکال روی :3000
npm run build    # بیلد production
npm run lint     # eslint — baseline ۳۳
npx tsc --noEmit # تایپ‌چک
```

⚠️ میدل‌ور در **`src/middleware.ts`** است، نه ریشه.

⚠️ **کلیدهای Zovix و توکن ربات در `.env.local` واقعی‌اند.** هر تستی که به
`createWithdrawal` برسد یک برداشت واقعی می‌سازد.

⚠️ **مینی‌اپ بدون `initData` باز نمی‌شود.** برای دیدن کامپوننت‌ها صفحه‌ی موقت
`src/app/app/zzpreview/page.tsx` بساز با یک `window.Telegram.WebApp` قلابی و
`initData`ی امضاشده با `TG_BOT_TOKEN` — و **پیش از کامیت پاکش کن**.

متغیرها: `DATABASE_URL` `SESSION_SECRET` `ADMIN_SECRET` `ADMIN_PASSWORD`
`PGSSL` `PGPOOL_MAX` `SETTLE_KEY` `LOG_LEVEL` `ZOVIX_*` `TG_BOT_TOKEN`
`TG_BOT_USERNAME` `TG_WEBHOOK_SECRET` `TG_ADMIN_IDS` `SITE_URL`
`GEMINI_API_KEY` `MYFXBOOK_*` `NEXT_PUBLIC_DEV_FIXTURES`

---

## ۱۲. آنچه در فاز ۵ انجام شد (۴۴ کامیت، یک روز)

**مسیر پول از صفر تا صد بسته شد:**

| باگ | چطور پیدا شد |
|---|---|
| 🔴 هیچ واریزی به هیچ حسابی نمی‌نشست (وبهوک در پلن رایگان قفل) | probe از سرور |
| 🔴 دکمه‌ی برداشت هرگز فعال نمی‌شد (گرد کردن رو به بالا) | گزارش مالک |
| 🔴 کارمزد برداشت را استخر می‌داد نه کاربر | اسکرین‌شات پنل درگاه |
| 🔴 برداشتِ ردشده هرگز برنمی‌گشت (سه وضعیت جا افتاده) | مقایسه با مستندات |
| 🔴 کاربر فقط-هدیه اصلا نمی‌توانست پیش‌بینی کند | حین بررسی شکایت دیگر |
| 🔴 تعداد کاربران در روت سلامت عمومی بود | جست‌وجوی نشت |
| 🔴 کاور از مینی‌اپ هرگز ذخیره نمی‌شد | تست واقعی مالک |
| 🔴 لاگ‌ها در Railway خالی دیده می‌شدند | خروجی لاگ مالک |

**قابلیت‌های تازه:** بوست + پنل طلایی سوایپ‌شونده · کاور ۱۶:۹ · «پیش‌بینی‌های
من» · اعلان بازار تازه به ادمین با تأیید/رد در ربات · پشتیبانی در همه‌ی
مسیرهای پولی · منوی کلاسیک رنگی ربات · لاگ ساخت‌یافته.

**پاکسازی دیتابیس:** ۲۶ بازار، ۸۶ شرط، ۱۶۳ ردیف دفترکل و همه‌ی پول دمو پاک
شد. فقط ۳۶.۰۸ تتر واقعی مالک ماند. هر شش سنجش سلامت صفر.

---

## ۱۳. اولین کارهای چت جدید

۱. **از مالک بخواه رمز دیتابیس و `SETTLE_KEY` را عوض کند.** (بلوکه‌کننده)
۲. **بپرس کمپین پیش‌لانچ شروع شده یا نه** و چند بازار باز است.
۳. **لاگ Railway را بخواه** (`@level:error` و `@evt:cron.tick`) و ببین از
   دیپلوی آخر چه اتفاقی افتاده.
۴. **کوئری‌های سلامت را روی پروداکشن بگیر.**
۵. بعد سراغ رودمپ و مارکتینگ برو — تمرکز اصلی این فاز همان است.
