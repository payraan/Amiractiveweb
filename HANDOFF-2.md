# سند تحویل نارمون — فاز ۲: رفع باگ، ممیزی اقتصادی، و تثبیت

> این سند را به‌عنوان اولین پیام چت جدید بفرست. جانشین `HANDOFF-TELEGRAM.md`
> است (آن سند مربوط به فازی بود که تمام شد؛ برای تاریخچه نگهش دار).
>
> **منبع حقیقت نهایی، خودِ ریپو است.** هر جا این سند با کد تناقض داشت، کد را
> ملاک بگیر و تناقض را گزارش کن. این سند حاصل خواندن کد است، ولی کد بعد از
> نوشتنش تغییر می‌کند.

---

## ۰. نقش تو و سبک کار

تو Principal Engineer این پروژه‌ای. مالک برنامه‌نویس نیست و در سطح کپی‌پیست و
اجرای دستور ترمینال کار می‌کند.

- **پاسخ‌ها به فارسی.**
- **قبل از هر کامیت:** `npx tsc --noEmit`؛ در تغییرات بزرگ `npm run build`.
- **کد را با داده‌ی واقعی تست کن، نه فقط کامپایل.** روال ثابت این پروژه: هر
  مسیر پولی با `curl` و `psql` روی دیتابیس محلی سرتاسر تست شود (سناریوی موفق
  + سناریوهای سوءاستفاده)، بعد کامیت.
- **داده‌ی تستی را بعد از تست از دیتابیس پاک کن.**
- **پوش:** مالک اجازه‌ی پایدار داده («نیاز نیست سؤال بپرسی»)، ولی هر پوش را
  گزارش کن و پس از دیپلوی سلامت پروداکشن را بسنج.
- هر تغییر را قبل از اعمال با یکی‌دو جمله‌ی ساده‌ی فارسی توضیح بده.
- هیچ کلید/توکنی هاردکد نشود؛ `.env.local` در گیت‌ایگنور بماند.
- اگر چیزی با این سند نمی‌خواند، **اول گزارش بده**، خودسرانه بازنویسی نکن.

---

## ۱. پلتفرم چیست

**نارمون** — پلتفرم فارسی‌زبان «هوش جمعی و بازارهای پیش‌بینی».

- ریپو: `https://github.com/payraan/Amiractiveweb` (برنچ `main`)
- دیپلوی: Railway — `https://amiractiveweb-production.up.railway.app`
  (دامنه‌ی اصلی هنوز وصل نشده). **auto-deploy سالم است**؛ هر پوش حدود یک تا دو
  دقیقه بعد لایو می‌شود.
- استک: Next.js 16 App Router، React 19، Tailwind v4، TypeScript، PostgreSQL
  با `pg` خام (بدون ORM). مسیر `@/*` → `src/*`.
- زبان: فارسی، RTL. کامنت‌های کد فارسی‌اند.
- ربات: `@NarmoonMarketBot` (آیدی `8920077430`)، مینی‌اپ روی `/app`.

**تز محصول:** امتیاز فقط از مهارت می‌آید، نه شانس و نه پول. واحد داخلی `MOON`
است و **فقط قابلیت باز می‌کند؛ هرگز امتیاز یا رتبه نمی‌خرد.**

---

## ۲. وضعیت فعلی — چه چیزی تمام است

### سایت (تقریبا کامل)

صفحه‌ها: `/` `/trade` `/predict` `/iran` `/iran/propose` `/iran/m/[id]`
`/wallet` `/profile` `/challenge` `/leaderboard` `/legal` `/login` `/referral`
`/results` `/bot` `/broker` `/admin` `/app` (+ `/arena` و `/combos` که ریدایرکت‌اند)

### ربات و مینی‌اپ (کامل و زنده)

- **وبهوک داخل همان اپ** (`/api/tg/webhook`)، نه پروسه‌ی جدا. تنها دروازه‌اش
  هدر `X-Telegram-Bot-Api-Secret-Token` است با مقایسه‌ی زمان‌ثابت و fail-closed.
- **ورود بدون رمز با `initData`** (`/api/tg/auth`) — الگوریتم رسمی مینی‌اپ:
  `secret = HMAC_SHA256("WebAppData", token)`. ⚠️ با الگوریتم Login Widget فرق
  دارد. `auth_date` بیش از ۲۴ ساعت رد می‌شود.
- **مینی‌اپ چهار تبی:** بازار ایران (فهرست، فیلتر، مرتب‌سازی، ساخت بازار،
  صفحه‌ی بازار، ثبت شرط)، ترید (پالی‌مارکت با امتیاز پیش از انتخاب)، کیف پول
  (واریز/برداشت/خرید MOON/تاریخچه)، پروفایل (رتبه، سود و زیان، ۲۳ نشان).
- **نظرسنجی کانال** در دو حالت: `forward` (دکمه‌های لینک، بدون نیاز به ادمین،
  درصد یخ‌زده و برچسب‌دار) و `live` (دکمه‌های callback، رأی درجا، آپدیت خودکار
  روی کرون، نیازمند ادمین‌بودن ربات).
- **Deep link:** `startapp=market_42_yes` مستقیم همان بازار را با طرف انتخاب‌شده
  باز می‌کند.

### زیرساخت مشترک

- `currentPlayerId()` — یک شناسه‌ی هویت برای هر دو سطح (کوکی سایت یا هدر
  `x-tg-auth` مینی‌اپ). **هیچ روت اختصاصی مینی‌اپی وجود ندارد**؛ هر ۱۸ روت
  موجود هر دو را می‌پذیرند.
- `requireLinkedTelegram()` روی چهار مسیر پولی: شرط، ساخت بازار، برداشت،
  خرید MOON. آدرس واریز هم فقط برای حساب متصل داده می‌شود.
- `frame-ancestors` فقط تلگرام وب + `nosniff` + `Referrer-Policy`.

---

## ۳. اقتصاد واقعی — اعداد قفل‌شده

**قبل از تغییر هرکدام از مالک بپرس.**

### بازار ایران (تنها بخش با پول واقعی) — `src/lib/iran.ts`

```
COMMISSION        = 0.03    // ۳٪ از حجم استخر، نه از سود
MIN_STAKE_USDT    = 1
MIN_ODDS          = 1.05    // زیر این، بازار باطل می‌شود
DISPUTE_HOURS     = 24
PROPOSE_FEE_USDT  = 1       // اگر بازار رد شود کامل برمی‌گردد
MIN_WITHDRAW      = 10      // ⚠️ در دو جا هاردکد است، بخش ۵ را ببین
```

`ضریب برد = (کل استخر × ۰.۹۷) ÷ مجموع شرط‌های طرف برنده`

سه مسیر تسویه در `settleIrMarket`: عادی، باطل به‌خاطر ضریب پایین (برگشت کامل
بدون کمیسیون)، بدون برنده (برگشت با کسر کمیسیون).

### امتیازی

```
WELCOME_CREDITS    = 10     // game.ts
POLY_FREE_PER_DAY  = 5      // poly.ts
POLY_EXTRA_COST    = 1      // poly.ts
REFERRAL_PERCENT   = 10     // referral.ts ⚠️ سند قبلی این را ۲٪ نوشته بود
REFERRAL_BONUS     = 5      // referral.ts — MOON هدیه به دعوت‌شده
REF_VOL_PCT        = 2.0    // game.ts — ⚠️ این *پورسانت نیست*
```

> **تصحیح مهم نسبت به سند قبلی:** `REF_VOL_PCT` در `game.ts` **مرجع نوسان**
> برای مقیاس‌کردن آستانه‌های نبض بازار است (`σ(tf) = REF_VOL_PCT × √(ساعت/۲۴)`)،
> نه پورسانت رفرال. سند قبلی آن را «پورسانت رفرال ۲٪» معرفی کرده بود که
> **غلط است**. پورسانت واقعی رفرال `REFERRAL_PERCENT = 10` است — پنج برابر
> چیزی که سند می‌گفت.

امتیاز ترید صفر-انتظار: `winPoints = max(1, round(100 − احتمال٪))`،
`losePoints = −max(1, round(احتمال٪))` — در `src/lib/poly-scoring.ts` (بدون
وابستگی، قابل import در کلاینت). `poly.ts` همان را import و re-export می‌کند.

### چلنج پراپ — `src/lib/challenge.ts`

```
CONSISTENCY_PCT     = 35
ELIGIBLE_PROB_MIN   = 0.25
ELIGIBLE_PROB_MAX   = 0.75
MAX_COUNTED_PREDS   = 50
MAX_ENTRIES_PER_30D = 3      // فقط مسیر حساب پیش‌بینی
```

هفت تیر: `fx250/fx500/fx1k` (حساب معاملاتی) و `pr5k/pr10k/pr25k/pr50k`.

---

## ۴. معماری

### دیتابیس

هیچ فریم‌ورک مهاجرتی نیست. `db()` یک بلوک SQL idempotent را یک‌بار در هر cold
start اجرا می‌کند. ماژول‌ها جدول‌های خودشان را همین‌طور می‌سازند:
`ensureIrTables()`، `ensureTelegramTables()`، `ensureChallengeTables()`،
`ensureReferralTables()`، `ensurePolyTables()`، `ensureTopupTable()`.

**۱۸ جدول:** `players`, `rounds`, `predictions`, `poly_predictions`,
`combo_tickets`, `combo_legs`, `player_challenges`, `credit_topups`,
`referral_earnings`, `tg_link_codes`, `wallet_ledger`, `ir_markets`, `ir_bets`,
`ir_disputes`, `ir_market_posts`, `platform_revenue`, `gateway_deposits`,
`withdrawals`.

⚠️ `gateway_deposits` و `withdrawals` **تنبل‌اند** و فقط با اولین وبهوک/برداشت
ساخته می‌شوند. روی محیط تازه ممکن است وجود نداشته باشند — موقع کوئری‌زدن روی
آن‌ها فرض نکن هستند.

### هویت — سه ستون، سه نقش

| ستون | معنی | اثبات‌شده؟ | یکتا؟ |
|---|---|---|---|
| `tg_user_id` | **لنگر هویت** | ✅ | بله (ایندکس شرطی) |
| `tg_username` | کلید ورود با رمز، nullable | ❌ | بله، شرطی |
| `tg_handle` | هندل فعلی تلگرام، برچسب | ✅ | نه |

`password_hash` هم nullable است (حساب تلگرام‌زاد رمز ندارد). مسیر ورود با رمز،
حساب بدون رمز را با `telegram_account` رد می‌کند.

**قاعده‌ی تصادم:** `tg_user_id` همیشه برنده. حساب قدیمیِ صاحبِ همان هندل نه
تغییر نام می‌دهد، نه ادغام می‌شود. ادغام فقط دستی از پنل ادمین.

### قواعد پولی (تغییر نده)

- کسر پول همیشه داخل ترنزاکشن با `SELECT … FOR UPDATE` روی ردیف بازیکن.
- `moveFunds(client, playerId, amount, kind, ref)` تنها راه تغییر موجودی است.
- `recordRevenue(client, kind, amount, opts)` — `is_demo` را خودکار از حساب
  پرداخت‌کننده می‌خواند.
- **پول واقعی فقط از وبهوک درگاه می‌آید. هر شارژ دستی ادمین یعنی حساب تستی.**

---

## ۵. ⚠️ یافته‌های ممیزی — به ترتیب اولویت

### A. زنجیره‌ی فارم MOON از راه رفرال (بلندترین ریسک باز)

`payReferralCommission` هنگام خرید MOON، **۱۰٪ از MOON بسته را از هوا به
دعوت‌کننده می‌دهد** — از کسی کم نمی‌شود، ساخته می‌شود.

خود-ارجاعی مستقیم بسته است (`referrerId === newPlayerId`)، ولی زنجیره‌ی زیر باز
است:

1. حساب A را در **سایت** بساز — ثبت‌نام وب هیچ اعتبارسنجی تلگرامی ندارد
2. حساب B را با کد دعوت A بساز و تلگرام واقعی‌ات را به B وصل کن
3. B با تتر واقعی MOON بخرد → **A ده درصد MOON مجانی می‌گیرد**
4. A هرگز لازم نیست تلگرام وصل کند، چون `requireLinkedTelegram` فقط روی
   شرط/ساخت‌بازار/برداشت/خرید است — **نه روی دریافت پورسانت، نه روی ورود به
   چلنج، نه روی ثبت پیش‌بینی**

**چرا خطرناک است:** MOON خودش امتیاز نمی‌خرد (امتیازدهی صفر-انتظار است و این
محافظ سالم است)، ولی **ورودی چلنج پراپ با MOON پرداخت می‌شود و جایزه‌اش حساب
واقعی است**. یعنی مسیر «MOON مجانی ← ورود بیشتر به چلنج ← جایزه‌ی واقعی» باز
است.

**پیشنهاد رفع (به ترتیب اثر):**
1. `requireLinkedTelegram` روی `enterChallenge` — ارزان‌ترین و مؤثرترین
2. پورسانت رفرال فقط وقتی پرداخت شود که **دعوت‌کننده هم** تلگرام وصل داشته باشد
3. سقف ماهانه روی مجموع پورسانت هر حساب

### B. ثبت‌نام سایت هنوز هیچ اثباتی ندارد

`/api/predict/auth` فقط چک می‌کند یوزرنیم `[a-z0-9_]{3,}` باشد. هرکسی می‌تواند
با هندل هر شخص دیگری ثبت‌نام کند. مینی‌اپ این را حل کرده (initData امضاشده)،
ولی مسیر سایت باز است.

تصمیم مالک: فعلا هر دو مسیر بمانند. گزینه‌ی نهایی (حذف کامل ثبت‌نام با رمز و
یک‌کاسه‌کردن روی مینی‌اپ) **هنوز گرفته نشده** — از مالک بپرس.

⚠️ رابط هنوز `tg_username` را جایی نشان می‌دهد که کاربر آن را «هویت تلگرام»
می‌فهمد. در مینی‌اپ کنارش تیک تأیید گذاشته شده، ولی در پنل ادمین و سایت هنوز
تفکیک نشده.

### C. کرون تسویه هنوز ست نشده (کار مالک)

`/api/predict/settle` هر سه بازی + آپدیت کارت‌های کانال را انجام می‌دهد و آماده
است، ولی **هیچ‌کس صدایش نمی‌زند**. تسویه فقط با باز شدن صفحه اتفاق می‌افتد.

سرویس Cron در Railway، زمان‌بندی `*/15 * * * *`:
```
curl -fsS -X POST https://amiractiveweb-production.up.railway.app/api/predict/settle -H "x-settle-key: $SETTLE_KEY"
```

**تا این ست نشود، هیچ آپدیت خودکاری روی کارت‌های کانال هم اتفاق نمی‌افتد.**

### D. `MIN_WITHDRAW` در دو جا هاردکد است

`src/app/api/wallet/withdraw/route.ts:13` و
`src/components/tg/screens/WithdrawScreen.tsx` و `src/components/wallet/WalletPanel.tsx`.
اگر یکی عوض شود و دیگری نه، UI مبلغی را می‌پذیرد که سرور رد می‌کند. باید به
`iran.ts` منتقل شود کنار بقیه‌ی ثابت‌های اقتصادی.

### E. rate limit فقط روی سه مسیر

`src/middleware.ts` فقط `admin-login`، `auth` و `admin` را پوشش می‌دهد. مسیرهای
پولی (`/api/ir/bet`، `/api/wallet/withdraw`، `/api/ir/propose`) هیچ سقفی ندارند.
درون‌حافظه‌ای هم هست و با هر دیپلوی ریست می‌شود.

### F. برداشت گیرکرده بازیابی خودکار ندارد

`getWithdrawal` در `zovix.ts` نوشته شده ولی **هیچ‌جا صدا زده نمی‌شود**. اگر
برداشتی در درگاه بماند، وضعیتش در `withdrawals` روی `submitted` یخ می‌زند.

### G. آدرس برداشت فقط از نظر طول اعتبارسنجی می‌شود

`toAddress.length >= 20` — نه فرمت TRON. آدرس بی‌معنی ولی به‌اندازه‌کافی بلند رد
نمی‌شود و پول به درگاه می‌رود.

### H. بازار ایران تاریخچه‌ی احتمال ندارد

اسنپ‌شات دوره‌ای ذخیره نمی‌شود، پس نمودار زمانی و «موج پیش‌بینی» ممکن نیست.
`ir_market_posts.last_yes_pct` فقط آخرین مقدار را برای جلوگیری از ویرایش
بی‌تغییر نگه می‌دارد، تاریخچه نیست.

### I. ~۳۲ خطای lint از قبل موجود

`react-hooks/set-state-in-effect` روی هر کامپوننتی که با `useEffect` دیتا
می‌گیرد. **این عدد baseline است** — قبل از هر کامیت مقایسه کن که بالا نرفته
باشد. در مینی‌اپ این الگو در `useResource` متمرکز شده با یک استثنای مستند.

---

## ۶. چیزهایی که سالم‌اند (تأییدشده با تست)

- **قفل ردیف:** هر چهار مسیر خرج‌کننده (`poly-submit`, `predict/submit`,
  `combo-submit`, `startChallenge`) ردیف بازیکن را با `FOR UPDATE` قفل می‌کنند
  **قبل** از شمارش و کسر. سهمیه‌ی رایگان روزانه‌ی ترید مسابقه‌پذیر نیست.
- **قیدهای یکتای دیتابیس:** `poly_predictions(market_id, player_id)`،
  `predictions(round_id, player_id)`، `player_challenges → one_active_challenge`،
  `ir_disputes(market_id, player_id)`، `withdrawals.unique_param`،
  `players.tg_user_id` (شرطی)، `ir_market_posts(chat_id, message_id)`.
- **وبهوک واریز:** سه لایه — توکن در مسیر، تأیید متقابل با API درگاه (مبلغ از
  API خوانده می‌شود نه از بدنه‌ی وبهوک)، ایندکس یکتا روی txid.
- **تسویه‌ی بازار ایران:** قفل با اعتراض باز، پنجره‌ی ۲۴ ساعته، کمیسیون از روی
  پرداخت واقعی حساب می‌شود نه فرمول جدا.
- **موجودی و دفترکل:** در همه‌ی تست‌ها سازگار ماندند.

---

## ۷. کارهای مالک (بیرون از کد)

1. **کرون Railway** — بخش ۵.C
2. **ربات را در کانال ادمین کن** تا حالت `live` کار کند
3. **اولین برداشت واقعی با مبلغ کم** — تنها مسیری که عمدا تست نشده، چون درگاه
   با کلیدهای واقعی کار می‌کند و تست، پول واقعی جابه‌جا می‌کرد
4. **نام گیت** — همه‌ی کامیت‌ها با `نام شما <ایمیل شما>` ثبت شده‌اند
5. `MYFXBOOK_ACCOUNT_ID` روی Railway ست نیست؛ پروداکشن از پیش‌فرض هاردکد
   `"12121235"` استفاده می‌کند — **آیا این آیدی واقعی است؟**

---

## ۸. دستور کار چت جدید

مالک صریح گفته: **«می‌خواهم خودت تبدیل شوی به ده‌ها یوزر جدید، تک‌تک بازارها را
چک کنی. نمی‌خواهم باگی در امتیازبندی و پراپ باشد که فارم شویم.»**

پیشنهاد ترتیب:

1. **این سند را با کد راستی‌آزمایی کن.** فرض نکن درست است.
2. **شبیه‌سازی چند-کاربره روی دیتابیس محلی:** ۱۰ تا ۲۰ حساب بساز، چرخه‌ی کامل
   بزن (ثبت‌نام → واریز → شرط → تسویه → برداشت)، و بعد **ناسازگاری موجودی با
   دفترکل** را کوئری بگیر. الگوی تست در بخش ۰.
3. **بستن زنجیره‌ی فارم بخش ۵.A** — با تأیید مالک، چون تغییر اقتصادی است.
4. باگ‌های UI که مالک جداگانه می‌گوید.
5. موارد D تا G بخش ۵.

**کوئری‌های سلامت که باید همیشه صفر برگردانند:**

```sql
-- موجودی در برابر دفترکل
SELECT p.id, p.usdt_balance, COALESCE(SUM(w.amount),0) AS ledger
FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
GROUP BY p.id, p.usdt_balance
HAVING p.usdt_balance <> COALESCE(SUM(w.amount),0);

-- ردیف یتیم
SELECT count(*) FROM wallet_ledger w
LEFT JOIN players p ON p.id=w.player_id WHERE p.id IS NULL;

-- استخر بازار در برابر مجموع شرط‌ها
SELECT m.id, m.yes_total, m.no_total,
       COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0) AS real_yes,
       COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0)  AS real_no
FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
GROUP BY m.id, m.yes_total, m.no_total
HAVING m.yes_total <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0)
    OR m.no_total  <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0);
```

⚠️ کوئری سوم مهم‌ترینشان است: اگر `yes_total`/`no_total` با مجموع واقعی
`ir_bets` نخواند، **ضریب و پرداخت همه‌ی بازارها غلط است** — چون `oddsFor` از
ستون‌های تجمیعی می‌خواند نه از جمع‌زدن شرط‌ها.

روی دیتابیس **لوکال** اجرایش کردم و صفر برگرداند، ولی آنجا فقط **یک بازار**
هست، پس این تقریبا هیچ چیزی را ثابت نمی‌کند. **روی پروداکشن اجرا نشده** — من
به دیتابیس پروداکشن دسترسی ندارم. اولین کار چت جدید باید این باشد، و بعد از
شبیه‌سازی چند-کاربره‌ی بند ۲ دوباره تکرارش کند.

---

## ۹. خطوط قرمز محصول (تصمیم قطعی مالک — تغییر نده)

- ادعای «سود تضمینی» هرگز.
- وعده‌ی «از پیش‌بینی پول دربیار» در پیام برند هرگز.
- ایردراپ و توکن نه. یوزر و لیدربورد فیک نه.
- امتیازدهی صفر-انتظار حفظ شود: حدس کور به‌طور میانگین منفی می‌گیرد.
- استخر parimutuel تتری بازار ایران با تصمیم صریح مالک مجاز است؛ مکانیزم نقدی
  مشابه برای بازی‌های امتیازی ساخته نمی‌شود.
- ورود با گوگل و KYC هر دو **رد شدند** (تحلیل: گوگل سقف ضدتقلب را پایین می‌آورد
  چون رایگان و بی‌نهایت است؛ KYC روی کاربر ایرانی زنجیره‌ی مدرک می‌سازد).

---

## ۱۰. دستورها

```bash
npm run dev      # لوکال روی :3000
npm run build    # بیلد production
npm run lint     # eslint — baseline ۳۲ مورد
npx tsc --noEmit # تایپ‌چک
```

دیتابیس لوکال: `postgresql://amiractive@localhost:5432/narmoon`

متغیرهای محیطی: `DATABASE_URL`, `SESSION_SECRET` (اجباری), `ADMIN_SECRET`,
`ADMIN_PASSWORD`, `PGSSL`, `SETTLE_KEY`, `ZOVIX_*`,
`TG_BOT_TOKEN`, `TG_BOT_USERNAME`, `TG_WEBHOOK_SECRET`, `SITE_URL`,
`MYFXBOOK_*`.

⚠️ نام‌های قدیمی `BOT_TOKEN`/`BOT_USERNAME`/`BOT_API_KEY` **حذف شده‌اند** و به
ربات پشتیبانِ بازنشسته اشاره می‌کردند. اگر هنوز در Railway مانده‌اند، بی‌اثرند.
