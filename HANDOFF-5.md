# سند تحویل نارمون — فاز ۵: بستن حلقه‌ی پول، سپس لانچ

> این سند را به‌عنوان اولین پیام چت جدید بفرست. جانشین `HANDOFF-4.md` است
> (آن را برای تاریخچه نگه دار).
>
> **منبع حقیقت نهایی، خودِ ریپو است.** هر جا این سند با کد تناقض داشت، کد را
> ملاک بگیر و تناقض را گزارش کن.
>
> 🔴 **اولویت مطلق: واریز ۲۰ تتری که به پنل Zovix رسیده ولی به پلتفرم نرسیده.**
> تا این حل نشود، لانچ بی‌معنی است — چون هیچ کاربری نمی‌تواند پول بگذارد.
>
> آخرین به‌روزرسانی: ۲۰۲۶/۰۸/۱۵

---

## ۰. نقش تو و سبک کار

تو Principal Engineer این پروژه‌ای. مالک برنامه‌نویس نیست و در سطح کپی‌پیست و
اجرای دستور ترمینال کار می‌کند.

- **پاسخ‌ها به فارسی.**
- **قبل از هر کامیت:** `npx tsc --noEmit`؛ در تغییرات بزرگ `npm run build`.
- **لینت baseline = ۳۲ مورد** (۲۷ خطا + ۵ هشدار). بالا نرود. خطای تازه را
  **رفع کن، سرکوب نکن**.
- **کد را با داده‌ی واقعی تست کن، نه فقط کامپایل.** هر مسیر پولی با `curl` و
  `psql` روی دیتابیس **محلی** سرتاسر تست شود (سناریوی موفق + سوءاستفاده)،
  بعد کامیت. **داده‌ی تستی را بعد از تست پاک کن.**
- **در مرورگر ببین.** برای هر تغییر دیداری، صفحه را در عرض ۳۷۵px باز کن.
- **پوش:** مالک اجازه‌ی پایدار داده؛ هر پوش را گزارش کن و پس از دیپلوی
  (~۱ تا ۲ دقیقه) سلامت پروداکشن را بسنج.
- هر تغییر را پیش از اعمال با یکی‌دو جمله‌ی ساده‌ی فارسی توضیح بده.
- **هر جا تصمیم سلیقه‌ای یا اقتصادی بود، از مالک بپرس.**
- اگر چیزی با این سند نمی‌خواند، **اول گزارش بده**، خودسرانه بازنویسی نکن.

### دسترسی‌ها

```bash
# دیتابیس محلی
postgresql://amiractive@localhost:5432/narmoon

# دیتابیس پروداکشن (مالک اجازه داده — فقط SELECT)
postgresql://postgres:<PASS>@tokaido.proxy.rlwy.net:15526/railway
```

⚠️ **رمز پروداکشن در چت قبلی لو رفته و باید عوض شود.** رشته‌ی تازه را از
مالک بگیر. **هرگز در `.env.local` نگذار.** فقط `SELECT`؛ هر نوشتنی را اول
به مالک نشان بده و تأیید بگیر.

### درس‌های گران (تکرارشان نکن)

1. **تعداد را با مقدار اشتباه نگیر.** «۱۰ دارایی ۷۲۵ کندل دادند» — همه
   بیت‌کوین بودند.
2. **پیش از اعلام «انجام شد»، در مرورگر ببین.** `grep` جای دیدن را نمی‌گیرد:
   متن «سقف اعلام‌شده» در صفحه‌ی چالش فقط با باز کردن صفحه پیدا شد.
3. **پنل خالی = پنل نامرئی.** هر فهرستی حالت خالی داشته باشد.
4. **کامپایل شدن یعنی کد ساخته شد، نه اینکه اجرا می‌شود.**
5. **جای‌گزینی متنی روی JSX نیمی از کار را بی‌صدا جا می‌گذارد.** بعد از هر
   تغییر تکرارشونده، **همه‌ی** جایگاه‌ها را بشمار.
6. **خطا را نبلع.** شمارنده‌ی بدون علت از خودِ مشکل بدتر است.
7. 🆕 **جست‌وجوی خراب، «پاک است» نیست.** `grep` فارسی در zsh صفر می‌داد —
   حتی برای واژه‌هایی که قطعاً وجود داشتند. **همیشه اول با واژه‌ای که
   می‌دانی هست صحت‌سنجی کن.** برای فارسی از پایتون استفاده کن، نه grep.
8. 🆕 **۲۰۰ گرفتن یعنی سرور نمرد، نه اینکه کار درست انجام شد.** وبهوک تلگرام
   **همیشه** ۲۰۰ می‌دهد. برای دیدن رفتار واقعی، ابزار موقت بگذار و بردار.
9. 🆕 **راه‌حل را به‌اندازه‌ی مسئله بررسی کن.** ممیز قفل‌نداشتن تسویه را درست
   دید ولی راه‌حلش بن‌بست ABBA می‌ساخت. اگر بی‌بررسی اعمال می‌شد، تسویه‌ها
   می‌شکستند.
10. 🆕 **نتیجه‌ی شبیه‌سازی به پارامترهای ناگفته حساس است.** نرخ قبولی چالش با
    «چند پیش‌بینی در روز» از ۰.۰۱٪ تا ۱۱.۸٪ نوسان می‌کند. هر عددی که
    گزارش می‌دهی، فرض‌هایش را هم بنویس.
11. 🆕 **Postgres کل دستور را پیش از اجرا تجزیه می‌کند.** ارجاع به جدول
    ناموجود داخل `CASE` هم خطا می‌دهد، حتی اگر آن شاخه اجرا نشود.

---

## ۱. 🔴 مسئله‌ی شماره‌ی یک: واریز به پلتفرم نمی‌رسد

### آنچه می‌دانیم (شاهد دارد)

| شاهد | وضعیت |
|---|---|
| پنل Zovix موجودی ۲۰ USDT نشان می‌دهد | ✅ اسکرین‌شات مالک |
| تراکنش در پنل: `INTERNA...af6d451` · TRON · **SUCCESS** · Fee 0.00 | ✅ |
| موجودی پلتفرم | ❌ صفر |
| بلاک‌چین TRON روی آدرس `TKZgMiTjFDF9yDm7P2qYTLyE37nrffNQnx` | **صفر تراکنش** (TronGrid + Tronscan، هر دو) |
| `GET /my-blockchain/deposit/index` با کلید ما | `200` با `data: []` و `total_records: 0` |
| همان با `?txid=INTERNAL-9be378bbf8315b3f27fc8cdabaf6d451` | `200` خالی |
| `GET /deposit/index` (بدون پیشوند) | `404 route not found` |
| `GET /my-blockchain/address/index` | `403 api key lacks the required ability` |
| `POST /my-blockchain/address/get` | ✅ کار می‌کند، آدرس ثابت می‌دهد |

مدیر Zovix گفته «پول منتقل شده و مشکل از سمت شماست».

### فرضیه‌ی اصلی (تأیید نشده)

**انتقال «داخلی» کیف پروژه را شارژ می‌کند ولی رکورد `deposit` نمی‌سازد.**

چون:
- هیچ تراکنشی روی زنجیره منتشر نشده (`INTERNAL-` پیشوند خودِ فرستنده است)
- `deposit/index` خالی است — یعنی Zovix این را «واریز» نمی‌داند
- وبهوک `DEPOSIT_DONE` هم احتمالاً هرگز شلیک نشده

اگر درست باشد، **کد ما هیچ اشکالی ندارد** — هیچ نقطه‌ای از زنجیره فرصت
اجرا پیدا نکرده.

### فرضیه‌ی دوم (باید رد یا تأیید شود)

**کلید API به پروژه‌ی دیگری تعلق دارد.** آدرس پروژه در اسکرین‌شات:
`dashboard.zovix.io/project/a27454b3-ee1e-483e-834b-af1be5ecc183`.
اگر `ZOVIX_API_KEY` مال پروژه‌ی دیگری باشد، همه‌ی نشانه‌ها همین‌طور می‌شوند.

### فرضیه‌ی سوم

**وبهوک شلیک شده ولی ما بی‌صدا ردش کرده‌ایم.** در
`src/app/api/wallet/webhook/route.ts`، پس از دریافت وبهوک، کد **از خود
درگاه** می‌پرسد که این `txid` واقعی است (`verifyDeposit`). اگر آن پرسش خالی
برگردد — که الان برمی‌گردد — کد `{ok: true, ignored: "not_confirmed"}`
می‌دهد و **هیچ لاگی نمی‌گذارد**. یعنی اگر وبهوک آمده باشد، بی‌سروصدا دور
ریخته شده.

### کارهای اولین ساعتِ چت جدید

۱. **از مالک بپرس آیا لاگ Railway نشان می‌دهد وبهوکی به
   `/api/wallet/webhook` رسیده یا نه** (تاریخ ۱۴ اوت ۲۰۲۶ حوالی ۲۱:۰۶).
   این فرضیه‌ی سوم را قطعی می‌کند.

۲. **بررسی کن `ZOVIX_WEBHOOK_TOKEN` در Railway ست است یا نه.** اگر نباشد،
   `webhookTokenValid` همه‌ی وبهوک‌ها را با ۴۰۱ رد می‌کند — fail-closed و
   بی‌صدا. این ساده‌ترین توضیح ممکن است و اول باید ردش کنی.

۳. **بررسی کن آدرس وبهوک در پنل Zovix ثبت شده و درست است**
   (`https://amiractiveweb-production.up.railway.app/api/wallet/webhook?t=<TOKEN>`).

۴. **لاگ‌گذاری روی وبهوک اضافه کن** — الان مسیرهای «نادیده گرفتم» هیچ ردی
   نمی‌گذارند. هر شاخه‌ی `ignored` باید در دیتابیس یا لاگ ثبت شود. این
   مستقل از علت، لازم است: بدون آن، دفعه‌ی بعد هم کور می‌مانیم.

۵. **این سؤال‌ها را برای مدیر Zovix آماده کن:**
   - آیا انتقال داخلی (`INTERNAL-…`) رویداد `DEPOSIT_DONE` می‌فرستد؟
   - چرا `GET /my-blockchain/deposit/index` با کلید ما خالی برمی‌گردد در
     حالی که پنل تراکنش را نشان می‌دهد؟ آیا این endpoint فقط واریزهای
     on-chain را برمی‌گرداند؟
   - کلید API ما به کدام `project_id` تعلق دارد؟ همان
     `a27454b3-ee1e-483e-834b-af1be5ecc183` است؟
   - `address/index` می‌گوید «api key lacks the required ability» — کدام
     دسترسی‌ها روی کلید ما فعال است؟
   - آیا آدرس یک `client_id` **هرگز** عوض می‌شود؟ (ما آدرس را کش کرده‌ایم.)

۶. **تا حل شدن، هشدار «انتقال داخلی ممنوع» در هر سه سطح هست** — نگهش دار.

---

## ۲. پلتفرم چیست

**نارمون** — پلتفرم فارسی‌زبان بازار پیش‌بینی.

- ریپو: `https://github.com/payraan/Amiractiveweb` (برنچ `main`)
- دیپلوی: Railway — `https://amiractiveweb-production.up.railway.app`
- ربات: `@NarmoonMarketBot` · مینی‌اپ روی `/app`
- استک: Next.js 16 App Router، React 19، Tailwind v4، TypeScript،
  PostgreSQL با `pg` خام (بدون ORM). مسیر `@/*` → `src/*`. بدون تست خودکار.

**تز محصول:** امتیاز فقط از مهارت می‌آید. `MOON` فقط قابلیت باز می‌کند و
**هرگز امتیاز یا رتبه نمی‌خرد.**

### سه سطح، یک دیتابیس

| سطح | مسیر | احراز هویت |
|---|---|---|
| سایت | `/` و بقیه | کوکی `amir_session` |
| مینی‌اپ | `/app` | هدر `x-tg-auth` |
| ربات | وبهوک `/api/tg/webhook` | `tg_user_id` |

`currentPlayerId()` هر سه را به یک شناسه می‌رساند. **هیچ روت اختصاصی
مینی‌اپی وجود ندارد** و نباید ساخته شود. از ۲۲ روت هویت‌دار، همه از این یک
تابع می‌گذرند — این با آزمون سنجیده شده.

### قواعد پولی — تغییر نده

- کسر پول **همیشه** داخل ترنزاکشن با `SELECT … FOR UPDATE`.
- **ترتیب قفل سراسری: اول بازار، بعد بازیکن.** برعکسش بن‌بست ABBA می‌سازد.
- `moveFunds()` تنها راه تغییر موجودی. مقدار **مطلق** می‌نویسد نه دلتا، پس
  ردیف باید از قبل قفل شده باشد.
- `recordRevenue()` برای درآمد، همیشه با `demoAmount`.
- **پول واقعی فقط از وبهوک درگاه می‌آید.**
- همه‌ی دیتای upstream سمت سرور پروکسی می‌شود.
- **میدل‌ور در `src/middleware.ts` است، نه ریشه.**
- `total_points` از نوع `NUMERIC(14,4)` — هر `SELECT` باید `ROUND(...)::int`
  بزند وگرنه `pg` **رشته** می‌دهد.

### پول دمو (اقتصاد دوم، مصوب مالک)

- بونوس در `players.demo_balance`، پول واقعی در `usdt_balance`.
- **خرج همیشه اول از دمو** — روی شرط، خرید MOON، کارمزد ساخت بازار.
- **برگشت اصلِ پول به دمو، سود به واقعی.** کاربری که با بونوس ۱۰۰ برد و
  ۱۳۷ گرفت: ۱۰۰ دمو می‌ماند، ۳۷ واقعی و قابل برداشت.
- **برداشت فقط `usdt_balance` را می‌بیند** (پرچم `realOnly` در `moveFunds`).
- `ir_bets.demo_stake` و `platform_revenue.demo_amount` سهم دمو را نگه
  می‌دارند. پنل درآمد **مبلغ** را تفکیک می‌کند نه ردیف را.
- **مهاجرت یک‌باره:** تا وقتی هیچ واریز درگاهی ثبت نشده، هر موجودی دمو
  حساب می‌شود. با اولین واریز واقعی برای همیشه خاموش می‌شود.

⚠️ **این یعنی وقتی مسئله‌ی بند ۱ حل شود و اولین واریز واقعی بنشیند،
مهاجرت خاموش می‌شود.** پیش از آن، اگر می‌خواهی بازار و نقدینگی دموی بیشتری
بسازی، همان موقع است.

### لحن — `src/lib/tone.ts`

**مسیر پول رسمی («شما»)، بقیه خودمانی («تو»).** مرز روی **موضوع** است نه
فایل: یک فایل می‌تواند هر دو را داشته باشد.

⚠️ صفحه‌های بازاریابی سایت (`Faq.tsx`, `config/site.ts`, سکشن‌های لندینگ)
هنوز رسمی مانده‌اند — حدود ۱۲ فایل، اولویت پایین.

### واژگان ممنوع

«شرط‌بندی»، «باخت»، «قمار»، «بازی» (به معنای game)، «بازنده». استثنا: متن
حقوقی که این‌ها را برای **نفی کردن** دارد، و دستور ترجمه در `translate.ts`.

---

## ۳. ماژول‌های مشترک — قبل از نوشتن کد تازه ببینشان

| فایل | چه چیزی |
|---|---|
| `lib/iran.ts` | `moveFunds`، `recordRevenue`، تسویه، اسکیمای پول |
| `lib/withdrawal.ts` | **تنها مسیر برداشت** — سایت و ربات |
| `lib/withdrawal-sync.ts` | آشتی برداشت با درگاه + `refundWithdrawal` |
| `lib/deposit-address.ts` | آدرس واریز ذخیره‌شده (کش دائمی) |
| `lib/wallet-rules.ts` | `MIN_WITHDRAW` — **بدون ایمپورت** |
| `lib/ledger-labels.ts` | برچسب فارسی — **بدون ایمپورت** |
| `lib/poly-scoring.ts` | فرمول امتیاز — **بدون ایمپورت**، هرگز کپی نشود |
| `lib/tone.ts` | مرز لحن (فقط کامنت) |
| `lib/onboarding.ts` | قوانین + آموزش اول ورود + `TERMS_VERSION` |
| `lib/profile.ts` | داده‌ی پنل کاربری — هر سه سطح |
| `lib/bot-menu.ts` | متن و کارت‌های ربات |
| `lib/bot-flow.ts` | گفت‌وگوی چندمرحله‌ای + `claimConfirmedFlow` |

⚠️ **الگوی تکرارشونده:** هر مقدار یا برچسبی که در بیش از یک فایل کپی شود،
دیر یا زود از هم جدا می‌شود. امروز چهار بار اتفاق افتاد.

---

## ۴. آنچه در فاز ۴ انجام شد (۲۰ کامیت، یک روز)

**کارت تلگرامی ترید** — بازارهای پالی‌مارکت هم کارت قابل فوروارد گرفتند.
`marketPoll` دو-حالته شد (`kind: ir | trade`) چون ترید پول واقعی ندارد.

**مینی‌اپ** — دروازه‌ی پذیرش قوانین (با نسخه)، آموزش سه‌اسلایدی، چیدمان تازه‌ی
کیف پول.

**ربات** — صفحه‌کلید ثابت شش‌گزینه‌ای، منوی شیشه‌ای بدون تکرار.
⚠️ Bot API 10.2 هیچ فیلدی برای **رنگ دکمه** ندارد؛ تنها اهرم ایموجی است.

**پول دمو** — جداسازی کامل (بالا توضیح داده شد).

**اصلاح چهار باگ مالی:**
| باگ | چطور پیدا شد |
|---|---|
| `is_demo` قابل برداشت بود | ممیزی دور ۱ |
| `getWithdrawal` هیچ‌جا صدا نمی‌شد | ممیزی دور ۱ |
| تسویه ردیف بازیکن را قفل نمی‌کرد (**پول گم می‌شد**) | ممیزی دور ۲ |
| دابل‌تپ روی تأیید، **دو برداشت** می‌ساخت | ممیزی دور ۳ |

**نشت اطلاعات:** کیف پول و پروفایل در گروه تلگرام موجودی را لو می‌دادند — از
دو راه (دکمه و دستور متنی). هر دو بسته شد.

**کارایی** — آدرس واریز ذخیره شد: کیف پول از **۳ به ۱۸۳ درخواست در ثانیه**.
استخر اتصال از ۵ به ۲۰ و `connectionTimeoutMillis` اضافه شد.

**قفل خودکار بازار** — بازاری که مهلتش گذشته خودش `locked` می‌شود. چهار
بازار روی پروداکشن روزها جا مانده بودند.

**سقف پرداخت چالش** در هر سه سطح دیده می‌شود (`payoutCap` عددی شد).

---

## ۵. اقتصاد — اعداد قفل‌شده

**قبل از تغییر هرکدام از مالک بپرس.**

```
COMMISSION        = 0.03    # ۳٪ از حجم استخر
MIN_STAKE_USDT    = 1
MIN_ODDS          = 1.05
DISPUTE_HOURS     = 24
PROPOSE_FEE_USDT  = 1
MIN_WITHDRAW      = 10      # wallet-rules.ts
WELCOME_CREDITS   = 10 · POLY_FREE_PER_DAY = 5
REFERRAL_PERCENT  = 10 · REFERRAL_BONUS = 5 · GROUP_BONUS = 20
```

`ضریب برد = (کل استخر × ۰.۹۷) ÷ مجموع پیش‌بینی‌های طرف برنده`

### چالش پراپ

```
CONSISTENCY_PCT=35 · ELIGIBLE_PROB 0.25–0.75
MAX_COUNTED_PREDS=50 · MAX_ENTRIES_PER_30D=3
هدف=300 · افت=150 · ضرر روزانه=60 · حداقل ۲۵ پیش‌بینی در ۷ روز · ۳۰ روز
سقف پرداخت ماهانه: pr5k=150$ · pr10k=300$ · pr25k=750$ · pr50k=1500$
```

⚠️ شناسه‌ی تیرها (`fx250`, `pr5k`, …) هرگز عوض نشود — در دیتابیس‌اند.

### 🟡 یافته‌ی باز: هدف ۳۰۰ خیلی شل است

شبیه‌سازی (۲۰۰ هزار تکرار، با قوانین دقیق کد):

| پیش‌بینی در روز | نرخ قبولی **بدون هیچ مهارتی** |
|---|---|
| ۱ | **۱۱.۴٪** |
| ۲ | ۰.۰۷٪ |
| ۳ | ۲.۱٪ |

علتش: سقف ضرر روزانه ۶۰ است و در `p=۵۰` هر باخت ۵۰− — پس با **یک** پیش‌بینی
در روز، قوی‌ترین نگهبان سیستم هرگز فعال نمی‌شود.

با سقف پرداخت فعلی (۱۵۰۰$)، ضرر پلتفرم روی کاربر بی‌مهارت ‎+۵۵$ و روی کاربر
ماهر (+۱۰pp) ‎+۵۰۳$ در هر ورود است. کران‌دار ولی واقعی.

**تصمیم مالک: هدف ۳۰۰ برای دو-سه ماه اول بماند و با کاربر واقعی سنجیده
شود.** اگر مشکل‌ساز شد، ۶۰۰ نرخ بی‌مهارت را به ۱.۹٪ می‌رساند و تبعیض
مهارتی را از ۳.۶× به ۱۳.۸× می‌برد.

---

## ۶. یافته‌های باز

| # | مورد | شدت |
|---|---|---|
| **۱** | **واریز به پلتفرم نمی‌رسد** (بند ۱) | 🔴 |
| ۲ | برداشت واقعی **هرگز یک بار هم** انجام نشده | 🔴 |
| ۳ | وضعیت‌های Zovix در `withdrawal-sync.ts` **حدس زده شده‌اند** (`SUCCESS`, `FAILED`, …). مستندات حالا فهرست کامل دارد: `PENDING`, `PENDING_ADMIN`, `SENT_TO_BLOCKCHAIN`, `PENDING_CONFIRM`, `SUCCESS`, `FAILED`, `CANCELLED_BY_ADMIN`, `CANCELLED_BY_USER`, `REJECTED_BY_AML`. **با مستندات هم‌خوان کن.** | 🟡 |
| ۴ | آدرس واریز برای همیشه کش می‌شود؛ اگر درگاه روزی عوضش کند دیده نمی‌شود | 🟡 |
| ۵ | غبار اعشار در تسویه (`amt` و `backDemo` پیش از `moveFunds` گرد نمی‌شوند) | 🟡 |
| ۶ | ورودی چالش از MOONِ رفرال/هدیه عملاً رایگان می‌شود | 🟡 |
| ۷ | سقف نرخ در حافظه است؛ با هر دیپلوی صفر می‌شود (کارایی‌اش سنجیده شد و گلوگاه نیست) | 🟡 |
| ۸ | `/api/tg/me` هیچ مصرف‌کننده‌ای ندارد | 🟢 |
| ۹ | `referred_by` هیچ `CHECK` در سطح دیتابیس ندارد | 🟢 |
| ۱۰ | چندحسابی: سقف ۳ ورود به `tg_user_id` گره خورده | 🟢 |

### 🆕 مستندات Zovix — چیزهایی که باید بررسی شوند

مالک مستندات کامل را فرستاده. سه نکته که با کد فعلی نمی‌خواند:

1. **`/withdrawal/create` در مستندات Session API است**، ولی ما آن را زیر
   `/my-blockchain/` صدا می‌زنیم. کار می‌کند یا نه — **هرگز آزموده نشده**،
   چون هیچ برداشت واقعی انجام نشده.
2. **`POST /my-blockchain/withdrawal/cancel/{uuid}`** وجود دارد با پنجره‌ی
   ۴۵ ثانیه‌ای. ما اصلاً استفاده‌اش نمی‌کنیم. ارزش دارد برای «انصراف».
3. **`GET /my-blockchain/withdrawal/index?uuid=…`** — همان چیزی است که
   `getWithdrawal` می‌زند. این یکی طبق مستندات **واقعاً External است**، پس
   احتمالاً درست کار می‌کند.

---

## ۷. کوئری‌های سلامت — باید همیشه صفر برگردانند

```sql
-- ۱. موجودی (واقعی + دمو) = جمع دفترکل
SELECT count(*) FROM (
  SELECT p.id FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
  GROUP BY p.id, p.usdt_balance, p.demo_balance
  HAVING p.usdt_balance + p.demo_balance <> COALESCE(SUM(w.amount),0)) x;

-- ۲. موجودی دمو = جمع ستون demo دفترکل
SELECT count(*) FROM (
  SELECT p.id FROM players p LEFT JOIN wallet_ledger w ON w.player_id=p.id
  GROUP BY p.id, p.demo_balance
  HAVING p.demo_balance <> COALESCE(SUM(w.demo),0)) x;

-- ۳. استخر بازار = مجموع پیش‌بینی‌ها  ← مهم‌ترین
SELECT count(*) FROM (
  SELECT m.id FROM ir_markets m LEFT JOIN ir_bets b ON b.market_id=m.id
  GROUP BY m.id, m.yes_total, m.no_total
  HAVING m.yes_total <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='yes'),0)
      OR m.no_total  <> COALESCE(SUM(b.stake) FILTER (WHERE b.side='no'),0)) x;

-- ۴. سهم دمو هرگز از اصل بیشتر نباشد
SELECT count(*) FROM ir_bets WHERE demo_stake > stake;
SELECT count(*) FROM platform_revenue WHERE abs(demo_amount) > abs(amount);

-- ۵. هیچ موجودی منفی نباشد
SELECT count(*) FROM players WHERE usdt_balance < 0 OR demo_balance < 0;

-- ۶. اصل دمو نباید واقعی شده باشد (ردیف‌های پیش از ۱۵ اوت ۲۰۲۶ استثنا)
SELECT count(*) FROM ir_bets b
  JOIN wallet_ledger l ON l.player_id=b.player_id
   AND l.kind='ir_payout' AND l.ref='m'||b.market_id
 WHERE b.status='won' AND l.demo > b.demo_stake + 0.000001
   AND l.created_at > '2026-08-15';
```

آخرین اجرا روی **پروداکشن**: هر شش صفر.

---

## ۸. کارهای این فاز — به ترتیب

1. 🔴 **حل مسئله‌ی واریز** (بند ۱). تا این نشود، بقیه بی‌معنی است.
2. 🔴 **اولین برداشت واقعی با ۱۰ تتر.** تنها راه فهمیدن اینکه
   `withdrawal/create` با پیشوند `/my-blockchain/` کار می‌کند و Zovix چه
   رشته‌های وضعیتی برمی‌گرداند. تا آن موقع، `withdrawal-sync.ts` روی حدس
   ایستاده.
3. 🟡 هم‌خوان کردن وضعیت‌های برداشت با مستندات (یافته‌ی ۳).
4. 🟡 تنظیمات BotFather: عکس پروفایل، توضیح، دکمه‌ی منو، Mini App.
   ⚠️ برچسب دکمه‌ی منو الان «نارموون» است (یک «و» اضافه) — مالک باید در
   `/setmenubutton` اصلاحش کند.
5. 🟡 لحن صفحه‌های بازاریابی سایت.
6. 🟢 `robots.txt` و `sitemap.xml` وجود ندارند؛ `metadataBase` و لینک‌های
   هاردکد باید به دامنه‌ی جدید بروند. **پس از تغییر `SITE_URL`، وبهوک تلگرام
   باید دوباره ثبت شود** (دکمه‌اش در `/admin`).

---

## ۹. کارهای مالک (بیرون از کد)

1. 🔴 **رمز دیتابیس Railway را عوض کن** — در چت قبلی لو رفته.
2. 🔴 **پیگیری Zovix** — سؤال‌های بند ۱ را بپرس.
3. 🟡 **دامنه‌ی `narmoon.io`** — منقضی شده بود؛ مالک گفت فعلاً بی‌خیال.
4. 🟡 برچسب دکمه‌ی منوی ربات را در BotFather اصلاح کن.
5. ✅ کرون Railway هر ۱۵ دقیقه — کار می‌کند.
6. ✅ `TG_ADMIN_IDS` ست شده.

---

## ۱۰. دستورها و محیط

```bash
npm run dev      # لوکال روی :3000
npm run build    # بیلد production
npm run lint     # eslint — baseline ۳۲
npx tsc --noEmit # تایپ‌چک
```

⚠️ میدل‌ور در **`src/middleware.ts`** است، نه ریشه.

⚠️ **`TG_BOT_TOKEN` در `.env.local` معتبر است** (برخلاف سند فاز ۴). یعنی
مسیرهای تلگرام لوکال قابل تست‌اند و می‌شود `initData` واقعی ساخت.

⚠️ **کلیدهای Zovix در `.env.local` واقعی‌اند.** هر تستی که به
`createWithdrawal` برسد یک برداشت واقعی می‌سازد. روال امن: موجودی کاربر
تستی را **صفر** بگذار، یا مبلغی بزن که قطعاً رد شود.

⚠️ **مینی‌اپ بدون `initData` باز نمی‌شود.** برای دیدن کامپوننت‌ها صفحه‌ی
موقت در `src/app/app/zzpreview/page.tsx` بساز و **پیش از کامیت پاکش کن**.

متغیرها: `DATABASE_URL`, `SESSION_SECRET` (اجباری), `ADMIN_SECRET`,
`ADMIN_PASSWORD`, `PGSSL`, `PGPOOL_MAX`, `SETTLE_KEY`, `ZOVIX_*`,
`TG_BOT_TOKEN`, `TG_BOT_USERNAME`, `TG_WEBHOOK_SECRET`, `TG_ADMIN_IDS`,
`SITE_URL`, `GEMINI_API_KEY`, `MYFXBOOK_*`, `NEXT_PUBLIC_DEV_FIXTURES`.

---

## ۱۱. خطوط قرمز محصول (تصمیم قطعی مالک)

- ادعای «سود تضمینی» هرگز. وعده‌ی «از پیش‌بینی پول دربیار» هرگز.
- ایردراپ و توکن نه. یوزر و لیدربورد فیک نه.
- **امتیازدهی صفر-انتظار حفظ شود.** (تک‌پیش‌بینی و کمبو هر دو ریاضی
  آزموده شده‌اند و صفرند.)
- استخر parimutuel تتری بازار ایران با تصمیم صریح مالک مجاز است.
- ورود با گوگل و KYC هر دو رد شدند.
- **برداشت هرگز به دلیل بلاک‌بودن ربات قفل نمی‌شود.**
- **پول دمو هرگز قابل برداشت نیست؛ فقط سودش.**
- واژگان شرط‌بندی برنگردد.

---

## ۱۲. ممیزی‌های انجام‌شده

چهار دور با ایجنت مستقل. پرامپت‌هایشان در ریشه‌ی ریپو هستند
(`AUDIT-PROMPT.md` تا `AUDIT-PROMPT-4.md`) و الگوی خوبی برای دورهای بعدی‌اند.

| دور | دامنه | یافته‌ی واقعی | چیزی که ممیز جا انداخت |
|---|---|---|---|
| ۱ | کل ریپو | `is_demo` قابل برداشت · `getWithdrawal` بی‌فراخوان | علت hydration را غلط حدس زد |
| ۲ | دیف مسیر پول | قفل‌نداشتن تسویه | راه‌حلش بن‌بست ABBA می‌ساخت |
| ۳ | ربات و احراز هویت | دابل‌تپ دو برداشت | نشت `/wallet` از راه دستور متنی |
| ۴ | اقتصاد چالش | صفر-انتظار کمبو تأیید شد | سوراخ «یک پیش‌بینی در روز» را ندید |

**درس مشترک:** هر دور یافته‌ی واقعی داد و هر دور چیزی هم جا انداخت. هیچ
گزارشی را بی‌بازسنجی نپذیر — نه مال ایجنت، نه مال خودت.
